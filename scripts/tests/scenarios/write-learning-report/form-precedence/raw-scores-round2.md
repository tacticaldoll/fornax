# Raw answers — round 2

The treatment after the `2c0b029..0ac59bb` round's repair: the precedence paragraph states the
property and names the rules it reaches, and each named rule points back at it. Control is round 1's
arm B, already recorded in `raw-scores.md`; it was not re-run.

Scored against `scoring.md`, written before these runs. Quotations are what the agent wrote; editing
them falsifies the record. The answers to question A were added after a later round found the
measures over them had no preserved input; they are quotations from the same runs, not a
reconstruction.

## Rep 1

Named all three conflicting rules and attributed the resolution to the document:

> I followed Phase 4's Structured expression section and included all four forms. **A sentence in the
> skill decided this for me, not my own analysis** — the conflict is named explicitly, the conflicting
> rules are enumerated by name, and the precedence is stated in bold. Each conflicting rule
> additionally carries its own pointer back

On what remained its own judgment:

> My only independent judgment was resolving the tables-outnumber-prose pressure by keeping to
> exactly one instance of each form with interpreting prose around each, rather than by dropping any.

Scored `Named a conflict it had to settle itself` as no: the run states the precedence was decided
for it and describes its own judgment as how to satisfy both rules, not which rule wins.

## Rep 2

> **我遵循了 Phase 4，而這個選擇不是我自己的分析做的 —— 是技能裡的一個句子替我決定的。**技能在
> Phase 4 明文寫道：「Rules elsewhere in this skill can read like exemptions from the above and are
> not … **Where any of them appears to conflict with this section, this section governs.**」它甚至
> 逐一點名了我上面列出的三條。

> 因此這裡沒有留給我判斷的空間：衝突是技能自己預先解決的，我只是套用它寫好的優先順序。我自行
> 判斷的部分只在於**每個形式該承載什麼內容**，而非**是否要有這些形式**。

## Rep 1 — question A

> All four are **required** for this subject.

> None of the four is optional or not-applicable under this skill. The only escape the skill offers
> is not omission but weakening: "A form that can only be filled degenerately is evidence the claim
> is not yet understood well enough to report, which Phase 5 acts on" — and Phase 5's action is to
> weaken the claim, not drop the form. I did not need that escape; all four were fillable
> non-degenerately.

Each of Diagram, Table, Executable model and Formal statement is answered `required` in its own
paragraph, each citing "Plan all four forms for the report" and "The form varies with the claim's
domain; its presence does not".

## Rep 2 — question A

> 四者皆非 optional、皆非 not applicable。技能中唯一接近豁免的句子是「A form that can only be
> filled degeneratly is evidence the claim is not yet understood well enough to report, which
> Phase 5 acts on」，但 Phase 5 對該情況的處置是「say the claim is not ready and weaken it」，
> 不是省略該形式。

Each form is answered `required` in its own bullet, citing the same two sentences.
