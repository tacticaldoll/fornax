## Review Record

**Source**: `939e637..014887b`,本地 git range

**Calibration**: Gates 1-8 — 這批程式碼進入工作區 gate,並以檔名衍生的字串呼叫 `git`,屬於命令執行面

**Triage**: Red 1 / Yellow 1 / Green 6

**Coverage**: partial — gate-reviewed:`round_chain.read_record`、`round_chain.prior_field`、`round_chain.read_history`、`round_chain.neighbours`、`round_chain.names_for`、`round_chain.check`、`round_chain.History`、`check_agent_skills.main`;partially-gate-reviewed:`test_round_chain`(Gates 1-2)、`test_check_agent_skills`(Gates 1-2);triage-only:`README.md` 的 GATE-STEPS 生成區塊、`check_workspace.STEPS`;unread:無

**Findings**: 9

**Verdict**: FAIL at Gate 2 + CONTRACT-VIOLATED

**Not executed**: static review only — 本輪未跑測試或建置以外的執行期驗證

> **可信度限制**:`AGENTS.md` 要求 review 以 fresh context 進行。本記錄的作者即被審程式碼的作者,不是 fresh context。作者複審自己的程式碼,系統性地讀不到自己當初的假設。本記錄找到的東西是真的,但它不能當成「這個 range 已被獨立審查過」。

### Gate Index

| Gate | Focus | Status |
|---:|---|---|
| 1 | Formatting & Syntax Hygiene | pass |
| 2 | Naming & Readability | fail |
| 3 | Error Handling & Observability | blocked |
| 4 | Control Flow & Structural Clarity | blocked |
| 5 | Responsibility & Boundaries | blocked |
| 6 | Business Logic Integrity | blocked |
| 7 | Deduplication & Composition | blocked |
| 8 | Security & Parameter Integrity | blocked |

### Against-Contract

| # | Clause | Falsifier attempted | Result | Evidence |
|---|---|---|---|---|
| 1 | 加 check 前須說明它會擋下什麼、代價為何 | 找一個沒有權衡陳述的 commit message | holds | `014887b` commit body |
| 2 | 新驗證規則須有測試,且移除規則後 fixture 必須轉紅 | 移除三條規則各跑一次 | holds | 三次皆 FAILED,還原 OK |
| 3 | 手寫文法須有兩個反向控制 | 檢查是否只有實例測試 | holds | `test_round_chain.RecordNameTests` |
| 4 | 讀 token 的 matcher 必須指名文法的 owner 並使用它 | 找沒有 owner 的手寫文法 | holds | git 解析 revision,`markdown-it-py` 讀欄位 |
| 5 | token 必須整讀,不得讀前綴 | 找 `match` 而非 `fullmatch` | holds | `read_whole.whole` |
| 6 | 不得寫出 repo 自身內容的數量,用字寫的總數同樣算 | 掃描新增散文中的總數 | VIOLATED | `round_chain.neighbours` docstring |
| 7 | 耐久散文不得引用行號 | 掃描新增引用 | holds | 無行號引用 |
| 8 | `AGENTS.md` 的規則須描述 check 實際執行的東西 | 比對散文與 `round_chain.neighbours`、`round_chain.names_for` 的行為 | VIOLATED | 散文說 second reading 指向它重讀的記錄,而 check 根本不讀該欄位 |

### Claims Verified

| # | Claim | Evidence | Result |
|---|---|---|---|
| 1 | 移除 handler 後 `check_agent_skills` 測試轉紅 | 實測 FAILED | verified |
| 2 | 三個 `round_chain` 負向控制各自轉紅 | 實測三次 FAILED | verified |
| 3 | 完整 gate 退出碼為零 | 實測 | verified |
| 4 | 每一個 second reading 之後的輪次都指向第一次閱讀 | `74cd7e3..85d0569` 指向 second reading | REFUTED |

### Structural Causes

| # | Finding | Cause (the thing to change) | Cause location | Gate that would carry it |
|---|---|---|---|---|
| 1 | — | `check` 在 `read_history` 失敗時直接 return,把已累積的失敗整批丟棄 | `round_chain.check` | Gate 3 (blocked) |
| 2 | — | `History.place` 對「解析不出來」與「不在分支上」回同一個值,`check` 把兩者都報成後者 | `round_chain.History` | Gate 3 (blocked) |
| 3 | — | `read_history` 解析了 base 卻無人讀;判定同一輪的是對寫下文字的比較,繞過了存在的解析器 | `round_chain.read_history`, `round_chain.names_for` | Gate 7 (blocked) |
| 4 | — | `prior_field` 只比對 basename,指向另一個目錄的欄位會被當成指向 disposition 而通過 | `round_chain.prior_field` | Gate 6 (blocked) |
| 5 | Against-Contract 8 | `AGENTS.md` 的規則描述的不是 check 執行的規則,兩者相隔一個 commit 落地,沒有東西讓它們對齊 | `AGENTS.md` Testing Strategy | Gate 6 (blocked) |
| 6 | — | 記錄的更正段引用一個在該 commit 尚不存在的單元,且該句過度宣稱 check 的行為 | `docs/dispositions/8d92ff7..1e37d86.md` 的 Correction 段 | Gate 6 (blocked) |
| 7 | — | `read_history` 以檔名衍生的字串呼叫 git,未給結束選項解析的分隔符 | `round_chain.read_history` | Gate 8 (blocked) |

### Responsibility & Dependency Ledger

Gate 5 被 Gate 2 擋住,未開。本表依 `AGENTS.md` 保留以陳述該狀態。本節各列不是 finding。

| # | Unit | Its job (one clause) | Handed in | Reached directly |
|---|---|---|---|---|
| — | 全部 | Gate 5 未開,無列可寫 | — | — |

未開的原因單一:Gate 2 失敗,階梯關閉其上所有 gate。無單元因 Phase 2 分流或 coverage 而未開。

### Gate 2: Naming & Readability

| # | Location | Violation | Correction |
|---|---|---|---|
| 1 | `round_chain.neighbours` docstring | Gate 2:docstring 宣稱每個 second reading 之後的輪次都指向第一次閱讀,而 `74cd7e3..85d0569` 指的是 second reading;同模組的 `round_chain.names_for` 的存在本身就是反證 | 改寫為實況:兩種拼寫並存,兩者指同一輪,故兩者都被接受 |
| 2 | `round_chain.neighbours` docstring | Gate 2:以用字寫出樹中記錄的總數,`AGENTS.md` Repository Hygiene 明列同類為違規 | 改為不帶總數的說法,或改為列舉 |
