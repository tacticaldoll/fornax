# Review Record — `8a45707..2c1b448`

Archived as received, from a review run in fresh context because the author of the range could not
supply one. Kept so the reconciliation in `docs/dispositions/8a45707..2c1b448.md` can be checked
against its input rather than taken on trust.

Not byte-guaranteed and not reformatted: the record reached this triage inside a message, so what
follows is a transcription of that message, condensed in its table prose and complete in its
findings, verdict, coverage and evidence. It is fenced, which preserves it and keeps its line
citations as text rather than as claims this repository makes. Those citations are correct for a
Review Record — `AGENTS.md` exempts a record's own evidence column.

```text
## Review Record

Source: git diff 8a45707..HEAD (d0f61da, 09cc1e7, 8d63253, e15a5e1, 6f42e63, 2c1b448; HEAD
2c1b448), against AGENTS.md, PROJECT.md, docs/dispositions/e144212..05025bc.md,
docs/reviews/e144212..05025bc.md
Calibration: Gates 1-8 (read_whole.shell_words reads shell command text out of a CI workflow and
its result decides what runtime_contract compares a pin against — a command/untrusted-input
boundary)
Triage: Red 0 / Yellow 4 (read_whole.py, docs/guards.md, development-knowns.yaml,
test_module_claims.py) / Green 3 (test_read_whole.py, test_record_shape.py,
test_check_citations.py)
Coverage: partial — gate-reviewed: read_whole.shell_words, read_whole.COMMENT,
test_module_claims.TESTS, test_module_claims.entry_point_out_of_place,
test_module_claims.EntryPointPlacement, test_read_whole.ShellWordTests (the two added cases),
test_read_whole.RequirementsComment, the entry-point block placement in test_read_whole.py,
test_record_shape.py and test_check_citations.py; partially-gate-reviewed: docs/guards.md (Gate 1
only — it failed Gate 1, which blocked 2-8 for it), development-knowns.yaml
shell-command-structure-read-by-hand (Gates 1, 2, 6 — a YAML prose field offers no subject for
Gates 3, 4, 5, 7, 8); triage-only: none; unread: none. Every changed path accounted for.
Findings: 26 — 1 Gate 1 row, 11 Gate 6 rows, 9 Against-Contract rows reading VIOLATED, 3 Claims
Verified rows reading REFUTED, 2 reading partial. Rows reading holds or verified, the Triage file
counts, and the Ledger rows are excluded; every Structural Causes row names a finding, so none
adds to the count.
Verdict: FAIL at Gate 1 + SECURITY-ALERT + CONTRACT-VIOLATED + CLAIM-REFUTED
Not executed: CI-only steps (node syntax check, tools/fornax-cli suite) and no deployment path.
Run: check_workspace.py (green, 498 tests), ruff, the unit suite, the registry verification field's
own command, a bash differential over tens of thousands of constructed commands, and every revert
each new docs/guards.md row names, in a throwaway clone at 8a45707, d0f61da, e15a5e1 and HEAD.

Gate Index: 1 fail, 2 pass, 3 pass, 4 pass, 5 pass, 6 fail, 7 blocked, 8 blocked.
Gates 1 and 6 are worst-case aggregates. Gate 1 fails only for docs/guards.md. Gate 6 fails for
read_whole.shell_words, development-knowns.yaml and test_module_claims.py. Gates 7-8 opened and
passed for the units whose Gate 6 passed and are blocked only for the three that failed it.
Gates 2-8 are blocked for docs/guards.md alone.

Security Triage Alert
  read_whole.shell_words returns a shorter, well-formed word list than the command holds when a
  hash follows an escaped punctuation character, and the shortfall is silent.
    run: find . -name '*.pyc' -exec rm {} \;#note && pip install ruff==9.9.9
    bash executes pip install ruff==9.9.9
    shell_words returns ['find','.','-name','*.pyc','-exec','rm','{}',';']
    workflow_pins therefore returns ([], []) — no pin, no Unread
  The workspace gate passes while the workflow installs a version
  requirements-maintenance.txt does not declare, and nothing is reported. Same alert the prior
  round raised, reopened in the branch this range added. read_whole.py:157

Against-Contract (VIOLATED rows)
 1 AGENTS.md: a read is Whole or Unread, never a shorter well-formed value | pip install \;#c
   ruff==9.9.9 returns ['pip','install',';'], not an Unread | read_whole.py:157
 2 AGENTS.md: two negative controls, a near-miss and a valid alternate spelling |
   entry_point_out_of_place compares ast.unparse output to one literal; if "__main__" == __name__:
   and if __name__ in ("__main__",): both return False above a class | test_module_claims.py:73
 3 Same clause for COMMENT's new class | both controls are near-misses sharing the accepted
   character; the accepted-side alternate spelling, a hash after a tab, has none |
   test_read_whole.py:200
 4 Same clause for the begins-a-word rule | the operator branch has no negative control, and that
   is exactly where the defect is | read_whole.py:160
 5 AGENTS.md: after repairing, sweep by enumerating the mechanism | the sweep was 106 constructed
   commands; enumerating the three branches reaches the escape/operator interaction on the first
   try. Re-run as a differential: 525 silent shortfalls out of 16,358 accepted commands, in two
   causes | read_whole.py:157
 7 AGENTS.md: a message's number must not become the tree's | e15a5e1's message says thirty-three
   against fifty-two; the same figures are now in an undated docstring, already stale at 52/52 |
   test_module_claims.py:64
 8 AGENTS.md: no count of what the repository contains, a total spelled as a word is the same
   claim | "most of this tree has none" — 23 test modules, 20 carry a block |
   test_module_claims.py:134
11 docs/guards.md: a revert has to be what the code was | running the row's instruction as written
   gives 2 red, not the 1 recorded; reverting only the condition gives 1 | docs/guards.md:267
12 AGENTS.md: before adding a check, say what it costs | EntryPointPlacement states no standing
   cost: it reports a correctly-placed block followed by any trailing statement, holds one
   spelling, and silently excludes tools/fornax-cli/tests/ | test_module_claims.py:107

Against-Contract (holds): 6 guards-row completeness, 9 no path:line, 10 absent owner registered,
13 no version bump, 14 language policy, 15 commit style, 16 the verification field runs and
surfaces read_whole.py:151, 17 dated sections quotable with their own tree, 18 both prior records
persisted.

Claims Verified
 1 QUOTED-HASH-CUT-BEFORE-THE-LEXER repaired by 1a + 1b | partial — the reported instance is fixed
   and verified, but the disposition's cause, "infers a shell word boundary from a scan token
   boundary", survives in the operator branch and is refuted by echo a\;#b
 3 MATCHER-ENUMERATION-BLIND-TO-NON-REGEX repaired by 3a | partial — the widened search surfaces
   shell_words, but it enumerates six spellings where the cause named three forms; the slice and
   the membership loop are still unreachable
15 severed-guard section: "this repair moved no count but its own" | REFUTED — the row at
   docs/guards.md:250 names read_whole.COMMENT in its own revert instruction and was not re-run
16 prospective section: "One has since been repaired and has moved to the measured section below"
   | REFUTED — all six were repaired; all three guarded rows were removed, leaving a header with
   no rows, and the measured sections are above this heading, not below it
17 d0f61da: "Measured against bash over 106 constructed commands ... this one on none" | REFUTED —
   525 of 16,358 accepted commands are read short with no Unread. The 107th is echo a\;#b
 Verified: 2 GUARD-DIED repaired by 2b plus a direct class, 4 LEDGER-NAMES repaired,
 5 STATEMENT-OVERRUN repaired, 6 DIRECT-RUN repaired (21/21, 52/52, 32/32), 7-14 and 18-25 the
 guards readings, the workflow reading, the retirement's inertness, and 2c performed by hand.

Structural Causes
 1 Stop asking a non-posix shlex scan where a word begins. It does not resolve escapes and groups
   punctuation without knowing whether bash would treat the run as an operator, so every property
   read off its tokens is a proxy. Either take the boundary from a reader that resolves escapes,
   or refuse any command whose text before the hash holds a backslash at all | read_whole.py |
   Gate 6
 2 shell_words now carries a second, hand-written word-boundary grammar beside shlex's, answering
   a question shlex already answers — the duplication its own docstring argues against. Removing
   the second grammar rather than widening it is what ends the recurrence | read_whole.py | Gate 7
 3 Nothing re-measures a guards row when a different repair changes the unit that row's revert
   instruction names. Key the rows by the symbols their instructions name | AGENTS.md | Gate 6
 4 The registry's enumeration is keyed to how a decision is spelled, not to what it decides.
   Widening the spelling list is the same move as widening a terminator list |
   development-knowns.yaml | Gate 6
 5 entry_point_out_of_place decides a Python construct by string-comparing ast.unparse output, and
   decides its file set by a glob on its own directory | test_module_claims.py | Gate 6

Gate 1
 1 docs/guards.md:334 — the prospective section was spliced into rather than rewritten. One
   163-character line where the file wraps at 100, a sentence that does not close, an empty table
   under a paragraph promising rows, and "the measured section below" naming sections above. The
   repository has reported this class twice. Correction: delete the heading and its empty table,
   every accepted finding of that round having been repaired.

Gate 6
 1 read_whole.py:157 — operator asks whether every character of the previous scan token is in
   punctuation_chars, but an escaped punctuation character arrives as its own scan token. In
   echo a\;#b the scan yields echo, a\, ;, #b; previous is ;, the endswith guard looks at the
   wrong token, and the hash is cut where bash opens no word. 506 of 525 measured shortfalls.
 2 read_whole.py:157 — the same branch treats any all-punctuation run as ending a word, but bash
   does not end one at a command or process substitution. echo a$()#b and echo a>()#b both print
   a#b; the module drops the word. 19 of 525.
 3 read_whole.py:130 — the docstring states "Where that question cannot be answered the command is
   refused", refuted by finding 1.
 4 development-knowns.yaml:49 — the field says its enumeration is every place that decides where a
   token of a borrowed grammar ends, then names a grep over six spellings. Run against the tree it
   returns no line from read_whole.py:157 or from the ref terminator in install_refs.
 5 test_module_claims.py:64 — the docstring carries figures transcribed from a commit message,
   already stale.
 6 test_module_claims.py:134 — "most of this tree has none" is false and is a total over the
   tree's artifacts spelled as a word.
 7 test_module_claims.py:73 — the check recognises one spelling of the guard.
 8 test_module_claims.py:112 — the glob is scripts/tests alone; tools/fornax-cli/tests/ is
   silently outside and the bound is stated nowhere.
 9 test_module_claims.py:107 — the class docstring asserts "or it collects less", which does not
   hold for the shape the check reports.
10 test_read_whole.py:208 — the comment calls a case "the alternate spelling of the same meaning"
   where it asserts the line is not cut; it is a second near-miss.
11 test_read_whole.py:146 — CommentRule's docstring cites a development-knowns.yaml wording this
   same range removed.

Handoffs: settling these belongs to triage-findings — in particular whether the operator branch is
repaired or removed, and whether repair 2c is adopted as a rule rather than performed by hand. The
divergence class is characterised by measurement now; designing the case set that holds it belongs
to plan-testing.
```
