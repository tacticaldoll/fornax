# Review Record — `v0.4.1..7e2da1e`

The text this round's triage was handed, as it arrived. Kept so the reconciliation in
`docs/dispositions/v0.4.1..7e2da1e.md` can be checked against its input rather than taken on trust.

Not byte-guaranteed and not reformatted: the record reached this triage inside a message, so what
follows is a transcription of that message. It is fenced, which preserves it and keeps its
absolute-path links and line citations as text rather than as claims this repository makes.

````text
Review Record
Source: local git diff v0.4.1..HEAD, HEAD 7e2da1e. This round's own inspection is over 1609403..HEAD — the repair commit aecf03d (11 files) and the record commit 7e2da1e (2 files). The unchanged remainder of the range carries the coverage stated in docs/reviews/v0.4.1..1609403.md and is not re-claimed here.
Calibration: Gates 1–8 (unchanged — the repaired unit distribution_manifest.install_documents sits inside the validator the deployment CLI runs against a release snapshot, and evidence_currency.fingerprint resolves and reads registry-supplied paths)
Triage: Red 0 / Yellow 6 / Green 7
Coverage: partial — gate-reviewed (Gates 1–8): distribution_manifest.install_documents, distribution_manifest.validate_install_pins, evidence_currency.Fingerprint, evidence_currency.fingerprint, evidence_currency.check, evidence_currency.main, check_citations.Modules, check_citations.module_symbols, check_citations.citations, check_citations.check, check_citations.citable, markdown_links.heading_texts, the module docstrings of markdown_links, diagnostic_text and generated_block, seam_contract's import block, docs/guards.md, the two edited AGENTS.md clauses, test_evidence_currency.DriftTests.test_each_way_a_fingerprint_fails_says_which_one, test_evidence_currency.DriftTests.test_a_fingerprint_cannot_hold_both_a_digest_and_a_reason, test_validate_skills.ProjectedDescriptionTests.test_a_non_worktree_validates_from_the_filesystem, test_validate_skills.ProjectedDescriptionTests.test_a_non_worktree_still_reports_a_stale_pin; partially-gate-reviewed: docs/dispositions/v0.4.1..1609403.md (Gates 1–2), docs/reviews/v0.4.1..1609403.md (Gates 1–2); triage-only: none; unread: none within 1609403..HEAD. Outside that, the remainder of v0.4.1..HEAD is unread this round.
Findings: 9 (7 gate rows + 2 VIOLATED contract rows; holds, verified, partial and Ledger rows excluded. Contract row 2 keys to gate findings 1 and 4 — one defect, two tracks, left undeduplicated as Phase 4b requires)
Verdict: FAIL at Gate 1 + CONTRACT-VIOLATED
Not executed: static review only — the workspace gate, the test suite, Ruff and the build were not run.

Gate Index
Gate	Focus	Status
1	Formatting & Syntax Hygiene	fail
2	Naming & Readability	pass
3	Error Handling & Observability	fail
4	Control Flow & Structural Clarity	pass
5	Responsibility & Boundaries	fail
6	Business Logic Integrity	fail
7	Deduplication & Composition	fail
8	Security & Parameter Integrity	pass
Per file: distribution_manifest.py and test_evidence_currency.py fail Gate 1 (2–8 blocked); evidence_currency.py fails Gate 3 (4–8 blocked); markdown_links.py and test_validate_skills.py fail Gate 5 (6–8 blocked); docs/guards.md fails Gate 6 (7–8 blocked); check_citations.py fails Gate 7 (8 blocked); diagnostic_text.py, generated_block.py, seam_contract.py and AGENTS.md pass every gate opened.

Against-Contract
#	Clause	Falsifier attempted	Result	Evidence
1	docs/guards.md: "Every finding a round accepted in this range has a row, closed ones included"	Name a finding accepted in this range with no row	VIOLATED — the round settled at 7e2da1e accepted nine findings; docs/guards.md gained rows for two of them (SNAPSHOT-NEEDS-A-WORKTREE, FINGERPRINT-COLLAPSED-NONE) and a prose entry for a third (ORDINAL-IN-NEW-MODULE). MODULE-MAP-REBUILT, FORMATTING-DRIFT, SUBJECTS-STATED-TWICE, GUARDS-INCOMPLETE, ROUND-CLAUSE-TOO-WIDE and GUARD-NOT-A-SYMBOL have no row in either table. The commit that wrote the completeness sentence is the commit that violates it — the third consecutive instance of the Pattern its own record names	docs/guards.md vs docs/dispositions/v0.4.1..1609403.md Dispositions
2	AGENTS.md: "a claim nothing reads, a helper nothing calls: each is worse than its absence, and deleting one closes a finding as properly as fixing one does"	Find a capability introduced in this range that no caller reaches	VIOLATED — validate_install_pins's documents parameter and heading_texts' depths parameter are supplied by nothing in scripts/, scripts/tests/ or tools/. Keys to gate findings 1 and 4	scripts/distribution_manifest.py, scripts/markdown_links.py
3	AGENTS.md: the citation rule's subject is check_citations.SUBJECTS, not a prose enumeration beside it	Find a subject the checker reads that the rule contradicts	holds — the rule now names the code and stops restating the set	AGENTS.md, check_citations.SUBJECTS
4	AGENTS.md: "A round is a review and its settlement … work done without a review … falls inside the range of the next review"	Find a repair commit stranded without a record	holds — docs/reviews/v0.4.1..1609403.md and docs/dispositions/v0.4.1..1609403.md exist and aecf03d falls inside this review's range	git log --name-status 1609403..HEAD
5	AGENTS.md: "Persist the Review Record … and name it from the Disposition Record's Source"	Compare the filename against the Source line	holds — Source names docs/reviews/v0.4.1..1609403.md, the file exists, is fenced, and states it is a transcription rather than byte-exact	docs/dispositions/v0.4.1..1609403.md, docs/reviews/v0.4.1..1609403.md
6	AGENTS.md: "Cover a new or changed validation rule in scripts/tests/"	Find a new rule with no case	holds for the rule; each Fingerprint branch is not covered — see finding 2	scripts/tests/test_validate_skills.py, scripts/tests/test_evidence_currency.py
7	AGENTS.md: "Use the Python standard library by default" and avoid hidden assumptions about the host	Find a shell-out where the stdlib answers	VIOLATED at gate finding 5 — reported there rather than duplicated here, because the clause and the gate ask the same question of the same two lines	scripts/tests/test_validate_skills.py
8	AGENTS.md: "After repairing a defect, sweep the repository for the same class"	Apply the sweep the Fingerprint repair establishes — a function returning None for several reasons	holds — section_text, module_symbols and _read each return None for one reason; get_top_level_yaml_value is the one survivor and skill_yaml's docstring already records it as not-yet-three-state	scripts/skill_yaml.py, scripts/evidence_currency.py, scripts/check_citations.py

Claims Verified
#	Claim	Evidence	Result
1	"a git archive export of this repository, validated by the shipped script, exited 1 before and exits 0 after"	install_documents falls back to rglob when listed errors; two cases assert the pass and the still-detected stale pin	verified (statically — the export itself was not run)
2	"the bool helper collapses ABSENT into OUTSIDE, so this caller asks path_boundary directly"	fingerprint reads Verdict.ABSENT and Verdict.INSIDE separately; resolved_inside keeps its bool for records_missing	verified
3	"143 map builds and 165 parses per run before, 1 and 66 after … 0.77s to 0.26s"	The map is built once in check and threaded; Modules.read caches per module. The before-count of map builds matches this review's own prior measurement; the parse counts and wall time were not re-run	partial
4	"Each reason is a subtest: a path outside the tree, a file that is not there, a heading that is absent"	Three subtests exist; the fourth and fifth reasons (could not be read, resolves … the repository) have none, and the symlink fixture built for the last is never used	partial
5	"Nine rows added. Six were measured today … Three name prose units"	Six newly-measured guarded rows plus three ids appended to an existing prose row — the arithmetic resolves, but only three of the nine additions are rows; the guarded table gained eight	partial
6	"The row names the test. The checker verifies it" (GUARD-NOT-A-SYMBOL)	VERDICT-CONTROL now cites test_validate_skills.EntryPointTests.test_a_mismatched_sidecar_publisher_fails_the_entry_point, which exists in that class	verified
7	Every guard symbol added resolves	All nine new test symbols exist in the classes named; check_citations.URL_AUTHORITY, .citations, .check, .LINE_CITATION, .citable, distribution_manifest.install_documents, evidence_currency.fingerprint all exist	verified
8	Prior findings 1–8 repaired	Import order swapped, both guards.md prose lines now ≤100, the three docstrings reworded, Fingerprint split, the map threaded, install_documents extracted	verified

Structural Causes
#	Finding	Cause (the thing to change)	Cause location	Gate that would carry it
1	1, 4	Add the caller the seam was extracted for, or delete the seam — a parameter introduced "for testability" that no test supplies is the capability the hygiene rule names	scripts/distribution_manifest.py validate_install_pins, scripts/markdown_links.py heading_texts	Gate 1 (fail) / Gate 5 (fail)
2	2, 3	Enumerate Fingerprint's reasons and give each one a case — the repair split four answers into five and the suite covers three, so two branches shipped unread and one of them ships ungrammatical text	scripts/tests/test_evidence_currency.py DriftTests	Gate 1 (fail) / Gate 3 (fail)
3	6, contract row 1	Make the guards ledger's own completion part of the settling turn rather than of the repair turn: aecf03d wrote the rows it could see and 7e2da1e wrote the findings list, so the ledger was completed before the list it must satisfy existed	AGENTS.md Testing Strategy, the guards clause	Gate 6 (fail)
4	7	Give citable and module_symbols one parse per module — they read the same files for two questions and disagree about what an unreadable one means, which is the "same question answered twice" the sweep rule enumerates	scripts/check_citations.py citable	Gate 7 (fail)
5	—	install_documents' two branches are not equivalent: git excludes what the workspace ignores and rglob does not, and nothing says which branch answered. No instance exists in this tree — it carries no ignored .md file — so this is recorded, not raised	scripts/distribution_manifest.py install_documents	Gate 6 (fail)

Responsibility & Dependency Ledger
Gate 5 opened over the units aecf03d changed. It did not open for the rest of v0.4.1..HEAD, which this round did not inspect (the coverage line says so, and docs/reviews/v0.4.1..1609403.md carries that ledger); nor for docs/dispositions/v0.4.1..1609403.md and docs/reviews/v0.4.1..1609403.md, whose partial coverage stops at Gate 2. Layering is inferred, as before.

Rows in this section are not findings.

#	Unit	Its job (one clause)	Handed in	Reached directly
1	distribution_manifest.install_documents	name every Markdown file under a root	root (parameter)	workspace_files.listed (which reaches git), Path.rglob
2	distribution_manifest.validate_install_pins	require every documented install pin to name the release being shipped	root, repository, version, documents (parameters — documents is supplied by nothing)	PINNED_INSTALL_DOCS (module constant), install_documents, stdout
3	evidence_currency.Fingerprint	hold a digest or the reason there is none	_digest, reason (constructor)	none
4	evidence_currency.fingerprint	hash the text an entry claims to have measured, or say why it could not	root, tests, heading (parameters)	Boundary.at and resolve_within (constructed inline), the filesystem, hashlib
5	evidence_currency.check	report each entry as current, superseded, or drifted	root, entries (parameters)	stdout
6	evidence_currency.main	parse the two modes and report	argv (parameter)	ROOT, REGISTRY (module constants), stdout/stderr
7	check_citations.Modules	hold the module map, the colliding stems, and what each module defines once read	by_stem, collisions, read (constructor)	none
8	check_citations.module_symbols	say what a module defines, or why this could not	known, module (parameters)	the filesystem, ast
9	check_citations.citations	report every citation in one file naming a line or an undefined symbol	known, root, path, external (parameters)	LINE_CITATION, URL_AUTHORITY, SYMBOL_CITATION, SOURCE_SUFFIXES (module constants), one file read
10	check_citations.check	collect every citation defect across the subject documents	root (parameter)	subjects, modules, citable, the filesystem
11	check_citations.citable	name every module a citation may name without this tree defining it	root (parameter)	sys.stdlib_module_names, the scripts/ tree, ast
12	markdown_links.heading_texts	list every heading's text at the given depths	text, depths (parameters — depths is supplied by nothing)	PARSER (module constant)
13	test_evidence_currency.DriftTests.test_each_way_a_fingerprint_fails_says_which_one	assert each way a fingerprint fails names itself	none	TemporaryDirectory, the filesystem, evidence_currency
14	test_validate_skills.ProjectedDescriptionTests.test_a_non_worktree_validates_from_the_filesystem	assert a root with no git metadata still validates	none	TemporaryDirectory, fixtures.write_distribution, the rm binary via subprocess
15	test_validate_skills.ProjectedDescriptionTests.test_a_non_worktree_still_reports_a_stale_pin	assert the fallback still reports a stale pin	none	TemporaryDirectory, fixtures.write_distribution, distribution_manifest.PINNED_INSTALL_DOCS, the rm binary via subprocess

Gate 1: Formatting & Syntax Hygiene
#	Location	Violation	Correction
1	scripts/distribution_manifest.py:237-266	Gate 1, unreachable branches: validate_install_pins gained `documents: list[Path] | None = None`, and the `else documents` arm of `install_documents(root) if documents is None else documents` is reached by nothing — no caller in `scripts/`, `scripts/tests/` or `tools/` supplies it. `install_documents` is already the seam a caller would use, so the parameter buys nothing the extraction did not
2	scripts/tests/test_evidence_currency.py:143-145	Gate 1, dead code: the case builds escape, a symlink to a path outside the root, and cases never names it — the symlink-escape branch the Fingerprint docstring names first ("an entry pointing at a symlink out of the tree") is set up and never exercised. outside.md is likewise written inside the root while the case path ../outside.md points above it, so the write is inert too	Add "escape.md": "resolves outside the repository" to cases so the fixture is used, and delete the outside.md write, which no case reads

Gate 3: Error Handling & Observability
#	Location	Violation	Correction
3	scripts/evidence_currency.py:283-284	Gate 3, error messages are actionable: f"{tests} resolves {verdict.value} the repository" covers both remaining verdicts with one sentence. Verdict.UNRESOLVABLE.value is "unresolvable", so the diagnostic reads "x.md resolves unresolvable the repository" — and the branch discards Resolved.error, the only value that says why resolution failed. The repair split one None into five reasons; the fifth is the one that names nothing	Split the branch: Verdict.OUTSIDE keeps "resolves outside the repository"; Verdict.UNRESOLVABLE reads f"{tests} could not be resolved: {resolved.error}", which needs the Resolved rather than its .verdict alone

Gate 5: Responsibility & Boundaries
#	Location	Violation	Correction
4	scripts/markdown_links.py:105	Gate 5, public interfaces are minimal: heading_texts(text, depths=(2, 3)) has one caller, seam_contract.elements, which takes the default, and no test supplies depths either. The review scope is the whole repository, so the member is decidably unused — the case AGENTS.md calls "a capability nobody calls"	Inline the depths as the module constant the one caller needs, or delete the parameter and name the function for what it reads
5	scripts/tests/test_validate_skills.py:1269, scripts/tests/test_validate_skills.py:1288	Gate 5, hardwired to concrete infrastructure: both cases delete .git by spawning rm -rf through subprocess, which binds the suite to a POSIX host in a repository whose host_paths, evidence_currency and skill_model all reason explicitly about Windows spellings, and whose policy is "Use the Python standard library by default". check_sources.py already imports shutil for exactly this kind of host question	Replace both with shutil.rmtree(root / ".git")

Gate 6: Business Logic Integrity
#	Location	Violation	Correction
6	docs/guards.md:26-31	Gate 6, logic matches the stated requirement: the heading became "Measured 2026-08-27, extended 2026-08-28" while the preamble beneath it still says "Each revert applied to the tree at 1abbb01". Two of the eight rows added cannot have been measured there — distribution_manifest.install_documents and the reasons in evidence_currency.fingerprint were both created at aecf03d, and git show 1abbb01 finds neither. A dated evidence section is quotable only with the tree it names, which is AGENTS.md's own rule for reading one; a later round reproducing these two reverts is sent to a tree where the units do not exist	Split the section: keep "Measured 2026-08-27" with its 1abbb01 preamble and the five rows measured there, and open "Measured 2026-08-28" with its own preamble naming aecf03d for the eight rows added since

Gate 7: Deduplication & Composition
#	Location	Violation	Correction
7	scripts/check_citations.py:213-221	Gate 7, the same question answered twice: citable walks and ast.parses every file under scripts/ to collect imports, and module_symbols parses the same files again to collect definitions — the run's 66 parses are 48 from one pass plus the cited modules from the other. They also disagree about failure: citable swallows OSError/SyntaxError with continue, while module_symbols turns the identical failure into a reported Symbols reason, so an unparseable module is silently absent from the citable set and loudly reported as a citation defect in the same run. The map-threading repair reached the tree walk and stopped at the parse	Parse each module once into Modules.read and derive both answers from the stored tree: give Symbols the module's imported top-level names alongside its definitions, and have citable read the cache rather than re-walking. One failure path then serves both

Structural Appendix
distribution_manifest.install_documents — the fallback is correct for the case it was written for. A release snapshot carries no ignored files, so rglob and git ls-files agree there; in this repository they agree too (99 Markdown files either way, and no ignored .md). Where they would diverge is a worktree with git absent from PATH, and nothing in the output says which branch answered. Recorded, not raised: there is no instance, and the previous round's record already carries two observations in the same posture.

evidence_currency.Fingerprint — the split is the shape skill_yaml.Document, path_boundary.Boundary and check_citations.Symbols hold, enforced in __post_init__ and reachable only through digest, which raises the reason. check and main both read reason first. The one gap is the sentence the last branch composes, which is finding 3.

check_citations.Modules — dropping frozen=True to carry the read cache is the right trade: the dataclass is constructed once per run in check and threaded, so there is no second holder to surprise. The forward reference "Symbols" is redundant under from __future__ import annotations but harmless.

Sweep for the None-with-several-reasons class — run over scripts/ rather than over the repair's diff, as AGENTS.md requires. section_text, module_symbols, _read and read_json_object each answer with one reason or a pair; skill_yaml.get_top_level_yaml_value is the only survivor and its own docstring already records it as not-yet-three-state with no defect reported against it. The sweep found nothing new, which is worth stating because the previous round's sweep, bounded by a record's Reach, found three.

Where this leaves the range: the eleven findings from v0.4.1..1609403 are all repaired and verifiable in the tree. Of the nine new findings, seven sit in code or tests added by the repair commit itself, and two are contract violations — one of which (the guards ledger completing itself before the findings list it must satisfy existed) is the third consecutive instance of the pattern that round's own record named. Structural cause 3 is the one worth settling first, since it is the only one that changes when a defect of this class appears again.

Next step: hand this to triage-findings to settle the nine, then persist this text at docs/reviews/v0.4.1..7e2da1e.md and the disposition at docs/dispositions/v0.4.1..7e2da1e.md in the turn that repairs. I have modified nothing.
````
