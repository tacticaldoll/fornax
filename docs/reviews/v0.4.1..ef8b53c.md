# Review Record — `v0.4.1..ef8b53c`

The text this round's triage was handed, as it arrived. Kept so the reconciliation in
`docs/dispositions/v0.4.1..ef8b53c.md` can be checked against its input rather than taken on trust.

Not byte-guaranteed and not reformatted: the record reached this triage inside a message, so what
follows is a transcription of that message. It is fenced, which preserves it and keeps its
absolute-path links and line citations as text rather than as claims this repository makes.

````text
Review Record
Source: v0.4.1..ef8b53c
Calibration: Gates 1–8
Triage: Red 0 / Yellow 1 / Green 74
Coverage: partial — gate-reviewed: []; partially-gate-reviewed: [scripts/check_citations.py (Gates 1–3; Gate 4 blocked)]; triage-only: [其余 74 个已枚举范围文件]; unread: []
Findings: 10
Verdict: FAIL at Gate 3 + CONTRACT-VIOLATED + CLAIM-REFUTED
Not executed: static review only — tests, build, and runtime not run
Gate Index
Gate	Status
1	pass
2	pass
3	fail
4	blocked
5–8	not inspected

Gate 3: Error Handling & Observability
Location	Violation	Correction
[check_citations.py (line 232)](/home/qaz/work/fornax/scripts/check_citations.py:232)	解析失败的模块可能位于 scripts/tests/，但诊断固定输出 scripts/<module>.py，会指向不存在或错误的文件。	从 known.by_stem[module] 生成实际路径，再附加 Symbols.reason。

Against-Contract
#	Clause	Falsifier attempted	Result	Evidence
1	检查器必须验证 citation 指向的 symbol 确实存在。	`shlex.definitely_missing`	VIOLATED —外部模块只用 find_spec() 验证模块存在，完全不检查后续 symbol。	[check_citations.py (line 224)](/home/qaz/work/fornax/scripts/check_citations.py:224)
2	URL authority 的端口应豁免，但 URL 路径中的源码行引用仍应被识别。	https://host.example/docs/file.py:12	VIOLATED —整个 URL word 都被跳过，路径中的 file.py:12 同样逃逸。	[check_citations.py (line 205)](/home/qaz/work/fornax/scripts/check_citations.py:205)
3	通用 path:line 规则不能只接受纯字母扩展名。	schema.proto3:12	VIOLATED —扩展名含数字时不会匹配。	[check_citations.py (line 56)](/home/qaz/work/fornax/scripts/check_citations.py:56)
4	维护检查必须在声明的环境中得到稳定结果。	在安装或未安装某个同名第三方包的环境分别检查未知模块。	VIOLATED —find_spec() 使结果依赖当前 Python 环境，而不是仓库声明。	[check_citations.py (line 178)](/home/qaz/work/fornax/scripts/check_citations.py:178)
5	永久治理文本不得保存仓库构成测量。	检查引用规则说明。	VIOLATED —most of that convention's sites 仍是当前树的数量测量。	[AGENTS.md (line 692)](/home/qaz/work/fornax/AGENTS.md:692)
6	不可判定的旧输入状态不得写成事实。	比较旧记录的确定陈述与后续"不知道"结论。	VIOLATED —旧记录仍断言副本从 Gate Index 开始。	[v0.4.1..64667c6.md (line 3)](/home/qaz/work/fornax/docs/dispositions/v0.4.1..64667c6.md:3)
7	每轮必须及时保存 Review 和 Disposition Record。	查找 v0.4.1..3901827。	VIOLATED —两个记录都不存在；最新文件仍止于 804961c。	[reviews](/home/qaz/work/fornax/docs/reviews), [dispositions](/home/qaz/work/fornax/docs/dispositions)
8	模块重复 stem、解析失败及明显内部拼写错误必须被报告。	对应代码路径静态复验。	holds	[check_citations.py (line 75)](/home/qaz/work/fornax/scripts/check_citations.py:75)

Claims Verified
Claim	Result
check_citations 验证 cited symbol 存在	REFUTED —外部模块的 symbol 不检查
新 checker 已获得"可防守的 boundary"	REFUTED —URL 路径和环境依赖仍突破边界
新规则实施后每轮都会落盘 Review/Disposition	REFUTED —缺少 3901827
missing / unreadable / syntax-error 已分开	verified
重复 module stem 已显式报告	verified
长字母扩展名、普通文本和 URL authority 已覆盖	verified
ordinal inventory 已移除	verified

Structural Causes
- "外部模块"被当成无需验证的终止状态，导致 checker 只证明模块可发现，没有证明 citation 的 symbol 存在。
- URL 豁免施加在整个 whitespace word，而真正需要豁免的只有 authority 中的 host:port。
- find_spec() 把本地维护环境变成未声明的判断输入。
- 新的 record-persistence 规则仍未成为每轮提交前的完成边界。
结论：仍未通过。重复 stem、错误状态、长扩展名和明显拼写错误的修复有效，但 Gate 3 仍失败，citation checker 仍会漏掉外部模块的不存在 symbol 与 URL 路径引用；上一轮记录也再次没有落盘。此前 carried findings 仍未获得 gated review。
````
