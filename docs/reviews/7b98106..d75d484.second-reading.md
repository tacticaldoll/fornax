# Review Record — `7b98106..d75d484 (second reading)`

Archived as received. A second reading of the same range, scoped to `AGENTS.md`'s archived-record
clauses as the named contract, after the first reading's two findings were reproduced and settled.

Fenced, which preserves the record and keeps its citations as text rather than as claims this
repository makes.

```markdown
## Review Record

**Source**: local git range `7b98106..d75d484` (2 commits, 5 files), reviewed against `AGENTS.md`'s archived-record clauses as the named contract
**Calibration**: Gates 1-7 (production validator plus a shipped skill; this range crosses no filesystem or subprocess boundary, so Gate 8 has no subject)
**Triage**: skipped — 5 files
**Coverage**: partial — gate-reviewed: `scripts/record_shape.py` (Gates 1-7), `docs/dispositions/v0.4.1..af01263.md`, `skills/triage-findings/SKILL.md`; partially-gate-reviewed: `scripts/tests/test_record_shape.py` (Gates 1-6 — executed and mutated, not read whole), `docs/guards.md` (Gates 1-2); triage-only: none; unread: none
**Findings**: 8
**Verdict**: FAIL at Gate 6 + CONTRACT-VIOLATED + CLAIM-REFUTED
**Not executed**: no build or CLI runtime. The suite was run under five constructed reverts and `record_shape.py` under two contract mutations, and those outputs are reported.

Gate 1 note. The stranded `A cell is bounded by the pipe` comment appears in this range's diff as context, not as an addition — `3cbab14` predates `7b98106`. This range did place `Seat`'s fields immediately above it, which makes it read as `Seat`'s comment, but the defect is already accepted as `COMMENT-OUTLIVED-ITS-CODE` under cause 12 of the prior settlement. So Gate 1 passes for this range's hunks rather than being held open on a settled finding, and the higher gates stay reachable.

### Gate Index

| Gate | Focus | Status |
|---:|---|---|
| 1 | Formatting & Syntax Hygiene | pass |
| 2 | Naming & Readability | pass |
| 3 | Error Handling & Observability | pass |
| 4 | Control Flow & Structural Clarity | pass |
| 5 | Responsibility & Boundaries | pass |
| 6 | Business Logic Integrity | fail |
| 7 | Deduplication & Composition | blocked |
| 8 | Security & Parameter Integrity | not inspected |

### Against-Contract

| # | Clause | Falsifier attempted | Result | Evidence |
|---|---|---|---|---|
| 1 | `AGENTS.md`: "editing its numbers to satisfy a later rule makes it a worse record of the reading it reports" | Check whether the edit touched a number or a reading | holds — a label changed, the `pass` answer did not, and the edit is disclosed in the record | `docs/dispositions/v0.4.1..af01263.md`, "The second row's label read" |
| 2 | `AGENTS.md`'s own precedent: a judgment call the lists did not decide is "recorded here so the next reader is not deciding again" | Grep `AGENTS.md` for the label-versus-reading distinction | VIOLATED — nothing; the distinction was recorded inside the record it authorised editing, so the next round decides it again | `AGENTS.md`, "recorded here so the next reader is not deciding again" |
| 3 | `record_shape` docstring: "a predicate covering that costs nothing, where a list of files it does not apply to is the shape `AGENTS.md` refuses" | Reword one declared `Self-check` label in the contract | VIOLATED — 25 records fail at once; the list was avoided by taking an unstated freeze on the contract's wording instead | `record_shape.RequiredKeys` |
| 4 | `AGENTS.md`: "Before adding a check, say what it would have caught and what it costs per change… the exemptions it needs" | Look for the archived-corpus coupling in the check's stated cost | VIOLATED — the docstring states the missing-section exception costs nothing and never states this cost | `scripts/record_shape.py`, "a predicate covering that costs nothing" |
| 5 | `AGENTS.md`: "Revert the unit the claim names, not something it calls" | Reproduce all five revert measurements the commit reports | VIOLATED — four name a unit and reproduce exactly; the fifth names "the branch that lets the subject pick at all", which is a code shape, not a unit | `record_shape.rules_for` |
| 6 | Commit `d75d484`: "RecordRule is deliberately not built… the two rules that would need it do not exist" | Count record-level rules now wanted | VIOLATED — three: absent-section cardinality, duplicate rejection, and the malformed-table rule `record_defects` already holds | `record_shape.record_defects` |
| 7 | `AGENTS.md`: an archived Review Record must not be rewritten, being "a copy of what a producer wrote" | Check whether this range edited any `docs/reviews/` file | holds — the range touches no archived Review Record | `git diff --name-only 7b98106..d75d484` |

### Claims Verified

| # | Claim | Evidence | Result |
|---|---|---|---|
| 1 | "Corrected and marked where it is written" | `docs/dispositions/v0.4.1..af01263.md`, "The second row's label read" | verified |
| 2 | "this record's answer to it is untouched" | same — only the label differs; `pass` unchanged | verified |
| 3 | reverting the floor gives 2 red | `record_shape.RequiredKeys` | verified — 2 |
| 4 | reverting `ClosedKeys` gives 3 red | `record_shape.ClosedKeys` | verified — 3 |
| 5 | reverting `ValueReadWhole` gives 9 red | `record_shape.ValueReadWhole` | verified — 9 |
| 6 | reverting `UniqueFirstColumn` gives 2 red | `record_shape.UniqueFirstColumn` | verified — 2 |
| 7 | "removing the branch that lets the subject pick at all 13" | `record_shape.rules_for` | partial — the reconstruction gives 15; the mutation is under-specified, so the number cannot be checked from its description |
| 8 | "Workspace gate green at 460 tests" | `check_workspace.STEPS` | verified |
| 9 | "Self-check carries eight distinct undeclared labels" | `record_shape.Subject` | REFUTED — seven; the same commit's own label edit is what removed the eighth |
| 10 | "`Declared.checks` becomes `Declared.keys`; no record cited that member, which was checked before renaming it" | `check_citations.SUBJECTS` covers `docs/dispositions/`, and the gate is green | verified |

### Structural Causes

| # | Finding | Cause (the thing to change) | Cause location | Gate that would carry it |
|---|---|---|---|---|
| 1 | Gate 6-1, AC-3, AC-4 | Give the check a notion of which contract revision a record was settled under — key each record to that, or scope the seat rules to records the current round produces — rather than comparing every archived record against the contract as it stands today | `record_shape.RequiredKeys`, `record_shape.ClosedKeys` | Gate 6 (fail) |
| 2 | CV-9 | Take the corpus measurement after the round's own repairs land, not before; the number here was true when measured and false by the end of the same commit | `record_shape.Subject` | Gate 6 (fail) |
| 3 | AC-5, CV-7 | State a revert measurement as the named unit whose removal produces it, so a later round reproduces the same mutation | `record_shape.rules_for` | Gate 2 (pass) |

### Responsibility & Dependency Ledger

Rows are not findings. Layering inferred from `scripts/` structure; the repository declares none.

| # | Unit | Its job (one clause) | Handed in | Reached directly |
|---|---|---|---|---|
| 1 | `record_shape.rules_for` | Pick a seat's rules from whose conduct its rows judge | `subject` (parameter) | none |
| 2 | `record_shape.RequiredKeys` | Report a declared label the table omits | `seat`, `found`, `shape` (parameters) | none |
| 3 | `record_shape.ClosedKeys` | Report a row label the contract does not declare | same | `CONTRACT` (module global) |
| 4 | `record_shape.ValueReadWhole` | Report a verdict outside the declared domain | same, plus `column` (field) | none |
| 5 | `record_shape.UniqueFirstColumn` | Report two rows keying alike | same | none |
| 6 | `record_shape.record_defects` | Report one record's departures from the declared shape | `path`, `shape` (parameters) | `SEATS` (global), `heading_section` (module function) |
| 7 | `record_shape.declared` | Read the shape the contract's template declares | `root` (parameter) | `CONTRACT`, `MARKER`, `SEATS` (globals) |
| 8 | `record_shape.check` | Report every record's departures, and a contract that will not read | `root` (parameter) | `RECORDS` (global) |

Gate 5 did not open on `scripts/tests/test_record_shape.py` or `docs/guards.md` — Phase 3 placed both in `partially-gate-reviewed` at Gates 1-6 and 1-2, so the ladder never reached it for either.

### Gate 6: Business Logic Integrity

| # | Location | Violation | Correction |
|---|---|---|---|
| 1 | `scripts/record_shape.py`, `RequiredKeys` and `ClosedKeys` | Gate 6: the check binds every archived record to the contract's current wording, so a contract edit convicts history rather than the change. Measured: rewording one declared `Self-check` label reports 25 records; renaming one `Record integrity` check reports 26. The exits are editing 25 historical records, an exemption list the module's own docstring says `AGENTS.md` refuses, or an unstated freeze on the contract's wording — and this range already took the first exit once, for one record | Key a record to the contract revision it was settled under and judge it against that, or scope the seat rules to the record the current round produces and leave the archive to the round that wrote it. Then state the coupling as the check's standing cost, which `AGENTS.md` requires before adding one |

### Structural Appendix

`record_shape.rules_for` — the subject-to-rules derivation is sound and the four rules are each a clean predicate over one table. The defect is not in any rule but in the corpus they are pointed at: they are written as claims about a record and applied to every record, including 25 that were settled under earlier wordings of the same contract.

The archived-record edit itself is the most defensible thing in this range. It is minimal, label-only, leaves the answer untouched, and is disclosed in the record at the point of the change. The finding is not that the edit was wrong — it is that the check made the edit necessary, that the reasoning authorising it lives in the edited record instead of in `AGENTS.md`, and that the same edit silently invalidated the corpus number the commit used to justify the design.
```
