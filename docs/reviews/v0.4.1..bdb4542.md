# Review Record — `v0.4.1..bdb4542`

The text this round's triage was handed, as it arrived. Kept so the reconciliation in
`docs/dispositions/v0.4.1..bdb4542.md` can be checked against its input rather than taken on trust.

Not byte-guaranteed and not reformatted: the record reached this triage inside a message, so what
follows is a transcription of that message. It is fenced, which preserves it and keeps its
absolute-path links and line citations as text rather than as claims this repository makes.

````text
Review Record
- Source: v0.4.1..bdb4542
- Calibration: Gates 1–8
- Triage: Red 0 / Yellow 1 / Green 78
- Findings: 4（3 contract violations + 1 independently counted refuted claim）
- Verdict: PASS + CONTRACT-VIOLATED + CLAIM-REFUTED
- Not executed: tests、build、runtime；本輪僅靜態審查
- Worktree: clean
Gate Index
Gate	Status
1 Correctness	pass
2 Data integrity	pass
3 Error handling	pass
4 Control flow	pass
5–8	not inspected

check_citations.py 的實際模組路徑診斷、錯誤狀態拆分、環境獨立 module derivation 都已修好。雖然 citations() 偏長且有巢狀分支，目前控制流仍可沿 early-continue 清楚追蹤，因此 Gate 4 通過。
Against-Contract
1. Network-path URL authority 仍被誤判
   [check_citations.py (line 69)](/home/qaz/work/fornax/scripts/check_citations.py:69) 只辨識 ://authority。像 //example.com:443/path 也有 URL authority，但會被 LINE_CITATION 當成 example.com:443 行號引用。
   修正方向：同時辨識 scheme URL 和 //authority，但仍只豁免 authority span；加入 scheme-relative URL 測例。
2. 治理文字再次保存歷史數量
   [AGENTS.md (line 512)](/home/qaz/work/fornax/AGENTS.md:512) 的 "two rounds in a row" 計算了過去修復輪次，直接違反同檔「不得記錄 past repair 經過多少 forms/commits」的規則。
   修正方向：改成不帶數量的 "successive rounds lost theirs"。
3. Registry 保存目前樹的模糊數量測量
   [development-knowns.yaml (line 71)](/home/qaz/work/fornax/development-knowns.yaml:71) 的 "across most of scripts/" 是對當前 repository contents 的數量描述；它不在規則明定豁免的 dated Disposition Record 中。
   修正方向：只記錄如何重現，例如 "run the command to enumerate the current findings"，不要描述 scripts/ 中佔多少。
Claims Verified
Claim	Result
錯誤診斷使用實際 module path	verified
module legitimacy 不再依賴本機安裝環境	verified
外部 symbol 不檢查的邊界已明確聲明並登記	verified
缺失的兩輪 Review/Disposition Records 已保存	verified
舊 disposition 不再猜測收到的文字從哪裡開始	verified
citation checker 已「defend its edges」	REFUTED — //authority 邊界仍失守

Coverage 是 partial：scripts/check_citations.py 完成 Gates 1–4；其餘 78 個 range files 僅完成 triage。先前 carried findings 尚未獲得 gated review。
結論：目前仍不能算 pass。修掉上述 3 個 contract violations 後，需要再跑一輪 review。
````
