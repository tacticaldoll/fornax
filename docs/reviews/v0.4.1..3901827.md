# Review Record — `v0.4.1..3901827`

The text this round's triage was handed, as it arrived. Kept so the reconciliation in
`docs/dispositions/v0.4.1..3901827.md` can be checked against its input rather than taken on trust.

Not byte-guaranteed and not reformatted: the record reached this triage inside a message, so what
follows is a transcription of that message. It is fenced, which preserves it and keeps its
absolute-path links and line citations as text rather than as claims this repository makes.

````text
Gate Index
Gate	Status
1	pass
2	pass
3	fail
4	blocked
5–8	not inspected

Gate 3: Error Handling & Observability
Location	Violation	Correction
[check_citations.py (line 100)](/home/qaz/work/fornax/scripts/check_citations.py:100)	读取或解析已存在模块失败时返回 None，随后被错误报告成"模块不存在"。	将 missing、unreadable、syntax-error 分成不同结果并输出对应诊断。
[check_citations.py (line 75)](/home/qaz/work/fornax/scripts/check_citations.py:75)	modules() 用文件 stem 建字典；重复 stem 会静默覆盖，引用可能被拿去检查错误文件。	检测重复 stem 并明确失败，或让 citation 携带可消歧的模块路径。

Against-Contract
#	Clause	Falsifier attempted	Result	Evidence
1	任意文件后缀的 path:line 都应被拒绝。	docs/spec.markdown:12	VIOLATED —扩展名被限制为 1–6 个字母。	[check_citations.py (line 53)](/home/qaz/work/fornax/scripts/check_citations.py:53)
2	普通网络地址不能被误判为源码行引用。	https://example.com:443/path	VIOLATED —example.com:443 匹配当前正则。	[check_citations.py (line 53)](/home/qaz/work/fornax/scripts/check_citations.py:53)
3	拼错的内部模块引用不能静默通过。	使用与真实模块相似度低于 0.8 的错误模块名。	VIOLATED —模糊匹配失败后仍按外部模块直接放过。	[check_citations.py (line 153)](/home/qaz/work/fornax/scripts/check_citations.py:153)
4	永久治理文本不得保存仓库构成测量。	检查引用规则的新依据。	VIOLATED —most of that convention's sites 仍是对当前树的数量测量。	[AGENTS.md (line 692)](/home/qaz/work/fornax/AGENTS.md:692)
5	源码说明不得维护会失效的列表数量。	将测试说明与当前 suffix 列表比较。	VIOLATED —测试仍说 "list of six"，当前列表已经不是该数量。	[test_check_citations.py (line 54)](/home/qaz/work/fornax/scripts/tests/test_check_citations.py:54)
6	不可判定的旧输入传输状态不得写成事实。	对照旧记录的确定陈述和新记录的"不知道"。	VIOLATED —旧记录仍断言副本从 Gate Index 开始。	[v0.4.1..64667c6.md (line 3)](/home/qaz/work/fornax/docs/dispositions/v0.4.1..64667c6.md:3)
7	每轮 Review/Disposition 必须持久化。	查找 87c7bb4 和 804961c 对应记录。	holds	[reviews](/home/qaz/work/fornax/docs/reviews), [dispositions](/home/qaz/work/fornax/docs/dispositions)
8	CommonMark helper 不得保存 ordinal inventory。	检查 prose_lines 说明。	holds —已改回 "Another operation"。	[markdown_links.py (line 152)](/home/qaz/work/fornax/scripts/markdown_links.py:152)

Claims Verified
Claim	Result
Line citation 检查支持"任意后缀"	REFUTED —存在 6 字母上限
check_citations 验证引用的内部 symbol 存在	REFUTED —低相似度错误模块仍会通过
modules() 返回每个模块	REFUTED —重复 stem 会覆盖
普通无反引号引用与 .js 引用已覆盖	verified
缺失的 Review/Disposition records 已补齐	verified
先前的树测量与 ordinal 已全部移除	partial —仍有 most ... sites 和过期的 "list of six"

Structural Causes
- Citation 语法仍由正则和启发式模糊匹配共同猜测；它既没有明确区分内部/外部模块，也没有一个路径语法所有者。
- None 同时表示不存在、读取失败和解析失败，导致 Gate 3 的错误状态失真。
- modules() 选择 stem 作为唯一身份，但没有验证该身份在递归目录中唯一。
- 数量清理仍以替换已报告措辞为主，没有对新增 prose 的 referent 重新检查。
结论：仍未通过。记录落盘、普通 line citation、.js 和明显拼写错误已经修复；当前失败点集中在 citation checker 的错误状态、正则边界、模糊匹配和模块身份冲突。此前 carried findings 仍未经过 gated review。
````
