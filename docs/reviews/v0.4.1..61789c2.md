# Review Record — `v0.4.1..61789c2`

The text this round's triage was handed, as it arrived. Kept so the reconciliation in
`docs/dispositions/v0.4.1..61789c2.md` can be checked against its input rather than taken on trust.

Not byte-guaranteed and not reformatted: the record reached this triage inside a message, so what
follows is a transcription of that message. It is fenced, which preserves it and keeps its
absolute-path links and line citations as text rather than as claims this repository makes.

````text
Review Record
Source: local git diff v0.4.1..HEAD, HEAD 61789c2. This round's own inspection is over de72982..HEAD — 2733497 (4 files), b165753 (1 file), 61789c2 (4 record files). The earlier range carries the coverage stated in the three archived Review Records and is not re-claimed here.
Calibration: Gates 1–8 (unchanged)
Triage: Red 0 / Yellow 3 / Green 6
Coverage: partial — gate-reviewed (Gates 1–8): check_citations.Symbols, check_citations.Symbols.imports, check_citations.citable, test_check_citations.ModuleIdentity.test_a_symbols_holding_a_reason_answers_neither_question; partially-gate-reviewed: docs/guards.md (Gates 1–6), docs/dispositions/v0.4.1..de72982.md (Gates 1–6), docs/dispositions/v0.4.1..1609403.second-reading.md (Gates 1–6), AGENTS.md's guards clause (Gates 1–6) — Gates 3–5 opened over these four and found no subject to judge, which is a vacuous pass and is recorded as one rather than as a substantive one; triage-only: docs/reviews/v0.4.1..de72982.md, docs/reviews/v0.4.1..1609403.second-reading.md, read for fidelity to what they transcribe; unread: none within de72982..HEAD. The rest of v0.4.1..HEAD is unread this round.
Findings: 5 (3 gate rows + 1 VIOLATED contract row + 1 REFUTED claim; the contract row and the claim key to each other, left undeduplicated as Phase 4b requires)
Verdict: FAIL at Gate 6 + CONTRACT-VIOLATED + CLAIM-REFUTED
Not executed: static review only — the workspace gate, the test suite, Ruff and the build were not run. Git object queries (merge-base --is-ancestor, cat-file, branch --contains, reflog) were run read-only to resolve finding 1.

Gate Index
Gate	Focus	Status
1	Formatting & Syntax Hygiene	pass
2	Naming & Readability	pass
3	Error Handling & Observability	pass
4	Control Flow & Structural Clarity	pass
5	Responsibility & Boundaries	pass
6	Business Logic Integrity	fail
7	Deduplication & Composition	pass
8	Security & Parameter Integrity	pass
Per file: docs/guards.md and docs/dispositions/v0.4.1..de72982.md fail Gate 6 (7–8 blocked); scripts/check_citations.py, scripts/tests/test_check_citations.py and AGENTS.md pass every gate opened.

Against-Contract
#	Clause	Falsifier attempted	Result	Evidence
1	skills/triage-findings/SKILL.md: "Reach — every location the repair touches, enumerated as a file plus the unit inside it: a symbol …, or a quoted phrase where no symbol owns the text"	Find a Reach cell naming a file with no unit inside it	VIOLATED — seven of the nine Reach cells across the two records settled this turn name a bare file: docs/guards.md (×5), AGENTS.md (×2), plus test_check_citations.py with no unit. The units are nameable — a ledger row keyed by finding id, a clause with a heading — so the contract is satisfiable here and was not satisfied. Keys to claim 1	docs/dispositions/v0.4.1..de72982.md, docs/dispositions/v0.4.1..1609403.second-reading.md
2	AGENTS.md: "a row for every finding a round accepted, naming the unit whose revert turns a named test red — or saying that nothing can turn red, and why. Both are rows"	Find an accepted finding with no row of either kind	holds — every finding accepted in this range has a row; which table the row sits in is finding 2, not a gap in coverage	docs/guards.md
3	AGENTS.md: the guards ledger is "written when the record is written, not when the repair lands"	Check which commit carried the ledger edits	holds — 2733497 carried both the repair and the ledger, and 61789c2 carried the records; the clause takes effect from the next round, as the previous round noted	git log --name-status de72982..HEAD
4	AGENTS.md: "A round is a review and its settlement" — two readings of one range are two rounds	Check whether the second reading overwrote the first reading's record	holds — docs/dispositions/v0.4.1..1609403.second-reading.md carries the range plus what it is, and says why	docs/dispositions/v0.4.1..1609403.second-reading.md
5	AGENTS.md: "Persist the Review Record too … so a reconciliation can be checked"	Look for a settled round with no archived input	holds — both readings settled this turn have an archived Review Record named from the Source line	docs/reviews/
6	check_citations.Symbols: "What a module defines, or why this could not say — never both, and never neither"	Reach a field on a Symbols carrying a reason	holds — _imports is private and imports raises the reason exactly as names does, with a case asserting both	scripts/check_citations.py, scripts/tests/test_check_citations.py
7	docs/guards.md: "Reconsider when a repair is found to have lost its guard"	Ask whether the condition fired and was addressed	see gate finding 3 — a row's guard was found not to guard, and neither record settled this turn revisits the decline or says the trigger did not fire	docs/guards.md

Claims Verified
#	Claim	Evidence	Result
1	docs/dispositions/v0.4.1..1609403.second-reading.md Self-check: "Every accepted cause carries at least one repair with an enumerated Reach — pass — both name the file they touch; neither has a symbol to name, since both units are prose"	The contract's alternative to a symbol is "a quoted phrase where no symbol owns the text", not the file alone. Both Reach cells name AGENTS.md or docs/guards.md with no unit, so the check passes itself against a rule it restates incorrectly	REFUTED
2	"Re-measured: both implementations answer identically on that fixture, because the old walk swallowed an unparseable module exactly as the one-pass form does"	Matches this reviewer's own probe from the previous round	verified
3	"My revert used Path("scripts") relative to the working directory rather than the fixture's root"	Consistent with the reported symptom (cases reddening for an unrelated reason); the earlier probe itself is not re-runnable	partial
4	docs/dispositions/v0.4.1..de72982.md: "Rewrapped" (PARAGRAPH-NOT-REWRAPPED)	AGENTS.md:515 is gone and no line in the clause exceeds 100. The paragraph is not re-flowed: line 522 is 69 characters mid-paragraph with 97 on the line after it, so the overlong line was split rather than the paragraph rewrapped	partial
5	"_imports is private now, behind the same raising accessor as the payload, with a case that reddens when the guard goes"	The property and the case both exist; the redness on revert was not re-run	partial
6	"MODULE-MAP-REBUILT quoted 165 parses to 48 … its record says 66, the 48 belongs to the later finding, and no record holds 0.22s"	Confirmed against both disposition records	verified
7	"VERDICT-CONTROL named its revert as 'the same fold' one row down, so it now names the revert itself and stays readable alone"	The row names the revert in full	verified
8	2733497: "So the row moves to the no-guard table with its own history written out"	The DOUBLE-PARSE-DISAGREES row is still in the dated "Measured 2026-08-28" table; it gained — in two columns and did not move	REFUTED — reported as gate finding 2 rather than counted twice here

Structural Causes
#	Finding	Cause (the thing to change)	Cause location	Gate that would carry it
1	1	Read the settling commit's hash after the commit is final, not before — an --amend after the record is drafted leaves the record pointing at an object no clone but this one can resolve, and nothing in the gate reads a record's commit references	docs/dispositions/v0.4.1..de72982.md, the Scope line and the four "Settled by" cells	Gate 6 (fail)
2	2	Give the ledger one home per row kind: rows with a revert and a number belong under a dated tree, rows with neither belong in "Repairs with no guard, and why". The dated sections grew the second kind because a row was written where its finding's siblings were, not where its shape belongs	docs/guards.md, the "Measured 2026-08-28, at aecf03d" table	Gate 6 (fail)
3	3	Re-weigh the decline against what the file now records, or say in the trigger that "never had the guard it claimed" is not "lost its guard". The decline was weighed against one occurrence and the file has since added three more ways a guard result went wrong	docs/guards.md, "What was declined"	Gate 6 (fail)
4	contract row 1, claim 1	Name a prose unit the way the contract's own alternative says — a quoted phrase — rather than treating "the unit is prose" as licence to stop at the file. The self-check restates the rule as file-alone and then passes itself against the restatement	skills/triage-findings/SKILL.md "Reach" is correct; the two records' Reach cells and the second reading's Self-check row are not	Gate 6 (fail)
5	—	The names and imports properties repeat the same three-line guard. Two properties, three lines each — recorded and not raised, since extracting it would cost more than the repetition	scripts/check_citations.py Symbols	Gate 7 (pass)

Responsibility & Dependency Ledger
Gate 5 opened over the units 2733497 changed in code. It did not open over docs/guards.md, the two disposition records or the AGENTS.md clause: their partial coverage reaches Gate 6, and Gate 5 has no subject in a prose document — no unit there has a job to state or a dependency to trace. It did not open over the rest of v0.4.1..HEAD, which this round did not inspect.

Rows in this section are not findings.

#	Unit	Its job (one clause)	Handed in	Reached directly
1	check_citations.Symbols	hold what a module defines and imports, or why this could not say	_names, reason, _imports (constructor)	none
2	check_citations.Symbols.imports	answer what a module imports, or raise the reason it cannot	self	none
3	check_citations.citable	name every module a citation may name without this tree defining it	known (parameter)	sys.stdlib_module_names
4	test_check_citations.ModuleIdentity.test_a_symbols_holding_a_reason_answers_neither_question	assert a reason-carrying Symbols answers neither question	none	check_citations.Symbols

Gate 6: Business Logic Integrity
#	Location	Violation	Correction
1	docs/dispositions/v0.4.1..de72982.md:10, and the "Settled by" cell of every accepted finding	Gate 6, logic matches the stated requirement: the record settles all four findings against 9354d75, and states its scope as 9354d75..b165753. That object is the pre-amend version of 2733497 — same subject, 59 seconds earlier — and is unreachable from HEAD: git merge-base --is-ancestor 9354d75 HEAD is false, git branch --contains names nothing, and it survives only in this clone's reflog until the next git gc. On a fresh clone or the remote, git show 9354d75 and git log 9354d75..b165753 both fail. 2733497, which actually carries the repairs, appears nowhere in the record or its archived Review Record. A Disposition Record exists so a later round can check a settlement; this one's settlement cannot be reached by any round but the one that wrote it	Replace all five references with 2733497, and the scope line with 2733497..b165753. Then verify the same way a later round would: git merge-base --is-ancestor <sha> HEAD for every commit a record names
2	docs/guards.md:74-77	Gate 6, existing patterns are followed: the section headed "Measured 2026-08-28, at aecf03d", whose preamble says "The rows below were added after the tree above" and explains why sections are split by tree, carries four rows that are not measurements — MODULE-MAP-REBUILT, FORMATTING-DRIFT, the four-prose row, and DOUBLE-PARSE-DISAGREES all give — for the revert and — for the red count. None was measured at aecf03d; three cannot be measured at all. A table built for exactly this class, "Repairs with no guard, and why", sits below holding four other findings, so a reader asking which accepted findings have no guard must read two tables that answer one question in two shapes. This is the previous round's LEDGER-DATED-WRONG facing the other way — a dated measurement section holding rows that were never measured — and 2733497's own message says the DOUBLE-PARSE-DISAGREES row "moves to the no-guard table", which it did not	Move the four — rows into "Repairs with no guard, and why", carrying their reasons intact, and keep the dated sections to rows that name a revert and a number
3	docs/guards.md:91-100	Gate 6, logic matches the stated requirement: "What was declined" weighs the gate step against one occurrence — "which has happened here: a seam test stayed green under a full revert" — and the file has since recorded three more ways a guard result went wrong here: an edit that did not apply, a revert that was not the pre-repair code, and a row whose guard never guarded. The declined mechanism is described as catching precisely that, the section's own "Reconsider when a repair is found to have lost its guard" names the condition, and neither record settled this turn revisits the decline or says the trigger did not fire. A decision left standing on evidence its own file has superseded reads as weighed when it has not been	Restate the decline's evidence against what the file now records, and either re-weigh it or say explicitly that "never had the guard it claimed" falls outside "lost its guard", so a later reader can see the trigger was considered rather than missed

Structural Appendix
check_citations.Symbols — the repair is the right shape: _imports private, reached through a property that raises the reason on the same condition names does, with a case driving both accessors. One residue, recorded and not raised: _imports keeps a frozenset() default so the two error-path constructions can omit it, which leaves a readable Symbols built without imports answering "imports nothing". Nothing in check_citations constructs one — module_symbols always passes the third argument on the success path — so the state exists only for a test to reach.

check_citations.citable — unchanged this round; still reads known.by_stem, so a colliding stem's shadowed file contributes no imports where the pre-repair walk read every file. No instance in this tree, and a collision is already a reported diagnostic. Carried forward from the previous appendix unchanged, as is distribution_manifest.install_documents' non-equivalent fallback.

The second reading — reading a settled range independently is what falsified the guards clause's reading order, which four rounds over the same file had not. Its record states what such a reading can and cannot establish, and that boundary looks right: it falsified a claim, and it did not close anything against a tree it had not read.

Where this leaves the range: the code is clean at every gate this round opened, and all four of the previous round's findings are repaired. All three gate findings are in the record and ledger machinery rather than the product, and finding 1 is the one that matters most — a settlement that cannot be reached from any clone but this one defeats the reason the records exist, and it will become unrecoverable rather than merely wrong once the reflog expires.

Next step: hand this to triage-findings, then persist this text at docs/reviews/v0.4.1..61789c2.md and the disposition at docs/dispositions/v0.4.1..61789c2.md in the turn that repairs — and read the settling commit's hash after that commit is final. I have modified nothing in the repository.
````
