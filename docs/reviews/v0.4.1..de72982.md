# Review Record — `v0.4.1..de72982`

The text this round's triage was handed, as it arrived. Kept so the reconciliation in
`docs/dispositions/v0.4.1..de72982.md` can be checked against its input rather than taken on trust.

Not byte-guaranteed and not reformatted: the record reached this triage inside a message, so what
follows is a transcription of that message. It is fenced, which preserves it and keeps its
absolute-path links and line citations as text rather than as claims this repository makes.

````text
Review Record
Source: local git diff v0.4.1..HEAD, HEAD de72982. This round's own inspection is over 7e2da1e..HEAD — the repair commit 95d049d (9 files) and the record commit de72982 (2 files). The earlier range carries the coverage stated in docs/reviews/v0.4.1..1609403.md and docs/reviews/v0.4.1..7e2da1e.md and is not re-claimed here.
Calibration: Gates 1–8 (unchanged — check_citations.citable and evidence_currency.fingerprint both changed how the checks read files supplied by a registry or a tree)
Triage: Red 0 / Yellow 3 / Green 8
Coverage: partial — gate-reviewed (Gates 1–8): distribution_manifest.validate_install_pins, evidence_currency.fingerprint, markdown_links.heading_texts, markdown_links.RECORD_DEPTHS, check_citations.Symbols, check_citations.module_symbols, check_citations.citable, check_citations.check, docs/guards.md, the edited AGENTS.md clause, test_check_citations.ModuleIdentity.test_an_unparseable_module_is_absent_and_reported_by_one_answer, test_evidence_currency.DriftTests.test_each_way_a_fingerprint_fails_says_which_one, test_evidence_currency.DriftTests.test_an_unresolvable_path_says_what_stopped_it, test_validate_skills.ProjectedDescriptionTests.test_a_non_worktree_validates_from_the_filesystem, test_validate_skills.ProjectedDescriptionTests.test_a_non_worktree_still_reports_a_stale_pin; partially-gate-reviewed: docs/dispositions/v0.4.1..7e2da1e.md (Gates 1–2), docs/reviews/v0.4.1..7e2da1e.md (Gates 1–2); triage-only: none; unread: none within 7e2da1e..HEAD. The rest of v0.4.1..HEAD is unread this round.
Findings: 6 (4 gate rows + 1 VIOLATED contract row + 1 REFUTED claim. Contract row 1 and claim 1 key to gate finding 4 — one defect across three tracks, left undeduplicated as Phase 4b requires)
Verdict: FAIL at Gate 1 + CONTRACT-VIOLATED + CLAIM-REFUTED
Not executed: static review only — the workspace gate, the test suite, Ruff and the build were not run. One scratch probe was run to falsify a guard claim: it reconstructed the pre-repair citable in a temporary directory and compared both implementations' output on the guard case's own fixture (finding 4). It modified no file in the repository.

Gate Index
Gate	Focus	Status
1	Formatting & Syntax Hygiene	fail
2	Naming & Readability	pass
3	Error Handling & Observability	fail
4	Control Flow & Structural Clarity	pass
5	Responsibility & Boundaries	pass
6	Business Logic Integrity	fail
7	Deduplication & Composition	pass
8	Security & Parameter Integrity	pass
Per file: AGENTS.md fails Gate 1 (2–8 blocked); check_citations.py fails Gate 3 (4–8 blocked); docs/guards.md fails Gate 6 (7–8 blocked); distribution_manifest.py, evidence_currency.py, markdown_links.py, test_evidence_currency.py, test_validate_skills.py and test_check_citations.py pass every gate opened.

Against-Contract
#	Clause	Falsifier attempted	Result	Evidence
1	AGENTS.md: "Check the fixture actually fails when the rule is removed; a suite that passes either way proves nothing. Revert the unit the claim names, not something it calls"	Perform the revert the guard row names and run the case	VIOLATED — reverting check_citations.citable to a second walk that parses the same files leaves test_an_unparseable_module_is_absent_and_reported_by_one_answer green. Both implementations emit the same two diagnostics on its fixture. Keys to gate finding 4 and claim 1	scripts/tests/test_check_citations.py, scripts/check_citations.py citable
2	docs/guards.md: "Every finding a round accepted in this range has a row, closed ones included, and a finding whose repair nothing can guard has a row saying so"	Name a finding accepted in this range with no row	holds — the seven findings settled at 95d049d and the six the previous round omitted all have rows, and the four whose units are prose plus the one no test can hold say so instead of being absent	docs/guards.md vs docs/dispositions/v0.4.1..7e2da1e.md
3	AGENTS.md: "a claim nothing reads, a helper nothing calls: each is worse than its absence"	Find a capability introduced in this range that no caller reaches	holds — validate_install_pins's documents and heading_texts' depths are gone; RECORD_DEPTHS has its one reader; Iterable is still used at markdown_links._iter_token_links; subprocess was removed from test_validate_skills.py and appears nowhere else in it	scripts/distribution_manifest.py, scripts/markdown_links.py, scripts/tests/test_validate_skills.py
4	AGENTS.md: "Use the Python standard library by default" and avoid hidden host assumptions	Find a shell-out where the stdlib answers	holds — both rm -rf calls are shutil.rmtree	scripts/tests/test_validate_skills.py
5	AGENTS.md: the guards ledger is written "when the record is written, not when the repair lands"	Check which turn completed the ledger	holds for the rule as now stated; the rows were still written in 95d049d, the repair commit, and the record landed in de72982. The clause takes effect from the next round	AGENTS.md, git log --name-status 7e2da1e..HEAD
6	AGENTS.md: "A dated evidence log … a count, a verdict, or a standing claim is quotable only together with the dated section it sits under"	Find a row whose section names a tree its unit does not exist at	holds — the section is split, each names its tree, and the preamble says why	docs/guards.md
7	AGENTS.md: "After repairing a defect, sweep the repository for the same class"	Apply the sweep the Symbols value-or-reason discipline establishes to the field added beside it	see gate finding 2 — the new imports field on Symbols does not follow the discipline the type states	scripts/check_citations.py

Claims Verified
#	Claim	Evidence	Result
1	docs/dispositions/v0.4.1..7e2da1e.md: "Restoring the second walk turns it red" — and docs/guards.md DOUBLE-PARSE-DISAGREES, "Red on revert: 2"	Probed: the second-walk citable and the one-pass citable produce the identical two diagnostics on the case's own fixture, so the case is green under the revert	REFUTED
2	"Measured: 66 parses to 48"	citable now reads known.by_stem through module_symbols, which caches; the 48 modules under scripts/ are parsed once and the second walk is gone. The count itself was not re-run	partial
3	docs/guards.md MODULE-MAP-REBUILT: "the measurement in the round's record: 165 parses to 48, 0.77s to 0.22s"	The round's record for that finding says 165→66 parses and 0.77s→0.26s; 48 is the later finding's figure and 0.22s appears in no record	REFUTED — reported as gate finding 3 rather than counted twice here
4	"no reference to either parameter remains under scripts/, scripts/tests/ or tools/"	Grep finds none; RECORD_DEPTHS is read only by heading_texts	verified
5	"the now-unused subprocess import removed"	subprocess appears nowhere in test_validate_skills.py	verified
6	"git show 1abbb01 finds neither install_documents nor Fingerprint"	Confirmed against that tree	verified
7	"A self-referential symlink now reports what stopped the resolution"	fingerprint splits OUTSIDE from the remainder and carries resolved.error; the case asserts "could not be resolved" and the absence of the old wording	verified
8	Every guard symbol added resolves	check_citations.Modules, check_citations.citable, evidence_currency.fingerprint exist; both new test methods exist in the classes named (ModuleIdentity, DriftTests)	verified

Structural Causes
#	Finding	Cause (the thing to change)	Cause location	Gate that would carry it
1	1	Rewrap the paragraph after splicing into it — the edit prepended two sentences to a wrapped line and left the join at 120 characters, which is the fourth time in this range prose has been added to a paragraph without rewrapping it	AGENTS.md, the guards clause	Gate 1 (fail)
2	2	Put the new field behind the invariant the type already states rather than beside it: Symbols enforces mapping-xor-reason and exposes names through a raising property, and imports was added as a plain attribute with a default	scripts/check_citations.py Symbols	Gate 3 (fail)
3	3	Quote a measurement from the record that holds it, or state it as this ledger's own and date it — the row points at a record and quotes numbers that record does not contain, and credits one finding with two repairs' combined effect	docs/guards.md, MODULE-MAP-REBUILT row	Gate 6 (fail)
4	4, contract row 1, claim 1	Write the guard case against the observable difference the repair makes, or record that the repair has none. The disagreement removed was between two code paths' internal handling; both swallowed an unparseable module identically at the output, so no case over that fixture can separate them	scripts/tests/test_check_citations.py, docs/guards.md DOUBLE-PARSE-DISAGREES row	Gate 6 (fail)
5	—	citable now reads known.by_stem, which holds one path per stem, where the second walk read every file. A colliding stem's shadowed file no longer contributes imports. No instance exists — a collision is already reported by check — so this is recorded, not raised	scripts/check_citations.py citable	Gate 7 (pass)

Responsibility & Dependency Ledger
Gate 5 opened over the units 95d049d changed. It did not open for the rest of v0.4.1..HEAD, which this round did not inspect, nor for the two record files, whose partial coverage stops at Gate 2. Layering is inferred, as before.

Rows in this section are not findings.

#	Unit	Its job (one clause)	Handed in	Reached directly
1	check_citations.Symbols	hold what a module defines, or why this could not say	_names, reason, imports (constructor)	none
2	check_citations.module_symbols	say what a module defines and imports, or why this could not	known, module (parameters)	the filesystem, ast
3	check_citations.citable	name every module a citation may name without this tree defining it	known (parameter)	sys.stdlib_module_names
4	check_citations.check	collect every citation defect across the subject documents	root (parameter)	subjects, modules, citable, the filesystem
5	distribution_manifest.validate_install_pins	require every documented install pin to name the release being shipped	root, repository, version (parameters)	PINNED_INSTALL_DOCS (module constant), install_documents, stdout
6	evidence_currency.fingerprint	hash the text an entry claims to have measured, or say why it could not	root, tests, heading (parameters)	Boundary.at and resolve_within (constructed inline), the filesystem, hashlib
7	markdown_links.heading_texts	list every heading's text at a record's own depths	text (parameter)	RECORD_DEPTHS, PARSER (module constants)
8	test_check_citations.ModuleIdentity.test_an_unparseable_module_is_absent_and_reported_by_one_answer	assert one answer covers absence and defect for an unreadable module	none	TemporaryDirectory, the filesystem, check_citations
9	test_evidence_currency.DriftTests.test_an_unresolvable_path_says_what_stopped_it	assert an unresolvable path names what stopped it	none	TemporaryDirectory, the filesystem, evidence_currency
10	test_evidence_currency.DriftTests.test_each_way_a_fingerprint_fails_says_which_one	assert each way a fingerprint fails names itself	none	TemporaryDirectory, the filesystem, evidence_currency
11	test_validate_skills.ProjectedDescriptionTests.test_a_non_worktree_validates_from_the_filesystem	assert a root with no git metadata still validates	none	TemporaryDirectory, fixtures.write_distribution, shutil.rmtree
12	test_validate_skills.ProjectedDescriptionTests.test_a_non_worktree_still_reports_a_stale_pin	assert the fallback still reports a stale pin	none	TemporaryDirectory, fixtures.write_distribution, distribution_manifest.PINNED_INSTALL_DOCS, shutil.rmtree

Gate 1: Formatting & Syntax Hygiene
#	Location	Violation	Correction
1	AGENTS.md:515	Gate 1, formatting follows project convention: 120 characters of plain prose in a file whose prose wraps at 100. Two sentences were prepended to an already-wrapped line and the paragraph was not rewrapped, so the join carries "…could not have. A finding nothing can guard gets a row saying so. A repair measured only in a commit message cannot be re-run by". It is the only overlong line the commit added, and it lands in the paragraph the commit was editing. Fourth instance of this class in the range — d54130c, 64d6861 and the previous round's finding 2 are the others	Rewrap the paragraph from "have." to the end of the clause at 100 columns

Gate 3: Error Handling & Observability
#	Location	Violation	Correction
2	scripts/check_citations.py:145	Gate 3, invalid state fails fast: Symbols' docstring states "What a module defines, or why this could not say — never both, and never neither", __post_init__ enforces it, and names raises when a reason is held. The new imports: frozenset[str] = frozenset() sits outside that discipline — a Symbols carrying "could not be parsed" answers imports with a confident empty set, which reads as "this module imports nothing" where the truth is "this module could not be read". Nothing reads it wrongly today only because citable tests symbols.reason is None first, and a guard kept outside the type is the convention read_whole.Whole's docstring says the earlier rounds already had. It re-opens, through a new field, the swallow the same commit removed from citable	Store _imports and expose it through the same raising accessor as the payload

Gate 6: Business Logic Integrity
#	Location	Violation	Correction
3	docs/guards.md:63	Gate 6, logic matches the stated requirement: the MODULE-MAP-REBUILT row says "What settles it is the measurement in the round's record: 165 parses to 48, 0.77s to 0.22s". The round's record for that finding, docs/dispositions/v0.4.1..1609403.md, records 165→66 parses and 0.77s→0.26s. The 48 belongs to DOUBLE-PARSE-DISAGREES, recorded in docs/dispositions/v0.4.1..7e2da1e.md as "66 parses to 48", and 0.22s appears in neither record. The row credits one finding with two repairs' combined effect and points at a record that says something else — in the file that exists so a later round can retire a finding without re-deriving the story from commit messages	Restore the row's own figures — "165 parses to 66, 0.77s to 0.26s, in docs/dispositions/v0.4.1..1609403.md" — and leave the 66→48 with DOUBLE-PARSE-DISAGREES, where its record puts it. If 0.22s is to be kept it needs a dated section of its own, since no record holds it
4	docs/guards.md:66	Gate 6, logic matches the stated requirement: the DOUBLE-PARSE-DISAGREES row promises "Red on revert: 2" for reverting "the single pass in check_citations.citable, replaced by a second walk that parses the same files". Measured: that revert leaves test_an_unparseable_module_is_absent_and_reported_by_one_answer green. The old walk swallowed the unparseable module with except (OSError, SyntaxError): continue, so packaging was absent from the citable set under both implementations, and both emit the same two diagnostics on the case's fixture. The repair's observable effect is the parse count, not the output. (Reverting the signature instead makes check's citable(known) raise, which reddens all 25 cases in the file that reach check() — also not 2.) This is the failure the file's own "What was declined" section names as having happened here, now on the ledger built to stop it	Either give the case an input the two implementations answer differently — the only one that exists is a colliding stem, whose shadowed file contributed imports to the walk and does not to the map — or move the row to the no-guard table beside MODULE-MAP-REBUILT, since a pure convergence with no observable difference is guarded by its parse count and not by a test

Structural Appendix
check_citations.citable — the convergence is right in kind: one parse per module, one place where an unreadable module's meaning is decided, and the Symbols cache warmed for every module rather than only the cited ones. Two residues. The field it added escapes the type's invariant (finding 2), and reading known.by_stem rather than the tree means a colliding stem contributes only its first file's imports; the collision is already a reported diagnostic, so this is recorded and not raised, as the two previous rounds recorded their own.

evidence_currency.fingerprint — now five named reasons, each reachable and three of them covered by cases, with resolved.error carried on the branch that has something to say. resolved_inside correctly keeps its bool for records_missing, which only decides whether to read. Nothing further found.

distribution_manifest.install_documents — unchanged by this commit and its two branches remain non-equivalent, since git excludes what the workspace ignores and rglob does not, with nothing saying which answered. Still no instance in this tree. Carried forward from the previous appendix unchanged.

Sweep for the value-or-reason class — run again over scripts/ after this commit rather than over its diff. Symbols, Document, Boundary and Fingerprint all enforce the invariant in __post_init__ and expose the payload through a raising accessor; the one departure is Symbols.imports, which is finding 2. skill_yaml.get_top_level_yaml_value remains the recorded not-yet-three-state survivor.

Where this leaves the range: all nine findings from v0.4.1..7e2da1e are repaired and verifiable in the tree, and the guards ledger is now complete in the sense its own sentence claims. The six new findings are smaller than the last round's and three of them are in the ledger itself — two rows whose evidence does not hold up when followed, and the guard case behind one of them. Structural cause 4 is the one to settle first: a repair whose effect is a count and not an output cannot be guarded by a case, and saying so is cheaper than writing a case that proves nothing.

Next step: hand this to triage-findings, then persist this text at docs/reviews/v0.4.1..de72982.md and the disposition at docs/dispositions/v0.4.1..de72982.md in the turn that repairs. I have modified nothing in the repository.
````
