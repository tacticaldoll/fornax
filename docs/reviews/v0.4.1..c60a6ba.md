# Review Record — `v0.4.1..c60a6ba`

Archived as the producer wrote it, so a later round can check this round's reconciliation against
its input. Arrived by paste; the fenced block is the text as received and is not edited.

````text
Review Record
Source: local Git range v0.4.1..HEAD
Re-review result: unchanged — HEAD remains c60a6ba, worktree clean, and all prior findings remain open.
Calibration: Gates 1–7
Triage: Red 2 / Yellow 2 / Green 92
Coverage: partial — gate-reviewed: []; partially-gate-reviewed: scripts/check_citations.py (Gates 1–3), scripts/tests/scenarios/static-review/gate5-ledger/fixture/billing.py (Gates 1–3), scripts/distribution_manifest.py (Gates 1–6), scripts/evidence_currency.py (Gates 1–6); triage-only: all other changed paths in v0.4.1..HEAD; unread: []
Findings: 8
Verdict: FAIL at Gate 3 + SECURITY-ALERT + CONTRACT-VIOLATED
Not executed: tests, build, and runtime; read-only Ruff and git diff --check passed.

Gate Index
Gate	Status
1	pass
2	pass
3	fail
4	blocked
5	blocked
6	fail
7	blocked
8	not inspected

Findings
1. scripts/check_citations.py:192 does not catch UnicodeDecodeError while parsing a discovered Python module, so malformed UTF-8 crashes the checker instead of producing a diagnostic. Catch UnicodeError and preserve the unreadable-module reason.
2. billing.py:7 returns the pooled connection only on success; query, validation, update, and HTTP failures leak it. Use try/finally.
3. evidence_currency.py:101 accepts a registry without evidence: and accepts repeated section markers. Require exactly one section.
4. distribution_manifest.py:202 accepts an unterminated quoted ref if any later unrelated quote exists. Validate the nearest closing quote and add that negative case.
5. The billing fixture embeds a credential sink and private endpoint at billing.py:20, conflicting with the repository security rules.
6. The install-ref behavior violates the whole-token contract in AGENTS.md:595; the malformed quoted-ref case can pass.
7. charge_customer mixes database access, billing policy, HTTP notification, and credential access; split those responsibilities. Gate 5 was blocked before it could formally review this unit.
8. The two resolved development-known claims remain only partially established because tests/runtime were not executed.

Responsibility Ledger
Unit	Job	Handed in	Reached directly
distribution_manifest.install_refs	Extract and validate release refs.	text, repository	WORD_QUOTES, RELEASE_REF, Unread, whole
evidence_currency.load	Parse one evidence registry.	path	Path.read_text, parser regexes, _scalar, EvidenceError
````
