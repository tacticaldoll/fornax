# Review Record — `7b98106..d75d484`

Archived as received, from a second producer. Kept so the reconciliation in
`docs/dispositions/v0.4.1..d75d484.md` can be checked against its input rather than taken on trust.

Not byte-guaranteed and not reformatted: the record reached this triage inside a message, so what
follows is a transcription of that message. It is fenced, which preserves it and keeps its line
citations as text rather than as claims this repository makes. Those citations are correct for a
Review Record — `AGENTS.md` exempts a record's own evidence column, being a coordinate into the tree
as reviewed.

Its markdown tables arrived without pipes, as the producer emitted them; that is part of what was
received and is left alone.

```markdown
Review Record
Source: 7b98106..d75d484
Calibration: Gates 1–8
Triage: Red 1 / Green 4
Coverage: complete — gate-reviewed: five changed files; partially-gate-reviewed: none; triage-only: none; unread: none
Findings: 2
Verdict: FAIL at Gate 6 + CONTRACT-VIOLATED
Not executed: CLI runtime/build

Findings
#	Location	Violation	Correction
1	record_shape.py:416-418	Missing declared sections are silently accepted. Removing the complete Self-check, Dispositions, or Record integrity section produces no diagnostic. This bypasses RequiredKeys entirely.	Declare section cardinality per Seat; require Self-check and Dispositions, while representing the specific legacy Record integrity exception separately.
2	record_shape.py:416	Duplicate headings are collapsed to the first section by heading_section. A second Record integrity section containing an undeclared key is ignored.	Reject repeated governed headings or return every matching section and require exactly one.

Against-Contract
Clause	Falsifier	Result
Self-check contains both declared checks and is completed last	Remove the entire section	VIOLATED: validator passes
Every finding is placed in Dispositions	Remove the entire section	VIOLATED: validator passes
Governed tables follow their section rules	Append a second invalid Record integrity section	VIOLATED: invalid table is ignored

Structural Cause
Section cardinality is reduced to section | None before rules run. The historical exception for one missing Record integrity section is consequently applied to every seat and every record, while duplicate sections are indistinguishable from a single section.

Gate Index
Gate	Status
1–5	pass
6	fail
7–8	blocked

Verification
Workspace gate: passed
record_shape suite: 32 tests passed
Ruff and git diff --check: passed
Previous Result-prefix finding: fixed and covered
Reproductions: all three missing-section cases and the duplicate-section case returned no diagnostics
```
