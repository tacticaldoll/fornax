# Review Record — `v0.4.1..e144212`

Archived as received. Kept so the reconciliation in `docs/dispositions/v0.4.1..e144212.md` can be
checked against its input rather than taken on trust.

Not byte-guaranteed and not reformatted: the record reached this triage inside a message, so what
follows is a transcription of that message. It is fenced, which preserves it and keeps its line
citations as text rather than as claims this repository makes. Those citations are correct for a
Review Record — `AGENTS.md` exempts a record's own evidence column, being a coordinate into the tree
as reviewed.

Its gate table and its Against-Contract and Gate 5 Ledger tables arrived tab-separated rather than
piped, as the producer emitted them; that is part of what was received and is preserved here.

Written in Chinese, which the language policy admits for an archived Review Record: it is a copy of
what a producer wrote, and translating it would destroy the only property it has. The reasoning a
reader needs is in the English Disposition Record that reconciles it.

```text
審查結果
高：引號內的 # 被誤判為 shell 註解
read_whole.py:34 在交給 shlex 前先用不理解引號的 regex 截斷字串。
例如 echo "value # kept" 會在 read_whole.py:95 中變成未閉合命令並遭拒。應改用能追蹤單／雙引號與跳脫狀態的 comment scanner，並補 quoted-hash 測試。

中：section 規則沒有遵守契約宣告的 shape
record_shape.py:539 和 record_shape.py:557 收到 Shape 卻未使用，仍檢查所有固定 SEATS。若某契約 revision 未宣告某 seat，重複或 malformed 的該 section 仍會被錯誤判定。兩個迴圈都應先檢查 seat.heading in shape.keys。

低：unjudged 的區域型別註記錯誤
record_shape.py:680 宣告 list[str]，實際加入 (filename, reason) tuple，與 Audit.unjudged 的公開型別不一致。應改成 list[tuple[str, str]]。

Review Record
Source: v0.4.1..e144212
Calibration: Gates 1–8，涉及 shell、路徑、文件解析及驗證器
Triage: Green 118；上述問題由深入 gate review 發現
Coverage: partial

gate-reviewed：check_citations.py、contract_revision.py
partially-gate-reviewed：read_whole.py（Gates 1–3）、record_shape.py（Gates 1–2）
triage-only：其餘 114 個 changed paths，包括測試、治理文件及封存 records
unread：無
Findings: 5 個報告列，對應 3 個缺陷
Verdict: FAIL at Gate 2 + CONTRACT-VIOLATED
Not executed: 靜態審查，未執行測試或 build

Gate	狀態
1	pass
2	fail
3	fail
4–8	blocked
Against-Contract
Clause	Falsifier	Result
shell words 的 quoting 由 lexer 決定	echo "value # kept"	VIOLATED，regex 在 lexer 前截斷
record rules 僅約束契約宣告的 sections	shape 未包含 Self-check，record 卻有重複 Self-check	VIOLATED，仍產生 cardinality diagnostic
Gate 5 Ledger
Unit	Job	Handed in	Reached directly
contract_revision	讀取 record 落盤時的契約版本	root、record、contract、history	Git subprocess、filesystem
check_citations	驗證 durable prose 的 citation	root、paths、module map	AST parser、filesystem
```
