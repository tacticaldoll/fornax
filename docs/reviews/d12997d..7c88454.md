## Review Record

**Source**: git range `d12997d..7c88454`（11 個提交、11 個檔案），以本機 `git show` / `git diff` 解析，未取用任何遠端 PR/MR 資料。輸入契約另含 `docs/reviews/d54813e..8c87363.md` 與 `docs/dispositions/d54813e..8c87363.md`（上一輪 16 個 finding、14 個 cause；cause 1、3 於 `d12997d` 結算前落地，本 range 是其餘 12 個 cause 的修復）。
**Calibration**: Gates 1-7（出貨路徑；不觸及認證、加密、命令執行或使用者輸入邊界，故不開 Gate 8）
**Triage**: Red 0 / Yellow 7 / Green 4
**Coverage**: partial — gate-reviewed（Gates 1-7 全開）：`validate_skills.validate_skill` docstring、`runtime_contract.run_commands` 與其 import、`read_whole.Line` docstring、`docs/dispositions/d54813e..8c87363.md` 的 Reach 欄與新增揭露段。partially-gate-reviewed：`test_validate_skills.py` 的 `check` / `SchemaSeamTests` / `FillingAskedNotDefaultedTests`（Gates 1-6）；`test_read_whole.py` 的 `words`、`test_shell_script.py` 的 `LineHoldsNoNewline`、`check_citations.py` 的 `modules`、`test_module_claims.py` 的 `_MODULES` / `MODULES` / `PACKAGE_MODULES` 與 collision case、`docs/guards.md`、`docs/dispositions/8dc34ca..ba81ab1.md`（皆 Gates 1-2）。triage-only：無。unread：無。
**Findings**: 12（Gate 2 五列、Gate 6 一列、Against-Contract 六列 VIOLATED 且未在他處計入。不計入：與 gate finding 同一缺陷的 VIOLATED／REFUTED 列、Ledger 列、`holds`／`verified` 列、上一輪已判定為非 finding 且未變動的觀察）
**Verdict**: FAIL at Gate 2 + CONTRACT-VIOLATED + CLAIM-REFUTED
**Not executed**: 未執行 build、部署、遠端呼叫。**本輪不是純靜態**：在 scratchpad 內 clone 出隔離複本並 checkout `7c88454`，跑了完整測試套件、`ruff check .`、`check_citations.py`、`check_workspace.py`，以 `markdown-it-py` 在 range 內**每一個提交**上解析 `docs/guards.md` 的表格結構，並做了七次還原／注入量測。基準為 529 tests OK、ruff 全過、`OK   citations in 40 document(s)`、`Workspace checks passed.`。使用者工作樹未被讀取以外的操作觸及。

### Gate Index

| Gate | Focus | Status |
|---:|---|---|
| 1 | Formatting & Syntax Hygiene | pass |
| 2 | Naming & Readability | fail |
| 3 | Error Handling & Observability | pass |
| 4 | Control Flow & Structural Clarity | pass |
| 5 | Responsibility & Boundaries | pass |
| 6 | Business Logic Integrity | fail |
| 7 | Deduplication & Composition | blocked |
| 8 | Security & Parameter Integrity | not inspected |

Gate 1 對全部 11 個檔案開啟並通過：`ruff check .` 全過；新增行無行尾空白；`markdown-it-py` 實測 `docs/guards.md` 在 range 內每一個提交上都是 21 個表格、0 個落在表格外的豎線行，首尾兩端 21 個表格的 body row 數逐一相同（上一輪的 Gate 1 缺陷未重現）。Gate 2 在六個檔案失敗。Gates 3、4、5 對仍在階梯上的檔案開啟並通過。Gate 6 在 `test_validate_skills.py` 失敗。Gate 7 對四個檔案開啟並通過，其餘 blocked。

**未列為 finding 的觀察**：Markdown 散文超過 100 字元者新增兩行（全樹既有同類行數以百計，無寬度檢查）；`read_whole.COMMENT` 的註解仍指名 repo 側的 `runtime_contract.pins`，上一輪已判定為非 finding 且本 range 未動。

### Against-Contract

| # | Clause | Falsifier attempted | Result | Evidence |
|---|---|---|---|---|
| 1 | Cause 2 repair 2a：「route the four readers to the type's own module」 | 兩端實跑 `grep -rn 'shell_script\.Line'` | holds（實測） | `d12997d` 四處，`7c88454` 零處。`shell_script` 保留匯入但自身三處建構 `Line`，確為使用者而非第二個名字 |
| 2 | Cause 4 repair 4a：「record the reading in a dated section **with the corpus it was taken over**」 | 依該段自陳的語料與自陳的樹重跑該讀數 | **VIOLATED** | 段落記 61／7 檔／69。實測：在承載該段的提交與 range head 均為 **63／7／71**；61／69 只在其父提交上成立。差的 2 正是該段自己那句同時指名兩個符號。標題指定的樹重跑不出記下的數字——與 cause 4 被修的缺陷同一形狀 |
| 3 | Cause 5 repair 5a：「restore the original reading and put the change in the current section」 | 取覆寫前的讀數欄與 head 逐字比對 | holds（實測） | 位元組相同；supersede 段落另置，未動舊列 |
| 4 | Cause 6 repair 6a：「extend the existing disclosure」 | 檢查該揭露段是否只是延伸，或改動了既有段落所說的話 | **VIOLATED**（與 Gate 2 #4 同一缺陷） | 新段被插進既有句子中間，把原段落的兩句結語從原主詞剝離、改掛到新段。原段落因此失去其結論，新段落則以「這是座標不是 reading」收尾，而它自己第三句才說 Self-check 格「nearer to a reading than a coordinate is」 |
| 5 | Cause 7 repair 7a：對本 range 內每一個落地的修復逐列查其 guard 列 | 列舉 16 列，比對哪些修復落地、哪些列被更新 | **VIOLATED** | `INIT-COUNTED-AS-A-COLLISION` 列仍寫 13a、仍宣稱「reddens today」、仍標 `not measured`。實際落地的是 13b，且該提交完全未動 `docs/guards.md`。實測：在 `d12997d` 加一個帶 marker 的子套件 → 2 紅 + gate 紅；在 head → 全綠。「reddens today」為假，「not measured」與該提交自稱的「Measured both ways」互相牴觸 |
| 6 | Cause 8 repair 8a | 以 GNU grep 實跑兩種形式 | holds（實測） | 正確形式 → 0；錯誤形式 → 117；tracked `.md` 共 142。記錄的三個數字逐一相符 |
| 7 | Cause 9 repair 9a | 以 AST 列舉全檔預設值，逐一比對新 docstring | holds（實測） | 新句的每個子句成立 |
| 8 | Cause 10 repair 10a | 逐一讀四個 case 的極性 | holds | 新契約句與四案相符 |
| 9 | Cause 11 repair 11a + 兩處座標跟進 | 檢查全檔類別命名、兩處座標、citation gate | holds（實測） | 全檔 12 個類別皆以 `Tests` 結尾；兩處座標均已更新且後者在檔內揭露；gate 綠 |
| 10 | Cause 12 repair 12a | 讀新 docstring 每一句，回頭查它指名的每一個對象 | holds | 三句改寫後從新位置讀為真 |
| 11 | Cause 13：過濾移進 owner 後，依賴同一張圖的另一個問題是否跟著改變 | 把跨界 import 放進 `agent_skill_format/__init__.py`，跑完整套件 | **VIOLATED（本輪最重）** | `modules()` 現在整段跳過 `__init__.py`，`MODULES` 直接取 `by_stem`，`PACKAGE_MODULES` 由 `MODULES` 依路徑切出——於是 package 自己的與任何子套件的 `__init__.py` 都不在被斷言的集合裡。實測：package marker 跨界 → 529 tests OK；子套件 marker 跨界 → OK；同一行放進一般模組 → 恰 1 紅。邊界唯一的機制對 package marker 全盲，這正是 cause 1 的同型洞換一個入口重開 |
| 12 | Cause 14 repair 14a：`forbid`／「remove the helper's defaults」 | 比對既決記錄的 14a、guard 列、與樹內實際落地 | **VIOLATED** | 三份文件說三件事且無任何一份把它們連起來：記錄仍寫「remove the helper's defaults」、kind `forbid`；guard 列寫「the keyword-only marker」；樹內保留預設只是移到 `*` 之後。cause 的陳述在 head 仍成立。替代決定只寫在 commit message 裡 |
| 13 | AGENTS.md：「A measurement of the tree written into a message … belongs in a dated record instead」 | 逐一讀 11 個 message，分出兩類數字 | **VIOLATED** | 一個提交寫「the other **eleven classes in the file**」——純粹是對樹的量測（實測 12 個類別，敘述為真），且是重新命名的理由，全樹無 dated record 承載它。另兩個提交同形但較輕，兩者都同時寫進了 dated section，而規則寫的是 instead 而非 as well |
| 14 | AGENTS.md：「the section heading governs」 | 讀本 range 填入 `**measured**` 的每一格，再讀管轄它的標題與導言 | **VIOLATED**（與 Gate 2 #2 同一缺陷） | 一節標題寫「nothing measured」、導言寫「and means it」，其下 7 列現有 4 列寫 `**measured**`，其中 3 列由本 range 填入。另一節導言「The rest … are prospective」亦因本 range 填入一列而為假。緊接在上一節就有正解先例，本輪未採用 |
| 15 | AGENTS.md：「One commit per cause」 | 把 12 個 cause 對到 11 個提交 | **VIOLATED**（輕微） | 一個提交承載 cause 10 與 cause 11。兩者都落在同一檔案，但屬兩個獨立理由；message 有指名兩者，故為可見的決定而非隱瞞 |
| 16 | AGENTS.md：「a row for every finding a round accepted」 | 逐一 grep 16 個 finding id | holds（實測） | 16 個 id 各有且僅有一列 |
| 17 | AGENTS.md：「A matcher that reads a token must name the grammar's owner」 | 檢查本 range 新增／修改的每一個 matcher 與讀數 | holds | 新增的是字串相等比較，非 grammar；8a 的讀數已改用 POSIX bracket expression 並敘明其 owner |
| 18 | AGENTS.md：「Check the fixture actually fails when the rule is removed」 | 對新填的量測各做一次還原 | holds（實測） | 拿掉 wrap → 2 紅；再拿掉 cleanup → 3 紅。與記錄完全相符 |
| 19 | AGENTS.md：「Run repository validation for every change」 | 在隔離複本跑公開入口 | holds（實測） | 529 tests OK / Workspace checks passed / ruff 全過 / citations 綠 |
| 20 | AGENTS.md：「Do not cite a line number in durable prose」 | 搜尋並實跑 gate | holds（實測） | gate 綠；新增散文無 `path:line` |
| 21 | AGENTS.md Language Policy | 檢查新增文字 | holds | 全數英文 |
| 22 | AGENTS.md Final Report Notes：不得含 AI 署名 | 檢查 trailers、作者與訊息全文 | holds（實測） | trailers 全空，author 皆為 `tacticaldoll` |
| 23 | AGENTS.md Versioning／「Release bumps are separate」 | 檢查是否動到版本 | holds | 未觸及 |
| 24 | 7b 是否被誤當成已落地 | 檢查 `AGENTS.md` 是否被本 range 修改 | holds | 未修改；提交訊息明寫 7b 未取，與記錄的 Route 一致 |
| 25 | 上一輪 Carried forward 的 closure condition | 檢查 `check_citations.RECORDS` 是否變更 | holds（仍為 defer） | 未變；新段落提到 `docs/reviews` 只作語料對照，未提議變更常數 |
| 26 | PROJECT.md：「`family` is a flat field …」 | 以變體 filling 跑 validate 再跑 `skill_graph` | **VIOLATED**（既決餘量，不計入） | 與前三輪同一結果，已記為「by decision rather than by omission」 |
| 27 | AGENTS.md：「A round is a review and its settlement」 | 檢查本 range 是否應自帶記錄 | holds | 本 range 是上一輪的修復，不是新一輪；其結算即本 Review Record 與其 triage |

### Claims Verified（摘要）

逐一驗證 28 項。verified：2a 的 4→0、`shell_script` 確為使用者、ruff 對搬移後未用 import 的回報、cause 5 的逐位元組還原、表格結構以 owner 實測、揭露位置、cause 7 的兩個紅數（2／3，實測）、whitespace 的三個數字（117／142／0，實測）、cause 9 的 AST 對照、cause 10 的四案極性、cause 11 的兩處座標與 gate、cause 12 的三句改寫、cause 13 的非 marker 那一半。partial：citation cost 的 61／69 在其自陳的樹上為 63／71；keyword-only 在現有呼叫點為 no-op。**REFUTED**：「the same subpackage importing across the boundary turns exactly one case red」——跨界放在 `inner/thing.py` 恰 1 紅，放在 `inner/__init__.py` 或 package 自己的 `__init__.py` 皆 **0 紅**。

### Structural Causes

| # | Finding | Cause (the thing to change) | Cause location | Gate that would carry it |
|---|---|---|---|---|
| 1 | AC #11、Gate 2 #5 | 一張圖現在同時回答兩個不同的問題，而過濾條件只服務其中一個。改法：讓 owner 回傳完整的圖並把「哪些 stem 可被引用」留在 citation 側，或讓邊界的兩側改由路徑直接走 `rglob` 而不經過 citation 用的 stem 圖 | `check_citations.modules`，對照 `test_module_claims` 的 `MODULES` 與 `PACKAGE_MODULES` | Gate 5 (blocked) |
| 2 | AC #5 | 7b 未落地，所以「prospective 列在其 repair 落地時該怎麼處置」仍無規則；本 range 於是同時出現三種做法。改法：落地 7b，並補一句要求修復提交必須同時更新其 guard 列 | `AGENTS.md`，在「Write them when the record is written」一句處 | Gate 6 (not inspected) |
| 3 | AC #14、Gate 2 #2 | `docs/guards.md` 沒有規則說「一節的標題與導言在其列被填入量測後該不該更正」，而該檔已有正解先例。改法：把該先例寫成該檔開頭的一條 | `docs/guards.md` 開頭 | Gate 6 (blocked) |
| 4 | AC #12 | `check` 轉呼 `validate_skill` 時四個參數全部位置傳入，所以 keyword-only 只擋住 `check` 自己的呼叫者，擋不住這個 seam 本身。改法：`check` 內部改以關鍵字呼叫 | `test_validate_skills.check` | Gate 7 (blocked) |
| 5 | Gate 2 #1 | cause 12 的 sweep 沒有列舉機制，只改了型別自己的 docstring。「哪些散文指名這個型別的舊名」是一個可列舉的問題。改法：把 cause 12 的 reach 擴為「指名舊名的每一句散文」 | `test_read_whole.words`、`test_shell_script.LineHoldsNoNewline` | Gate 2 (fail) |
| 6 | — | `runtime_contract` 的三行 import 分屬 package 內外且交錯排列；本 range 在中間那行加了一個名字而未整理分組。已登記為 `import-grouping-unchecked`，故不計入 finding，但這是同一個既知第三次出現在同一個檔案 | `runtime_contract` 的 import 區 | Gate 1 (pass) |

### Gate 2: Naming & Readability

| # | Location | Violation | Correction |
|---|---|---|---|
| 1 | `test_read_whole.words`、`test_shell_script.LineHoldsNoNewline` | Gate 2：型別的舊名還活在兩句 docstring 裡，而兩處都在本 range 改過的單元內。前者寫「`shell_words` takes a `shell_script.Command` now」——模組錯、類別名也錯；後者類別名為 `LineHoldsNoNewline`，docstring 卻寫「a reader of a `Command`」。全樹 `Command` 僅剩這兩處程式碼站點。這是本輪 Pattern 的第四與第五個實例，且是在修好第三個的同一個 range 裡留下的 | 兩處改為 `Line`，與 cause 12 的 reach 併為一次 sweep |
| 2 | `docs/guards.md` 兩節的標題與導言 | Gate 2：標題與導言說的話被它們自己管轄的列否定。一節寫「nothing measured」「and means it」而其下 4 列標 `**measured**`；另一節導言寫「The rest … are prospective」而其中一列已填入。緊鄰其上就有本檔自己的正解先例 | 依先例更正標題與導言（列不動） |
| 3 | `docs/guards.md` 的揭露段 | Gate 2：揭露段的「this round」在任何讀法下都不成立——它被插在一個標題與該節自己的導言之間，相鄰兩段各有一個沒有限定的 round 指涉不同輪次，而實際做 regroup 的又是第三個輪次。此外後續一個提交又改了同節另一列的座標，該段未跟著延伸 | 換成具名輪次，移到該節導言之後，並把後續座標更新併入同一段 |
| 4 | `docs/dispositions/8dc34ca..ba81ab1.md` 的揭露段 | Gate 2：新段落被插進既有句子中間，順手把上一段的兩句結語奪走。後果有三：原段落失去它的理由句與揭露句；「Same」的先行詞跨過一整段落；新段落以「這是座標不是 reading」收尾，卻在自己第三句才寫過 Self-check 格「nearer to a reading than a coordinate is」。這與既決 finding `DISCLOSURE-ATE-ITS-SUBJECT` 同形 | 兩句結語留在原段落，新段落自己寫收尾，明確分開兩類編輯 |
| 5 | `check_citations.modules` docstring、`test_module_claims` 的 `MODULES` 註解 | Gate 2：同一個過濾器讓兩句「every …」同時變假。`modules()` 首句仍寫「Every module under `scripts/`」而它現在跳過 `__init__.py`；`MODULES` 的註解亦然——package 的 `__init__.py` 確實在散文裡對依賴做了宣稱，而它永遠不會被掃到 | 兩句各自加上排除條件，實質修法見 Structural Causes #1 |

### Gate 6: Business Logic Integrity

| # | Location | Violation | Correction |
|---|---|---|---|
| 1 | `test_validate_skills.SchemaSeamTests` docstring | Gate 6：新契約句寫「**three cases** are refused under `FORNAX_FORMAT`」——把「樹裡有幾個測試」寫進 docstring。AGENTS.md 明文列舉了這條規則的主詞（「not how many … modules or tests there are」），四個 case 不是它的豁免項。它也會隨樹走味。本 repo 已有同型的既決 finding 與其修法示範 | 改寫成不帶數字的形式：「the per-skill cases are refused under `FORNAX_FORMAT` … and the collection-level case is the other way round」 |

### Structural Appendix（摘要）

**package 邊界** — 匯入層仍乾淨（package → repo 零邊），但守住這件事的唯一機制有一個新入口：`13b` 把 `__init__` 過濾移進 owner 之後，package 與所有子套件的 marker 一起離開了被斷言的集合。三次注入實測：package marker 跨界全綠、子套件 marker 跨界全綠、一般模組跨界恰 1 紅。cause 1 的教訓在 stem 層成立了，在 marker 層還沒有。

**`read_whole.Line`** — 本 range 完成度最高的修復；同檔 `COMMENT` 的註解是該 package 內唯一還指名 repo 側單元的散文，carve-out 執行時會懸空。

**`test_validate_skills.check`** — 14a 以 keyword-only 落地，在現有呼叫點是完全的 no-op；`check` 轉呼 `validate_skill` 時仍四個參數全位置傳入。

**`docs/guards.md`** — 六次編輯。修好的：讀數逐位元組還原並自當前章節 supersede、whitespace 讀數改為 POSIX class、兩列 prospective 填入並敘明、citation cost 取得具名語料的 dated section、表格結構在每一個提交上完整。新造或未及的：一列在其修復落地後未更新、兩節標題被自己的列否定、citation cost 的數字在其自陳的樹上重跑不同、一段揭露指涉錯輪次。

**本輪的 Pattern** — **修復的 reach 被算成「這個單元」而不是「這個問題的每一個答案處」。** cause 12 只修了型別自己的 docstring；cause 13 只修了 collision 這一個消費者；cause 4 與 7 修的是「量測該寫在哪」，但寫下去的量測沒有被拿回它自稱的樹上重跑一次。sweep 規則對這三件事都給了做法，三次都沒有執行到列舉那一步。
