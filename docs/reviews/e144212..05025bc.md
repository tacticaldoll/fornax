# Review Record — `e144212..05025bc`

Archived as received, from a review run in fresh context because the author of the range could not
supply one. Kept so the reconciliation in `docs/dispositions/e144212..05025bc.md` can be checked
against its input rather than taken on trust.

Not byte-guaranteed and not reformatted: the record reached this triage inside a message, so what
follows is a transcription of that message. It is fenced, which preserves it and keeps its line
citations as text rather than as claims this repository makes. Those citations are correct for a
Review Record — `AGENTS.md` exempts a record's own evidence column, being a coordinate into the tree
as reviewed.

```text
## Review Record

**Source**: local `git diff e144212..HEAD` (HEAD = `05025bc`) — six commits, 8 changed paths, +460/-59
**Calibration**: Gates 1-8 — scripts/read_whole.py is the repository's token-reading contract and its
only production consumer, runtime_contract, decides from it whether a `pip install` in
.github/workflows/validate.yml agrees with requirements-maintenance.txt; a misread there is a
validator bypass, which is Gate 8's boundary
**Triage**: Red 0 / Yellow 2 / Green 6
**Coverage**: partial — gate-reviewed (Gates 1-8): record_shape.record_defects, record_shape.RecordRule,
record_shape.RequiredSections, record_shape.OneSectionEach, record_shape.ReadableTable,
record_shape.audit, test_record_shape.UndeclaredSeatIsNotJudged; partially-gate-reviewed:
read_whole.shell_words (Gates 1-6; 7-8 blocked by its own Gate 6 failure), read_whole.COMMENT (1-6),
test_read_whole.ShellWordTests (1-6), test_read_whole.CommentRule (1-6), development-knowns.yaml entry
shell-command-structure-read-by-hand (Gates 1-2 and 6), docs/guards.md sections (Gates 1-2 and 6),
docs/dispositions/v0.4.1..e144212.md (Gates 1-2 and 6), docs/reviews/v0.4.1..e144212.md English
preamble (Gates 1-2 and 6); triage-only: test_record_shape.py outside UndeclaredSeatIsNotJudged,
record_shape.py outside the six changed units, read_whole.py outside the two changed units,
development-knowns.yaml entries other than the one changed; unread: none.
**Findings**: 11 reported rows over 5 defects — 5 gate rows, 5 Against-Contract rows reading VIOLATED,
1 Claims-Verified row reading REFUTED. The Ledger rows, the triage class counts, the Against-Contract
and Claims rows reading holds or verified, and the Structural Causes rows are excluded.
**Verdict**: FAIL at Gate 6 + SECURITY-ALERT + CONTRACT-VIOLATED + CLAIM-REFUTED
**Not executed**: build, CI, and the tools/fornax-cli suite. The full workspace gate was deliberately
not run — it was green before this review and a green gate is not evidence of one. Executed in a
throwaway copy under the scratchpad: the unit suite at e144212 and at HEAD, six revert measurements,
a differential fuzz of read_whole.shell_words against bash over 350 constructed commands, an AST
sweep of every local list annotation against its append, and ruff plus check_citations on the
changed scope.

Gate Index: 1 pass, 2 pass, 3 pass, 4 pass, 5 pass, 6 fail, 7 blocked, 8 blocked.
Gates 7-8 are blocked only for read_whole's units and test_read_whole.py; no gate is blocked for
record_shape.py or test_record_shape.UndeclaredSeatIsNotJudged, which is why those are declared
gate-reviewed without contradiction.

Security Triage Alert
  pip install "tool==9.9.9"#x
    shell_words -> ['pip', 'install', 'tool==9.9.9']   # reads a clean pin
    bash        -> ['pip', 'install', 'tool==9.9.9#x']
    requirement('tool==9.9.9#x') -> Unread
  A workflow line bash would install as tool==9.9.9#x, which runtime_contract is built to report as
  unreadable, is read as a well-formed pin and compared clean. Bound honestly: the input is an
  in-repo contributor-authored workflow file, and validate.yml today yields five commands, none
  containing a hash, though one carries a quoted spec.

Against-Contract
 1 shell_words docstring "quoting decided by a real lexer" | echo "a"#b — bash yields one word a#b,
   the module yields ['echo','a'] | VIOLATED | read_whole.py:132
 2 AGENTS.md "Read a token whole, never a prefix of it" | same input returns a shorter well-formed
   word list rather than an Unread | VIOLATED | read_whole.py:122-136
 3 AGENTS.md "A matcher that reads a token must name the grammar's owner, and use it" | the
   comment-start predicate is hand-written over the scan's output | VIOLATED | read_whole.py:132
 4 docs/guards.md "A row is an instruction a later round runs" | revert COMMENT's operator lookbehind:
   3 red at e144212, whole suite green at 05025bc | VIOLATED | docs/guards.md:214
 5 development-knowns.yaml "The shell's words and its comment rule are both shlex's now" | as row 1 |
   VIOLATED | development-knowns.yaml:35
 6 "a find from a running cursor is exact" | fuzzed 8,704 commands, 0 violations | holds
 7 RecordRule "a rule never handed an undeclared seat cannot judge one" | holds, measured
 8 50447d0 "the per-seat table rules pass through the same gate" | holds
 9 Audit.unjudged annotation agrees with what is appended | holds
10 development-knowns.yaml verification "grep re.compile enumerates every matcher in the tree" | the
   new decision is token.startswith("#"), which that enumeration cannot see; the field itself warns
   the list has under-reported twice, and this is the third | VIOLATED | development-knowns.yaml:48
11 AGENTS.md "Do not cite a line number in durable prose" | holds
12 AGENTS.md "Do not write a count of what the repository contains" | holds
13 AGENTS.md "revert the unit the claim names" | holds
14 AGENTS.md Commit Style / Classification | holds
15 AGENTS.md two negative controls | the near-miss control exists; the alternate spelling — a hash
   ending a quoted word — is absent, and is the exact form that fails | VIOLATED (counted with row 1)

Claims Verified
 1 64d9f28 "both wrong repairs are held by controls" | verified
 2 64d9f28 "reverting the pre-lexer split reddens the quoted-hash case and nothing else" | verified
 3 64d9f28 "checked against bash over the new and hostile cases, which agreed everywhere" | REFUTED —
   a differential run over 350 commands found a coherent divergence class of 8 ("a"#b, 'a'#b, ""#x,
   "a b"#c and their bare-hash forms) plus 2 in the escaped-whitespace form. The hostile set omitted
   a hash adjacent to a closing quote, one character to the right of the case being repaired
 4 "runtime_contract.pins is the only caller left of COMMENT" | verified
 5 50447d0 "the corpus answers exactly as before" | verified
 6 50447d0 "reverting the gate reddens both new cases and nothing else" | verified
 7 1aaf029 "swept the same class; this was the only instance" | verified, extended to extend and
   augmented assignment
 8 05025bc "each revert applied, suite run, tree restored, green afterwards" | verified
 9 guards row QUOTED-HASH red=2 | verified
10 guards row RECORD-RULES red=2 | verified
11 05025bc "no such escaped-hash case existed in CommentRule" | verified
12 disposition Scope "118 files" | verified
13 disposition Self-check lifecycle partition | verified by set comparison
14 disposition Self-check "every commit reachable from HEAD" | verified
15 disposition Self-check "every symbol resolved" | verified
16 guards row "ruff is selected to E, F and W" | partial — ruff.toml declares
   select = ["E","F","W","PGH004","RUF100"]. The conclusion survives; the enumeration is short by two
17 guards row "the repair has landed and nothing went red for it" | verified
18 development-knowns.yaml "such a line carries no shell quoting to misread" | unprovable statically —
   reverting COMMENT's operator lookbehind leaves the suite green, so nothing distinguishes the narrow
   rule from the wide one on a requirements line
19 01d5dfb "found by the sweep the repair owed" | unprovable statically — no artifact records it

Structural Causes
 1 finding 1 | Stop inferring a shell word boundary from a scan token boundary. Ask whether the token
   begins a word in the text — preceded by unquoted whitespace, an operator, or nothing | read_whole.py:132 | Gate 6
 2 finding 2 | Give read_whole.COMMENT a test reaching it directly or through runtime_contract.pins.
   CommentRule only ever reached it through shell_words | test_read_whole.py:126 | Gate 6
 3 finding 2 | The ledger's protection covers a rename and not a decoupling. Make a repair's turn
   re-measure the rows whose named unit or guard its Reach touches | docs/guards.md:11-15 | Gate 6
 4 finding 3 | State the registry's enumeration over hand-written grammar decisions rather than over
   re.compile sites | development-knowns.yaml:48 | Gate 6
 5 (no finding) | Pre-existing, not authored by this range, so it set no gate status:
   test_read_whole.py places `if __name__ == "__main__": unittest.main()` before class CommentRule.
   A direct run collects 13 tests; discover collects 16. A targeted direct run silently skips the
   entire class the guards ledger names | test_read_whole.py:123 | Gate 1 (pass)
 6 finding 1 | Process-level: AGENTS.md warns that a sweep does not validate the repair it prompts.
   01d5dfb's sweep found the surviving COMMENT caller and registered it; nothing tested the new
   boundary rule the repair introduced | development-knowns.yaml:35, read_whole.py:101-105 | Gate 6

Gate 6: Business Logic Integrity
 1 read_whole.py:132 — the comment-start rule is token.startswith("#") over a posix=False scan, but
   that scan ends a token at a closing quote, so a hash immediately following one opens a new token
   while opening no shell comment. echo "a"#b returns ['echo','a'] where bash gives one word a#b;
   echo "a"#b "keep me" silently drops a later word. Correction: treat a hash-leading token as a
   comment only when it genuinely begins a word. Measured against bash over the same 350 commands,
   this removes 8 of the 10 divergences; the residual 2 are the escaped-space form.
 2 docs/guards.md:214 — the SHELL-COMMENT-RULE-NARROWER-THAN-CLAIMED row instructs a later round to
   revert COMMENT's operator lookbehind and expect 3 red. Measured: 3 red at e144212, whole suite
   green at 05025bc. This range severed shell_words' call to COMMENT and CommentRule only reached it
   through that call, so the guard died with no rename for check_citations to catch. The finding it
   guards is still open and this row was its only re-runnable path out.
 3 development-knowns.yaml:48 — the verification field states grep re.compile enumerates every
   matcher; the repair moved a grammar decision into str.startswith, where it cannot be seen.
 4 docs/guards.md:309 — the row rests on "ruff is selected to E, F and W"; the config holds five
   rules. The conclusion is unaffected but the enumeration is short by two.
 5 development-knowns.yaml:29 — generalising the statement turned its list into the rule's extension,
   and the entry's own evidence exceeds it: distribution_manifest.install_refs bounds an unquoted ref
   by hand-written rules, not by a construct's own closing delimiter.

Handoffs: settling these belongs to triage-findings. The repeated shape — a repair that closes the
named instance and reopens the class one character away — is the Pattern the prior disposition
routed to harden-skill; this round is evidence the routing was correct and that nothing changed.
```
