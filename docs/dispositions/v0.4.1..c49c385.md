# Disposition Record — `v0.4.1..c49c385`

**Source**: a `static-review` Review Record for `v0.4.1..c49c385`, 9 findings,
PASS + CONTRACT-VIOLATED + CLAIM-REFUTED
**Scope**: `v0.4.1..c49c385`. Coverage is `triage-only` over an enumerated path list, with
`gate-reviewed` and `partially-gate-reviewed` both empty and `unread` empty. The paths are
enumerated, so membership is decidable; no gate was opened, so nothing here can close a prior
finding by silence
**Prior round**: `docs/dispositions/v0.4.1..64d6861.md`

The repairs settled here land above `c49c385`, in `00d85e2..eb56f8d`.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | PASS + CONTRACT-VIOLATED + CLAIM-REFUTED | every gate `not inspected`; PASS with no gate opened is the record's stated meaning for an uninspected ladder | pass |
| Calibration / Gate Index | Gates 1-8 | no gate carries a status other than `not inspected`, so the calibration declares a ladder the record never opened. The Structural Causes rows say so themselves — each names the gate that *would* have carried it, all `not inspected` | mismatch |
| Finding count | 9 | the Against-Contract track holds 10 rows, of which 5 are VIOLATED and 2 are `unprovable statically`; the Claims track holds 9, of which 4 are REFUTED. 5 + 4 = 9, so the count excludes the two unprovable rows and the holds | pass |
| Coverage | partial, `triage-only` enumerated, `unread` empty | the list enumerates paths and the record makes no gate claim about any of them | pass |
| Non-finding sections | not declared | the record carries a Structural Causes table and no Ledger; it does not state whether Structural Causes rows are findings | not claimed |

The two `unprovable statically` rows (AC-6, AC-7) are not flattened into the finding list. They
report that a clause cannot be settled by reading, which is a fact about the method, and both name
prior findings that already have a home in the previous record.

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| QUOTED-REF | re-reported as `AC-1` and `claim 1` | inside | current finding → Dispositions, and Recurring |
| COUNT-INVENTORY | re-reported as `AC-2` and `claim 2` | inside | current finding → Dispositions, and Recurring |
| COUNT-COMMITS | re-reported as `AC-3` and `claim 3` | inside | current finding → Dispositions |
| AC-6 of the prior round (the record is missing) | re-reported as `AC-5` | inside | current finding → Dispositions |
| F-11, F-12 | `AC-6` and `AC-7` state that neither is decidable by static reading; the coverage enumerates no commit object | undetermined | Undetermined |
| VERDICT-SEAM, VERDICT-CONTROL, FENCE-HANDREAD, BINARY-AS-TEXT, INPUT-PATTERN-SPLIT | `AC-8`, `AC-9`, `AC-10` and claims 5-8 report the repairs as holding | inside | closure candidate → Carried forward, see below |
| REGISTRY-UNDERREPORT, REGISTRY-NOT-DERIVED, LEDGER-TABLE-ROUNDS, RAWSCORES-PROVISIONAL | the files are in the enumerated `triage-only` list; no gate was opened on them | inside | closure candidate → Carried forward |

No prior finding closes this round. The closure test requires the input to have gate-reviewed the
unit or to verify the repair; a `triage-only` coverage cannot do the first, and the rows that report
a repair as holding are the input's own contract probes rather than a gate on the unit. This is the
first condition in the test, and it is unmet for every candidate — including the ones this round
says are fine.

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | Where a ref ends and whether its word is closed are one flag, so a fragment boundary proves a quote closure that never happened | REF-FRAGMENT-CLOSES-QUOTE (AC-1, claim 1) | 1a ask the closure of the text — the word's closer is wherever that quote character next appears — and keep the ref's own end at the quote or the fragment hash | distinguish | `scripts/distribution_manifest.py:185`, `scripts/distribution_manifest.py:202`, `scripts/tests/test_validate_skills.py:1216` | `plan-implementation` |
| 1 | ″ | ″ | 1b require the fragment to be followed by a closing quote before the ref is read at all | guard | `scripts/distribution_manifest.py:186` | `plan-implementation` |
| 2 | The count rule's measurement carve-out was read as exempting any number a measurement yields, so measurements of the tree were written into the prose that reported their removal | ORDINAL-IN-NEW-MODULE (AC-2, claim 2), COUNT-IN-COMMITS (AC-3, claim 3) | 2a narrow the carve-out to an experiment's parameters, and say that reading a commit message against the rule means reading its own sentences | declare | `AGENTS.md:682`, `AGENTS.md:708`, `scripts/markdown_links.py:126` | document — `AGENTS.md`, maintainer-owned |
| 2 | ″ | ″ | 2b rewrite the offending messages | `rewrite history (gap)` | 3 commit objects, unpushed at the time of the finding | declined — see Dispositions |
| 3 | Closure evidence in a Disposition Record was written from the session rather than from the file, in the row that asserts closures were verified | CLOSURE-NAMES-WRONG-SYMBOL (AC-4, claim 4) | 3a name each symbol with the line it sits on, read back from the file, and leave the correction visible in the cell | restate | `docs/dispositions/v0.4.1..64d6861.md:140`, `docs/dispositions/v0.4.1..64d6861.md:141`, `docs/dispositions/v0.4.1..64d6861.md:142` | document — disposition record, maintainer-owned |
| 4 | Four review rounds were never settled by a persisted record, and their texts are gone | ROUNDS-UNSETTLED (AC-5) | 4a persist a record for every round from here on, this file included | restate | `docs/dispositions/v0.4.1..c49c385.md` | document — maintainer-owned |
| 4 | ″ | ″ | 4b reconstruct the lost rounds from the commits that repaired them | `reconstruct (gap)` | not enumerable — the records' texts do not exist | declined — see Dispositions |

## Pattern

Causes 1 and 3 are the shape the previous record named, arriving twice more: **prose states the
property intended and the implementation carries a narrower one, and the gap is invisible because
the narrow reading returns a well-formed answer.** A fragment returned a valid release; a wrong
symbol name returned a sentence that reads as verification.

What is new is where they landed. Both were written **in the repairs for that same pattern** — the
quote-closure guard added one round earlier, and the closure cells in the record that reported the
pattern. Cause 2 is the same again in the governance layer: the exemption that made a measurement
look safe was read while removing measurements.

The pattern-level repair already recorded — feed the boundary something that crosses it — did not
fire here because the boundary was thought to be the one just fixed. The addition is narrower and
mechanical: **a claim naming a symbol is checked by opening that symbol**, and a guard added for one
malformed spelling is checked against the other spellings of the same malformation, not only the one
reported.

## Coupling

- `1a` voids `1b`: with the closure asked of the text, the fragment needs no special rule.
- `2b` is voided by nothing and declined on its own terms.
- `4a` does not void `4b`; `4b` is impossible rather than unnecessary.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| REF-FRAGMENT-CLOSES-QUOTE — `"…@v1.2.3#egg=x` with no closing quote reads as the shipped tag | 1 | carried from QUOTED-REF | accept | `00d85e2`, `1a` | Measured before: both quote characters, with `#subdirectory=` and `#egg=`, returned `v1.2.3` and no diagnostic. After: both are unread, the closed fragment-bearing form still reads `v1.2.3`, and the plain unterminated and quoted-with-space forms are unchanged. Restoring the flag turns the two new subtests red |
| ORDINAL-IN-NEW-MODULE — `marked_code_blocks` opens with "A third operation" | 2 | carried from COUNT-INVENTORY | accept | `5f0afe0`, `2a` | The line is reworded, and the reason it survived a sweep of the same shape is now in the rule rather than in this record alone |
| COUNT-IN-COMMITS — measurements of the tree in three post-rule messages | 2 | carried from COUNT-COMMITS | decline for those objects, accept for the rule | `2a`; `2b` declined | The prior round declined rewriting for the same reason and it holds here: a message describes the change it made, and rewriting it to satisfy a rule read later falsifies what was said. What binds the next message is the rule, which now says the exemption covers an experiment's parameters and not a measurement of the tree |
| CLOSURE-NAMES-WRONG-SYMBOL — the Closed table names `scenario_root`, `section`, and `pins` for mechanisms they do not carry | 3 | new | accept | `eb56f8d`, `3a` | `scenario_root` asks `spelled_inside` and requires a root below `SCENARIOS`; the resolution is `resolved_inside`, called at the two read sites. `section` does not exist — the caller is `section_text`. `pins` reads each declaration through `requirement`, which constructs `Requirement`. Every symbol now carries its line, read back from the file, and both corrections stay visible in the cells |
| ROUNDS-UNSETTLED — four rounds have no persisted record | 4 | carried | accept in part | `4a`, this file | This round is settled here rather than left to be the fifth. The four lost rounds stay lost; `4b` is declined because their texts do not exist, and no reconstruction from commits would be the record of what those rounds decided |

## Carried forward

Every prior finding this round did not re-report, because no closure condition is met by a
`triage-only` coverage:

- **VERDICT-SEAM**, **VERDICT-CONTROL**, **FENCE-HANDREAD**, **BINARY-AS-TEXT**,
  **INPUT-PATTERN-SPLIT** — the input reports each repair as holding under contract probes
  (`AC-8`, `AC-9`, `AC-10`, claims 5-8). A probe is not a gate on the unit, and the closure test's
  first condition asks for the unit to have been gate-reviewed. They are carried with their original
  `accept` and their reasons unchanged; what a gate-opened round would close is exactly this.
- **REGISTRY-UNDERREPORT**, **REGISTRY-NOT-DERIVED**, **LEDGER-TABLE-ROUNDS**,
  **RAWSCORES-PROVISIONAL** — in the enumerated coverage, no gate opened, not re-reported, nothing
  said about them either way. `REGISTRY-NOT-DERIVED` keeps its `decline` and the weighing that
  produced it.

## Closed

None. A round that opens no gate closes nothing, which is worth stating plainly: this round's PASS
is a statement about what its probes found, not about what its ladder examined.

## Out of scope this round

None. The enumerated coverage reaches every prior unit but commit prose.

## Undetermined

**F-11 and F-12**, unchanged from the prior round, and this round says so in its own words: `AC-6`
is `unprovable statically — causality requires the deferred governance decision`, and `AC-7` is
`unprovable statically — "when practical" is judgment`. That is the same conclusion the prior record
reached from the other side, and it is now stated by the reviewer as well as by the triage. The
deferred decision is the handoff to `audit-governance` that the digest round recorded and nobody has
made.

## Recurring

- **REF-FRAGMENT-CLOSES-QUOTE**, fourth instance of the cause first recorded as
  INSTALL-TAG-BOUNDARY. The prior repair's Reach — `scripts/distribution_manifest.py:154`, `scripts/distribution_manifest.py:185`,
  `scripts/distribution_manifest.py:187`, `scripts/tests/test_validate_skills.py:1193`,
  `scripts/tests/test_validate_skills.py:1213`, `development-knowns.yaml:37` —
  changed completely, and the finding survived in a form the repair itself introduced: the guard
  added for the unterminated quote used a flag that any end character set. The cause stands as
  restated last round and gains a clause: **a guard written for one malformed spelling is verified
  against the other spellings of that same malformation.**
- **ORDINAL-IN-NEW-MODULE**, third instance of the count cause. Its Reach changed completely and it
  reappeared inside the sweep's own commit. The cause was stated as the wording; the code shows it
  is the carve-out that made a measurement look exempt, and that is what changed this round.

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — QUOTED-REF, COUNT-INVENTORY, COUNT-COMMITS and the prior round's `AC-6` appear in Dispositions under this round's ids; the nine unre-reported findings are in Carried forward; F-11 and F-12 are in Undetermined and named nowhere else |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — causes 1, 2 and 3 enumerate `file:line`, each line read back from the file before this row was written. Cause 4's `4a` names this file, which is a whole-file Reach because the repair is the file; its `4b` is declined and names nothing enumerable, which is the reason it is declined |

## What this round leaves standing

- **Nine carried findings that no round has yet closed**, because the last round to open a gate on
  them was the one that reported them. A closure needs a gate-reviewed unit, and two consecutive
  rounds have now been `triage-only` or contract-probe passes over an unchanged tree.
- **F-11 / F-12**, with both the review and the triage now saying the same thing: nothing static
  settles them, and the governance decision they wait on has not been asked for.
- **The four lost rounds**, permanently.
