# Disposition Record — `v0.4.1..bdb4542`

**Source**: `docs/reviews/v0.4.1..bdb4542.md`, 4 findings, PASS + CONTRACT-VIOLATED + CLAIM-REFUTED
**Scope**: `v0.4.1..bdb4542`. Coverage is partial: `scripts/check_citations.py` completed Gates 1–4,
the rest of the range completed triage only
**Prior round**: `docs/dispositions/v0.4.1..ef8b53c.md`

The repairs settled here landed at `1abbb01`, and this record is written in the same turn.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | PASS + CONTRACT-VIOLATED + CLAIM-REFUTED | Gates 1–4 pass, 5–8 not inspected, and the input says in its own words that this is not a pass until the contract rows are repaired — a verdict that names the ladder's state and the record's separately | pass |
| Calibration / Gate Index | Gates 1–8 | Gates 1–4 carry statuses over the one gate-reviewed unit; 5–8 are `not inspected`. Gate 4's pass carries its reasoning — `citations()` is long and nested, and the control flow is followable along its early continues — which is the first gate this range has had passed with a stated reason rather than a status | pass |
| Finding count | 4, given as three contract violations plus one refuted claim counted separately | 3 violated contract entries + 1 refuted claim, and the claim keys to the first contract entry. So 3 stable findings, and the input says how it reached 4 | pass |
| Coverage | partial, the gate-reviewed unit and its gates named, the remainder triage-only | stated as a count of the remaining range rather than an enumeration, which is the shape earlier rounds reported. The gate-reviewed unit and its gates are exact, which is what decides what can close | mismatch |
| Non-finding sections | not declared | no section carries rows that are not findings | not claimed |

This is the first input in this range whose verdict, calibration and finding count all reconcile.

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| URL-EXEMPTION-TOO-WIDE | reported again as entry 1 and the refuted claim | inside | current finding → Dispositions, and Recurring |
| COUNTS-IN-THE-NEW-PROSE | reported again as entries 2 and 3 | inside | current finding → Dispositions, and Recurring |
| DIAGNOSTIC-COMPOSES-A-PATH | a claim reports the actual module path in use | inside | Closed |
| CITABLE-SET-ASKS-THE-ENVIRONMENT | a claim reports legitimacy no longer depending on the local environment | inside | Closed |
| EXTERNAL-SYMBOL-UNCHECKED | a claim reports the boundary stated and registered | inside | Closed |
| ROUND-ENDS-WHEN-ASKED | a claim reports both missing rounds' records saved | inside | Closed |
| RECONCILED-AGAINST-A-PARTIAL-COPY | a claim reports the record no longer guessing where the received text began | inside | Closed |
| CHECKER-ERROR-STATES, CHECKER-STEM-COLLISION, EXTENSION-RULE-TOO-NARROW | the Gate Index reports Gate 3 pass over the unit that carries them | inside | Closed |
| The twelve carried findings | the input states they remain without a gated review | undetermined | Undetermined |

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | The URL exemption began at the scheme separator, so a network-path reference — an authority with no scheme — read as a path and a line | NETWORK-PATH-AUTHORITY (entry 1, refuted claim) | 1a begin the exemption at the leading pair of slashes as well, still covering only the authority | guard | `check_citations.URL_AUTHORITY`, `test_check_citations.LineCitations.test_a_url_authority_is_not_a_line_citation`, `test_check_citations.LineCitations.test_a_citation_inside_a_url_path_is_still_one` | `plan-implementation` |
| 1 | ″ | ″ | 1b ask `markdown_links` where a destination's authority ends, since it owns the grammar | converge | `check_citations.py`, `markdown_links.py` | declined — see Dispositions |
| 2 | Prose written while repairing something else carried quantities: the completion-boundary paragraph counted the rounds that lost their records, and a registry entry measured how much of `scripts/` its findings reach | COUNTS-IN-THE-NEW-PROSE (entries 2, 3) | 2a say the thing without the number, and in the registry name the paths that matter and the command that enumerates the rest | restate | `AGENTS.md`, `development-knowns.yaml` | document — maintainer-owned |
| 2 | ″ | ″ | 2b re-sweep both governance files for the same shape rather than only the reported sites | restate | `AGENTS.md`, `development-knowns.yaml` | document — maintainer-owned |

## Pattern

Cause 1 is the fourth round in a row on one module's boundaries, and the shape has changed: the
earlier rounds found a rule stated too narrowly, and this one finds a rule that disagrees with the
module that owns the grammar. `markdown_links.local_target` already treats a network-path destination
as one this repository does not own. So the defect is not a missing example — it is two modules
holding different ideas of where a URL's authority starts, which is the shape the repository's own
grammar-ownership rule exists to prevent.

Cause 2 is the fifth instance of prose added during a repair carrying a quantity, and this time the
sweep found two the review had not reported — a proportion in one entry and a pointer at the first
two lines of another's evidence. That is the first time the sweep found more than the round did,
which is what the sweep is for.

## Coupling

- `2a` and `2b` are cumulative: the reported sites are repaired and the sweep is what makes the
  round's answer more than the round's report.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| NETWORK-PATH-AUTHORITY — an authority with no scheme read as a path and a line | 1 | carried, second instance | accept | `1abbb01`, `1a` | Measured on both spellings, exempt side and path side: an authority is exempt with a scheme or without one, and a citation inside either form's path is refused. Restoring the scheme-only rule turns the new case red |
| NETWORK-PATH-AUTHORITY, alternative `1b` | 1 | — | decline | — | `markdown_links.local_target` answers whether a destination is local, not where an authority ends inside arbitrary prose. Exposing that boundary would add a function to the parser's owner for one caller, and the agreement wanted here is about a rule rather than a reading — recorded in the module comment instead |
| COUNTS-IN-THE-NEW-PROSE — the boundary paragraph counted rounds; a registry entry measured its own reach | 2 | carried, fifth instance | accept | `1abbb01`, `2a` and `2b` | Both reported sites reworded, and the re-sweep over both governance files found two more of the same shape. What it left alone is the rule's own prohibition wording, the examples it quotes as forbidden, and quantities with no tree referent |

## Carried forward

The twelve findings, unchanged in disposition and reason: **REF-FRAGMENT-CLOSES-QUOTE**,
**ORDINAL-IN-NEW-MODULE**, **CLOSURE-NAMES-WRONG-SYMBOL**, **VERDICT-SEAM**, **VERDICT-CONTROL**,
**FENCE-HANDREAD**, **BINARY-AS-TEXT**, **INPUT-PATTERN-SPLIT**, **REGISTRY-UNDERREPORT**,
**REGISTRY-NOT-DERIVED**, **LEDGER-TABLE-ROUNDS**, **RAWSCORES-PROVISIONAL**.

Three rounds have opened gates and all three went to `scripts/check_citations.py`, which carries none
of the twelve. The module the rounds keep opening is the one the rounds keep adding to.

## Closed

| Finding | Prior disposition | Code evidence the cause no longer holds | Input evidence |
|---|---|---|---|
| CHECKER-ERROR-STATES, CHECKER-STEM-COLLISION, EXTENSION-RULE-TOO-NARROW | accept | `check_citations.Symbols` holds a mapping or a reason; `check_citations.modules` returns collisions as a value; `check_citations.LINE_CITATION` admits an extension of letters then digits | Gate 3 is pass over the unit that carries all three, which is the closure condition a gate-reviewed unit satisfies — the first time in this range that any finding closes on a gate rather than on an explicit re-verification |
| DIAGNOSTIC-COMPOSES-A-PATH | accept | the diagnostic takes the path from `check_citations.modules` | a claim reports the actual module path in use, verified |
| CITABLE-SET-ASKS-THE-ENVIRONMENT | accept | `check_citations.citable` derives from the pinned interpreter's standard library and the imports under `scripts/` | a claim reports legitimacy no longer depending on the local environment, verified |
| EXTERNAL-SYMBOL-UNCHECKED | decline the check, accept the narrowing | the module docstring and `AGENTS.md` state what is and is not verified; `development-knowns.yaml` carries the gap | a claim reports the boundary stated and registered, verified |
| ROUND-ENDS-WHEN-ASKED | accept | `AGENTS.md` states that a round ends when its record is written; both previously missing rounds have a record and an archive | a claim reports both saved, verified |
| RECONCILED-AGAINST-A-PARTIAL-COPY | accept, fourth instance | the Source line of `docs/dispositions/v0.4.1..64667c6.md` says only what this triage read | a claim reports the record no longer guessing where the received text began, verified. The previous round recorded this closure against an input that disagreed; this round's input agrees |

## Out of scope this round

None.

## Undetermined

**F-11 and F-12**, and the twelve carried findings' membership.

## Recurring

- **NETWORK-PATH-AUTHORITY**, second instance of the URL exemption, and the fourth consecutive round
  finding a boundary in this module. The cause is restated: it is not that a rule was stated too
  narrowly but that **two modules held different readings of one grammar, and only one of them owns
  it.**
- **COUNTS-IN-THE-NEW-PROSE**, fifth instance, all in prose added while repairing something else.

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — eight in Closed, two findings and one declined alternative in Dispositions, twelve in Carried forward, F-11 and F-12 in Undetermined |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — every Reach names files and symbols, checked by `scripts/check_citations.py` |
| This record was written in the turn that repaired | pass |

## What this round leaves standing

- **Twelve carried findings.** Three gated rounds, all over the same module, which carries none of
  them. Retiring the twelve needs a round calibrated over the units that do.
- **F-11 / F-12**, unchanged.
- **An external module's symbols**, unchecked by decision and registered.
- **The input's coverage**, given as a count of the remaining range rather than an enumeration. The
  producer's field, recorded rather than repaired.
