# Disposition Record — `v0.4.1..87c7bb4`

**Source**: `docs/reviews/v0.4.1..87c7bb4.md`, 5 contract rows and 6 claims,
verdict not stated in the copy this triage received
**Scope**: `v0.4.1..87c7bb4`. The input states no coverage sets at all; its Gate Index says no gate
was opened, which is what decides that nothing here can close a gated finding
**Prior round**: `docs/dispositions/v0.4.1..e693b19.md`

The repairs settled here landed at `4029b42`, before this record existed.

**Written late.** This round's findings were about the previous record's own output, so the repairs
went into that record and the round itself never got one — the rule requiring a record per round had
landed one commit earlier. Its successor reported the absence, and `development-knowns.yaml` had set
exactly that as the trigger to re-examine the debt. What the examination found is in the registry
entry; what it means for this file is that it is dated by the round it settles and not by the day it
was written, and that two of its findings sat in the previous record until now.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | not present in the received copy | the Gate Index states no gate opened; nothing in the copy states a verdict, and whether the producer emitted one is not determinable here | not claimed |
| Calibration / Gate Index | not present in the received copy | — | not claimed |
| Finding count | not present in the received copy | 4 VIOLATED contract rows + 2 REFUTED claims, and each refuted claim keys to a contract row, so 4 stable findings | not claimed |
| Coverage | not present in the received copy | no set is named, so no prior finding's membership is decidable from this input | not claimed |
| Non-finding sections | not declared | a Structural Cause paragraph naming the archive defect | not claimed |

Every row reads `not claimed` because the copy began at the Gate Index. That is a statement about
what reached this triage, not about what the producer wrote — the same distinction the previous
record had to be corrected to make, and the reason the archive above exists.

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| ARCHIVE-NOT-AS-RECEIVED | reported as `AC-1` and one refuted claim | inside | current finding → Dispositions |
| COVERAGE-REFERENCE-WENT-STALE | reported as `AC-2`, `AC-3` and one refuted claim | inside | current finding → Dispositions |
| RECONCILED-AGAINST-A-PARTIAL-COPY | reported again as `AC-4` | inside | current finding → Dispositions |
| ROUNDS-UNSETTLED | `AC-5` reports the debt conversion holding | inside | Closed by the previous round; re-verified here |
| The twelve carried findings | no coverage stated, no gate opened | undetermined | Undetermined |

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | An archive of an input claimed a fidelity it did not have, and reformatted the thing whose form was the point | ARCHIVE-NOT-AS-RECEIVED (AC-1) | 1a keep the text in a fenced block, which preserves it and keeps its absolute-path links from being parsed, and state in the file what the archive is not | restate | `docs/reviews/v0.4.1..e693b19.md`, `AGENTS.md` | document — maintainer-owned |
| 2 | A coverage line stated by reference went stale across a round that enumerated nothing, and the triage count taken from it went stale with it | COVERAGE-REFERENCE-WENT-STALE (AC-2, AC-3) | 2a report both as mismatches against the range's own changed-path set, and name the unaccounted path | restate | `docs/dispositions/v0.4.1..e693b19.md` | document — disposition record, maintainer-owned |
| 2 | ″ | ″ | 2b require the producer to enumerate rather than reference | — | `skills/static-review/SKILL.md` | already stated — the producer's field |
| 3 | A record stated what reached the triage as though it stated what the producer wrote | RECONCILED-AGAINST-A-PARTIAL-COPY (AC-4) | 3a say "the copy this triage received", and say in the same breath that whether the producer emitted more is not determinable here | restate | `docs/dispositions/v0.4.1..64667c6.md` | document — disposition record, maintainer-owned |

## Pattern

All three causes are one shape: **a record described its input from what was at hand rather than from
the input.** The archive reformatted what it claimed to preserve; the coverage line trusted a
reference instead of the range; the reconciliation read a partial copy as a complete one. The repair
in each case put the thing to be read into the tree, or said plainly which copy was read.

## Coupling

- `1a` is what makes `3a` checkable by a later round: with the archive kept, "which copy was read" is
  a question with an answer in the tree.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| ARCHIVE-NOT-AS-RECEIVED — the archive claimed verbatim and had reformatted the tables, bolded the header and rewritten the evidence links | 1 | new | accept | `4029b42`, `1a` | The file holds the received text in a fenced block and states what it is not: not byte-guaranteed, because it was transcribed from a message, and not reformatted. The fence is also what lets an absolute-path link and a line citation survive in an archive the workspace gate and the citation check both read |
| COVERAGE-REFERENCE-WENT-STALE — the input's coverage and triage count are one path short of the range | 2 | new | accept for the reconciliation; `2b` already stated | `4029b42`, `2a` | Measured: the range changed 66 paths and the input accounted for 65. The reference points at a list from two rounds back, because the round in between enumerated nothing, so `docs/dispositions/v0.4.1..5fd8c2f.md` is in neither the referenced list nor the named addition. Traced with `comm` over the two ranges' changed-path sets |
| RECONCILED-AGAINST-A-PARTIAL-COPY — a record's integrity table read a partial copy as a complete input | 3 | carried from the previous round | accept | `4029b42` and `a171eaf`, `3a` | The cells name what the producer's record carries and say this triage could not see it. The archive requirement is what makes the distinction checkable rather than asserted |

## Carried forward

The twelve findings, unchanged: **REF-FRAGMENT-CLOSES-QUOTE**, **ORDINAL-IN-NEW-MODULE**,
**CLOSURE-NAMES-WRONG-SYMBOL**, **VERDICT-SEAM**, **VERDICT-CONTROL**, **FENCE-HANDREAD**,
**BINARY-AS-TEXT**, **INPUT-PATTERN-SPLIT**, **REGISTRY-UNDERREPORT**, **REGISTRY-NOT-DERIVED**,
**LEDGER-TABLE-ROUNDS**, **RAWSCORES-PROVISIONAL**.

## Closed

None by this round. `ROUNDS-UNSETTLED` closed in the previous one and `AC-5` re-verifies it, which
changes nothing about its home.

## Out of scope this round

None decidable — the input states no coverage.

## Undetermined

**F-11 and F-12**, and the twelve carried findings' membership, because this input enumerates no
coverage at all. They are carried above on their own reasons regardless.

## Recurring

**RECONCILED-AGAINST-A-PARTIAL-COPY**, second instance. The previous round's repair named the
producer's fields and said the copy was partial; this round showed the same shape one layer out — the
archive built to make the copy checkable was itself not the copy. The cause stands and gains a
clause: **an archive of an input is worth only its fidelity, and a claim of fidelity is a claim like
any other.**

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — the two findings that had been filed in the previous record are here now and struck from there, `RECONCILED-AGAINST-A-PARTIAL-COPY` is here as carried, twelve in Carried forward, F-11 and F-12 in Undetermined |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — `1a`, `2a` and `3a` name the files they touch. None names a line, which is the convention this range adopted after this round |
| This record is dated by the round it settles | stated in the header, along with why it was written late |

## What this round leaves standing

- **Twelve carried findings**, and now their membership is undetermined as well, because this input
  enumerated no coverage.
- **F-11 / F-12**, unchanged.
- **The archive's residual limit**: it is a transcription of a message, not a byte copy, and no
  practice available here changes that. What it gives a later round is a text to check against.
