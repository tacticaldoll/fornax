## Disposition Record

**Source**: `docs/reviews/939e637..014887b.md`
**Scope**: `939e637..014887b`, derived with `git diff --name-only`; the changed files are `AGENTS.md`,
`README.md`, `docs/dispositions/8d92ff7..1e37d86.md`, `scripts/check_agent_skills.py`,
`scripts/check_workspace.py`, `scripts/round_chain.py`, `scripts/tests/test_check_agent_skills.py`,
and `scripts/tests/test_round_chain.py`
**Prior round**: `docs/dispositions/8d92ff7..d3e0eb3.md`

### Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | FAIL at Gate 2 + CONTRACT-VIOLATED | Gate 2 is the lowest failed gate and two Against-Contract rows are VIOLATED | pass |
| Calibration / Gate Index | Gates 1-8 | every gate carries a status; those above Gate 2 are blocked by it | pass |
| Finding count | 9 | the Gate 2 rows and the Structural Causes rows together come to that many; the non-gated tracks re-report three of them and the record says so | pass |
| Coverage | partial, with four enumerated sets | `AGENTS.md` and `docs/dispositions/8d92ff7..1e37d86.md` are changed by this range and appear in no set, while the record's `unread` set says none | mismatch |
| Non-finding sections | the Ledger declares its rows are not findings | the section declares that status and contains no violation claim | pass |

The input's own fitness, recorded here rather than as a finding: the Review Record states that its
author is the author of the code it reviews, which is not the fresh context `AGENTS.md` asks of a
round. That is a ceiling on this round's confidence, not a defect a later section can repair.

### Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| `REVIEW-EXEMPTION-NARROWER-THAN-ITS-OBJECT` | `AGENTS.md` is among this round's changed files | inside | closure candidate → Carried forward |
| the accepted ids of `docs/dispositions/d12997d..7c88454.md` | their recorded cause locations are `test_module_claims`, the `check_citations.modules` docstring, `docs/guards.md`, `docs/dispositions/8dc34ca..ba81ab1.md`, `docs/dispositions/d54813e..8c87363.md`, `test_read_whole`, `test_shell_script` and `test_validate_skills`, none of them among this round's changed files | outside | Out of scope this round |
| the ids of `docs/dispositions/ebf16c7..48ce457.md`, and `SCHEMA-STOPS-SHORT-OF-THE-MAP` | their units likewise lie outside this round's changed files | outside | Out of scope this round |
| `BUILDER-COMMAND-NOT-ON-PATH`, `PROFILE-CONSTRAINTS-UNGUARDED`, `GATE-PRESENCE-UNGUARDED` | closed by `docs/dispositions/1e37d86..605462f.md` and re-reviewed without recurrence by `docs/dispositions/8d92ff7..d3e0eb3.md` | inside | Closed remains Closed |

The ids of `docs/dispositions/8dc34ca..ba81ab1.md` and `docs/dispositions/ba81ab1..1a84428.md` were
read during this triage: each has a home in the later rounds of that chain, so none of them is
awaiting placement here. An earlier informal triage in the same session recorded a self-check failure
for exactly these two tables, and reading them is what settles it.

### Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---:|---|---|---|---|---|---|
| 1 | The second-reading exception is written by hand in prose in more than one place, with the code as one more, and nothing derives any of them from another — so they drifted apart inside one range | DOCSTRING-REFUTED-BY-ITS-OWN-SIBLING, PROSE-DOES-NOT-DESCRIBE-THE-CHECK, CORRECTION-NOTE-OVERSTATES-THE-CHECK | 1a state in `AGENTS.md` what the check enforces — a second reading sits outside the chain, and either record of a twice-read round identifies it — and correct the other two statements to match | `restate` | `AGENTS.md` Testing Strategy, `round_chain.neighbours` docstring, the Correction paragraph of `docs/dispositions/8d92ff7..1e37d86.md` | document repair for the prose, maintainer-owned; `plan-implementation` for the docstring |
| 1 | " | " | 1b have the check emit its own rule statement into a generated block, so the prose cannot drift from it | `derive` | `scripts/round_chain.py`, `scripts/generated_block.py`, `AGENTS.md`, `scripts/tests/test_round_chain.py` | `plan-implementation` |
| 2 | `round_chain.check` treats an unreadable branch as terminal and returns before reporting what it has already found, so a record it could not read disappears | FAILURES-DISCARDED-ON-AN-EARLIER-RETURN | 2a report the accumulated failures before the early return | `guard` | `round_chain.check`, `scripts/tests/test_round_chain.py` | `plan-implementation` |
| 3 | One spelling carries two absences: a revision that resolves to nothing and a revision the branch does not hold answer alike, and the caller reports both as the second | PLACE-COLLAPSES-TWO-ABSENCES | 3a give the absence variants a caller can tell apart | `distinguish` | `round_chain.History`, `round_chain.check`, `scripts/tests/test_round_chain.py` | `plan-implementation` |
| 4 | Round identity is compared as written text while the resolver that owns it already exists and is already fed | BASE-RESOLVED-BUT-NEVER-READ, PRIOR-FIELD-COMPARES-ONLY-A-BASENAME | 4a compare resolved object names rather than written revisions, and compare a record's whole path rather than its last segment | `index` | `round_chain.read_history`, `round_chain.names_for`, `round_chain.prior_field`, `scripts/tests/test_round_chain.py` | `plan-implementation` |
| 4 | " | " | 4b drop the base resolution if identity stays textual | `converge` | `round_chain.read_history` | `plan-implementation` |
| 5 | `round_chain.read_history` hands caller-derived text to git without the separator that ends option parsing, so a name opening with a hyphen is read as an option | REVISION-READ-AS-AN-OPTION | 5a pass git's end-of-options separator before the revision | `guard` | `round_chain.read_history`, `scripts/tests/test_round_chain.py` | `plan-implementation` |
| 6 | A docstring spells a total of the records this tree holds | TOTAL-SPELLED-AS-A-WORD-IN-A-DOCSTRING | 6a say it without the total | `restate` | `round_chain.neighbours` docstring | `plan-implementation` |

One input row carries two defects: the Structural Causes row about the Correction paragraph reports
both a reference to a unit absent at that commit and an overstatement of what the check does. They
violate different contracts, so they are triaged as separate identities; the overstatement joins
cause 1 and the reference is dispositioned below.

### Pattern

Causes 1 and 4 hold one shape: **a fact whose owner already exists is restated by hand beside that
owner instead of read from it.** The rule is restated in prose beside the code that enforces it;
identity is compared as text beside the resolver that owns it. `AGENTS.md` has named this shape for
written counts, for a record's scope, and for the gate step list. The pattern-level repair is 1b —
let the check produce the prose. Correcting the statements one at a time leaves the shape, and this
range is the demonstration that it can grow back inside a single turn.

### Coupling

`4b` is voided by `4a`. `1b` voids the `AGENTS.md` half of `1a`, which is a hand alignment of what
`1b` derives. `2a`, `3a`, `5a` and `6a` are independent.

### Dispositions

| Finding | Cause | Carried | Disposition | Reason (REQUIRED for decline and defer) |
|---|---|---|---|---|
| DOCSTRING-REFUTED-BY-ITS-OWN-SIBLING | 1 | new | accept | — |
| PROSE-DOES-NOT-DESCRIBE-THE-CHECK | 1 | new | accept | — |
| CORRECTION-NOTE-OVERSTATES-THE-CHECK | 1 | new | accept | — |
| FAILURES-DISCARDED-ON-AN-EARLIER-RETURN | 2 | new | accept | — |
| PLACE-COLLAPSES-TWO-ABSENCES | 3 | new | accept | — |
| BASE-RESOLVED-BUT-NEVER-READ | 4 | new | accept | — |
| PRIOR-FIELD-COMPARES-ONLY-A-BASENAME | 4 | new | accept | — |
| REVISION-READ-AS-AN-OPTION | 5 | new | accept | — |
| TOTAL-SPELLED-AS-A-WORD-IN-A-DOCSTRING | 6 | new | accept | — |
| RECORD-CITES-A-UNIT-THAT-DOES-NOT-EXIST-YET | ungrouped | new | decline | The reference holds at HEAD and at every checkout but one. Making it hold at each commit means landing the check before the record it depends on, and that commit would fail its own gate — the half-broken middle the plan avoided deliberately. Nothing reads this reference, so the other ordering buys a real red gate for a reference no reader consults |

### Carried forward

| Finding | Disposition | Original reason, unchanged | Closure condition not established |
|---|---|---|---|
| `REVIEW-EXEMPTION-NARROWER-THAN-ITS-OBJECT` | defer | the repair edits `AGENTS.md`'s governance prose and has no effect while `check_citations.RECORDS` names only `docs/dispositions`; now when a round proposes adding `docs/reviews` to that constant | Neither condition holds. `AGENTS.md` is inside this round's scope but `unread` — no gate opened over it, so its silence closes nothing — and `check_citations.RECORDS` is unchanged, so the reopening condition has not fired either |

### Closed

`BUILDER-COMMAND-NOT-ON-PATH`, `PROFILE-CONSTRAINTS-UNGUARDED` and `GATE-PRESENCE-UNGUARDED` remain
Closed. This round produces no evidence against those closures.

### Out of scope this round

Their cause locations lie outside this round's changed files, so this round makes no claim about
whether they still hold: `BOUNDARY-BLIND-TO-MARKERS`, `EVERY-MODULE-CLAIM-NOW-FALSE`,
`COLLISION-ROW-NAMES-THE-WRONG-REPAIR`, `HEADINGS-DENY-THEIR-OWN-ROWS`,
`CITATION-COST-OFF-BY-ITS-OWN-SENTENCE`, `DISCLOSURE-NAMES-THE-WRONG-ROUND`,
`DISCLOSURE-TOOK-THE-CLOSING-SENTENCES`, `OLD-TYPE-NAME-IN-TWO-DOCSTRINGS`,
`KEYWORD-ONLY-NOT-WHAT-WAS-RECORDED`, `CLASS-COUNT-IN-A-MESSAGE`, `CASE-COUNT-IN-A-CONTRACT`,
`TWO-CAUSES-ONE-COMMIT`, `SCHEMA-STOPS-SHORT-OF-THE-MAP`, and the ids of
`docs/dispositions/ebf16c7..48ce457.md` that `docs/dispositions/8dc34ca..ba81ab1.md` names one by
one.

### Undetermined

`none`; the input enumerates what it read.

### Recurring

`none`; no finding here returned after every location in a recorded repair's Reach had changed.

### Ungrouped

`RECORD-CITES-A-UNIT-THAT-DOES-NOT-EXIST-YET`, declined above and grouped under no cause.

### Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — `REVIEW-EXEMPTION-NARROWER-THAN-ITS-OBJECT` is Carried forward, the builder findings remain Closed, and the rest are named one by one under Out of scope this round |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — causes 1 through 6 each carry one, written as a file and the unit inside it rather than as a line number |
