# Disposition Record — `v0.4.1..ef8b53c`

**Source**: `docs/reviews/v0.4.1..ef8b53c.md`, 10 findings, FAIL at Gate 3 + CONTRACT-VIOLATED +
CLAIM-REFUTED
**Scope**: `v0.4.1..ef8b53c`. Coverage is partial with all four sets named:
`partially-gate-reviewed` holds `scripts/check_citations.py` at Gates 1–3 with Gate 4 blocked,
`triage-only` holds the rest of the range, `gate-reviewed` and `unread` are empty
**Prior round**: `docs/dispositions/v0.4.1..3901827.md`

The repairs settled here land above `ef8b53c`, in `bf76e7f..HEAD`, and this record is written in the
same turn as those repairs — which is the completion boundary the previous round's finding produced.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | FAIL at Gate 3 + CONTRACT-VIOLATED + CLAIM-REFUTED | Gate 3 is fail, Gate 4 blocked, 5–8 not inspected; contract rows are violated and claims refuted | pass |
| Calibration / Gate Index | Gates 1–8 | Gates 1–4 carry statuses and 5–8 are `not inspected`, over the one partially-gate-reviewed unit. The declaration is wider than the pass, and the Coverage line says which unit and which gates, so nothing here is left to be inferred | pass |
| Finding count | 10 | 1 Gate 3 row + 7 violated contract rows + 3 refuted claims. The refuted claims key to `AC-1`, `AC-2` and `AC-7`, so 8 rows are stable findings and the stated count reaches 10 by counting refuted claims separately | mismatch |
| Coverage | partial, four sets named, one unit's gates stated | the sets are named; `triage-only` is given as a count of the remaining range rather than an enumeration, which is the shape an earlier round reported and is reported again here | mismatch |
| Non-finding sections | not declared | a Structural Causes list whose entries each name an existing finding | not claimed |

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| CHECKER-ERROR-STATES | `AC-8` reports the split holding; a Gate 3 row reports its diagnostic naming a composed path | inside | partly Closed, partly current finding → Dispositions |
| CHECKER-STEM-COLLISION | `AC-8` reports it holding | inside | Closed |
| CITATION-FORM-STILL-PARTIAL | reported again as `AC-2` and `AC-3` | inside | current finding → Dispositions, and Recurring |
| CITATION-TYPO-BELOW-THRESHOLD | `AC-4` reports the replacement asks the environment | inside | current finding → Dispositions, and Recurring |
| COUNTS-IN-THE-NEW-PROSE | reported again as `AC-5` | inside | current finding → Dispositions, and Recurring |
| RECONCILED-AGAINST-A-PARTIAL-COPY | reported again as `AC-6` | inside | Closed |
| ROUND-ENDS-WHEN-ASKED | reported as `AC-7` | inside | current finding → Dispositions |
| The twelve carried findings | the input says they remain without a gated review | undetermined | Undetermined |

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | A diagnostic composed the path of the file it names instead of naming the path the module map already holds | DIAGNOSTIC-COMPOSES-A-PATH (Gate 3) | 1a take the path from the map | restate | `check_citations.citations`, `test_check_citations.ModuleIdentity.test_an_unparseable_module_is_named_where_it_actually_sits` | `plan-implementation` |
| 2 | The URL exemption covered the whole whitespace-bounded word, so a citation in a URL's path escaped with the port it was written for | URL-EXEMPTION-TOO-WIDE (AC-2) | 2a exempt the authority alone, from the scheme separator to the next slash | guard | `check_citations.URL_AUTHORITY`, `test_check_citations.LineCitations.test_a_citation_inside_a_url_path_is_still_one` | `plan-implementation` |
| 3 | The extension rule admitted letters only | EXTENSION-RULE-TOO-NARROW (AC-3) | 3a admit letters followed by digits, keeping the alphabetic first character that stops a version reading as a citation | guard | `check_citations.LINE_CITATION`, `test_check_citations.LineCitations.test_a_long_extension_is_still_a_line_citation` | `plan-implementation` |
| 4 | Whether a module name is legitimate was asked of the interpreter running the check rather than of anything this repository declares | CITABLE-SET-ASKS-THE-ENVIRONMENT (AC-4) | 4a derive it — the standard library set the pinned interpreter carries, plus the modules some file under `scripts/` imports | derive | `check_citations.citable`, `check_citations.citations`, `test_check_citations.SymbolCitations.test_a_module_the_standard_library_or_this_tree_accounts_for_passes` | `plan-implementation` |
| 5 | The checker's claim was wider than the checker: an external module's symbol is never read | EXTERNAL-SYMBOL-UNCHECKED (AC-1) | 5a narrow the claim to what is checked and register the gap with its weighing | declare | `AGENTS.md`, `check_citations.py`, `development-knowns.yaml` | document — maintainer-owned |
| 5 | ″ | ″ | 5b parse the standard library's source from the interpreter path | guard | `check_citations.py` | declined — see Dispositions |
| 5 | ″ | ″ | 5c import each cited module and read its attributes | guard | `check_citations.py` | declined — see Dispositions |
| 6 | A governance sentence measured how much of a convention sits inside fingerprinted sections | COUNTS-IN-THE-NEW-PROSE (AC-5) | 6a say what changing it would reach, without a quantity | restate | `AGENTS.md` | document — maintainer-owned |
| 7 | A round's record was offered as a follow-up rather than written, so the next input arrived first | ROUND-ENDS-WHEN-ASKED (AC-7) | 7a state that a round ends when its record is written and that the record belongs to the turn that repairs | declare | `AGENTS.md`, `development-knowns.yaml` | document — maintainer-owned |

## Pattern

Causes 2, 3 and 4 are the third consecutive round of the same shape in the same module: **a boundary
was defended at the examples the round named, and the next round named different examples.** The
first repair widened a pattern; the second widened it again and added a heuristic; this one replaces
the heuristic with a derivation and narrows two exemptions. What changed the trajectory is not
another widening — it is that `citable` now derives its answer from declarations rather than probing,
so there is nothing left for the next round to find a different example of.

Cause 5 is the same shape refused. The reviewer's falsifier was real and the repair was declined:
reading an external symbol needs source this repository does not hold, no such citation has ever been
wrong here, and the reading would reintroduce the environment dependence cause 4 removes. The claim
moved instead of the code, which is what the weighing rule asks for when a check would cost more than
the defect.

Cause 7 is the record-persistence lapse by a second mechanism, one round after the first was
examined. The first examination found where the repairs landed; this one finds when a round ends.

## Coupling

- `4a` supersedes the previous round's `4a`. Recorded in both records rather than quietly replaced.
- `5a` voids neither `5b` nor `5c`; both are declined on their own terms and `5a` is what makes the
  refusal legible.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| DIAGNOSTIC-COMPOSES-A-PATH — an unparseable module under `scripts/tests/` was reported at a path that does not exist | 1 | new | accept | `bf76e7f`, `1a` | The control needed writing first: the existing case put the module where the composed path happened to be right, so the probe passed. With a nested module, restoring the composition turns it red |
| URL-EXEMPTION-TOO-WIDE — a citation inside a URL's path escaped | 2 | new | accept | `bf76e7f`, `2a` | Measured: the authority is exempt and the path is not. Widening the exemption back turns the new case red |
| EXTENSION-RULE-TOO-NARROW — a numbered extension passed | 3 | new | accept | `bf76e7f`, `3a` | Measured on a long extension and a numbered one; a version and a column still pass. Restoring the letters-only rule turns two subtests red |
| CITABLE-SET-ASKS-THE-ENVIRONMENT — the answer depended on what was installed locally | 4 | carried, third instance | accept | `bf76e7f`, `4a` | The set is the pinned interpreter's standard library plus what `scripts/` imports, read from the import statements. Measured: a stdlib name passes, an imported third-party name passes, one nothing here imports is reported, and misspellings at three distances are reported. Adding a name to the derivation turns the third-party case red |
| EXTERNAL-SYMBOL-UNCHECKED — a symbol an external module does not define reads as verified | 5 | new | decline the check, accept the narrowing | `bf76e7f`, `5a` | The docstring and the rule now say the checker verifies a citation into this tree to its symbol and a citation naming anything else to the module. `development-knowns.yaml` carries the gap, its two declined repairs and the trigger |
| EXTERNAL-SYMBOL-UNCHECKED, alternative `5b` | 5 | — | decline | — | It covers the standard-library half of these citations and leaves the packaged half unchecked, so the boundary still needs stating; the reading is bought and the statement is still owed |
| EXTERNAL-SYMBOL-UNCHECKED, alternative `5c` | 5 | — | decline | — | It runs third-party code inside a maintenance check |
| COUNTS-IN-THE-NEW-PROSE — a governance sentence measured a convention's extent | 6 | carried, fourth instance | accept | `bf76e7f`, `6a` | The sentence says what changing the convention would reach. Two earlier repairs replaced one quantity word with a vaguer one; this one has none |
| ROUND-ENDS-WHEN-ASKED — the record was offered rather than written, twice running | 7 | new | accept | `bf76e7f`, `7a` | `AGENTS.md` states the boundary, the registry carries the second examination beside the first, and its trigger now fires on a third mechanism rather than on a third occurrence. This record and the previous one were both written before this report |

## Carried forward

The twelve findings, unchanged in disposition and reason: **REF-FRAGMENT-CLOSES-QUOTE**,
**ORDINAL-IN-NEW-MODULE**, **CLOSURE-NAMES-WRONG-SYMBOL**, **VERDICT-SEAM**, **VERDICT-CONTROL**,
**FENCE-HANDREAD**, **BINARY-AS-TEXT**, **INPUT-PATTERN-SPLIT**, **REGISTRY-UNDERREPORT**,
**REGISTRY-NOT-DERIVED**, **LEDGER-TABLE-ROUNDS**, **RAWSCORES-PROVISIONAL**.

Two rounds have now opened gates, both over `scripts/check_citations.py`, which carries none of the
twelve.

## Closed

| Finding | Prior disposition | Code evidence the cause no longer holds | Input evidence |
|---|---|---|---|
| CHECKER-STEM-COLLISION | accept, `2a` of the previous round | `check_citations.modules` returns the collisions as a value and `check_citations.check` reports them | `AC-8` reports duplicate stems, parse failures and obvious internal misspellings all reported |
| CHECKER-ERROR-STATES | accept, `1a` of the previous round | `check_citations.Symbols` holds a mapping or a reason, never both | a claim row reports missing, unreadable and syntax-error separated, verified. The diagnostic's composed path is a separate finding above, in the same function |
| RECONCILED-AGAINST-A-PARTIAL-COPY | accept, `6a` of the previous round | the Source line of `docs/dispositions/v0.4.1..64667c6.md` says only that this triage read a copy carrying no header fields, and that why is not determinable from this end | `AC-6` reports the old assertion still present, against a state the repair had already changed — the fourth report of a wording repaired three times. Closed on the code, with the input's disagreement recorded here rather than resolved silently |

## Out of scope this round

None. The input's coverage names the whole range.

## Undetermined

**F-11 and F-12**, and the twelve carried findings' membership: the input states they remain without
a gated review, which is a statement about their status rather than an enumeration placing them.

## Recurring

- **CITABLE-SET-ASKS-THE-ENVIRONMENT**, third instance of one cause across three rounds: a
  terminator-style guess, then a similarity threshold, then an environment probe. Each repair
  replaced the mechanism and kept the shape — *something outside the declaration decides*. The
  derivation is the first version with nothing outside the declaration in it.
- **COUNTS-IN-THE-NEW-PROSE**, fourth instance, all in prose added while repairing something else.

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — three in Closed, seven rows in Dispositions covering five findings and two declined alternatives, twelve in Carried forward, F-11 and F-12 in Undetermined. CHECKER-ERROR-STATES appears in Closed for the split and in Dispositions for a different defect in the same function, which are different contracts in one unit |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — every Reach names files and symbols, checked by `scripts/check_citations.py` rather than by a reading |
| This record was written in the turn that repaired | pass — stated in the header, and it is the finding this round produced |

## What this round leaves standing

- **Twelve carried findings**, unchanged. Two rounds have opened gates and both went to the same
  module, which carries none of them.
- **F-11 / F-12**, unchanged.
- **An external module's symbols**, unchecked by decision, registered with the trigger that reopens
  it.
- **The input's own coverage**, given as a count of the remaining range rather than an enumeration.
  That is the producer's field and is recorded as a mismatch, not repaired here.
