# Disposition Record — `9162b15..d8ed52f`

Produced by `triage-findings` from the `static-review` Review Record for `d8ed52f`, 2026-08-25.
Kept here because `AGENTS.md` Testing Strategy now requires it and names this path, so the next
round can be handed the prior one.

The full record is in the round's working notes; what this file carries is the disposition per
cause and how each was settled, which is what a later round reads.

## Causes and how each was settled

| Cause | Findings | Disposition | Settled by | Verified how |
|---|---|---|---|---|
| 1 two bindings for one constant, different patch semantics | F-01, F-05 | accept | `325a8ae` | Patching either constant now changes what the fixture writes; before, patching `HOST_VERSION_MANIFESTS` changed nothing |
| 2 the fixture emptied with the registry it was patched against | F-06 | accept | `6b2896c` | Test passes with the correct guard, fails with the reverted one |
| 3 a record whose parent covers the whole tree | F-09 | accept | `f137233` | Both new tests fail with the guard removed |
| 4 unreachable `path == claimed` disjunct | F-02 | accept | absorbed by Cause 7's rewrite | The predicate it named no longer exists; the record's coupling table predicted this |
| 5 module docstring described the replaced mechanism | F-03 | accept | `925e712`, with Cause 7 | The record notes 5a cannot land before whatever changes the mechanism |
| 6 unreflowed bullet | F-04 | accept | `f96819b` | Paragraph measured line by line after the edit, which had reproduced the defect once |
| 7 naming the directory masked its registered children | F-07 | accept | `925e712` | Old and new rules returned the same path on the cited fixture, measured; the new tests fail when the rule is reverted to naming directories |
| 8 repo-tree assertion lost its positive anchor | F-08 | accept | `f34f107` | A `return []` body fails the new test. Narrower than reported: four existing unit tests already caught it, so the suite was not blind — that one test was |
| 9 the review obligation had two homes and asserted its own singleness | F-10 | accept | `f96819b` | Release now points at the statement; the Authoring Workflow commits before deferring to it |
| 10 the round, not the cause, is the unit of a commit | F-11, F-12 | **open** | handed to `audit-governance` | The record flagged `10b` as needing that audit first: the section reads as judgment, so encoding it as a gate would convert a guideline into a refusal |
| 11 the noqa measurement lived only in a commit message | F-13 | accept | `222625c` | `PGH004`/`RUF100` selected and probed: neither catches the malformed-suffix form, so the comment says so and the open case is registered as `malformed-noqa-suppresses-silently` |
| 12 the review was required and the disposition was not | F-14 | accept | `f96819b` | This file is the requirement's first output |
| 13 one diagnostic served two omissions | F-15 | accept | absorbed by Cause 7's rewrite | The diagnostic names a file now, and "register the scenario it belongs to, or delete it" holds for both cases — an unregistered scenario registers, a stray file is deleted. Verified against both |

F-12 (a 76-character subject) stays `defer` for the reason the record gives: it is a consequence of
Cause 10, and the commit is already published.

## What the round leaves standing

- **Cause 10** — open, pending the governance audit.
- Three patterns the record names above the individual causes: a fixture that passes either way
  (Causes 2, 7, 8 — all three settled), prose claiming more than its mechanism (Causes 5, 9, 11, 13),
  and the round rather than the finding as the unit of work (Causes 10, 12 — 12 settled).
