# Disposition Record — `v0.4.1..e693b19`

**Source**: `docs/reviews/v0.4.1..e693b19.md`, the Review Record for `v0.4.1..e693b19` as this
triage received it, 6 findings, PASS + CONTRACT-VIOLATED + CLAIM-REFUTED
**Scope**: `v0.4.1..e693b19`. Coverage is `triage-only` over the previous round's enumerated set
plus `docs/dispositions/v0.4.1..64667c6.md`, with `gate-reviewed`, `partially-gate-reviewed` and
`unread` all empty
**Prior round**: `docs/dispositions/v0.4.1..64667c6.md`

The repair settled here lands above `e693b19`, at `a171eaf`.

This is the first disposition whose Source names a file rather than a paste, which is the repair
below.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | PASS + CONTRACT-VIOLATED + CLAIM-REFUTED | every gate `not inspected`, and the index states this round ran no gated review able to close a carried finding | pass |
| Calibration / Gate Index | Gates 1-8 | none opened; the index says so | mismatch, stated by the input |
| Finding count | 6 | 3 VIOLATED contract rows + 3 REFUTED claim rows, and each refuted claim keys to one of the contract rows. So 6 rows and 3 stable findings | pass |
| Coverage | partial, four sets, `triage-only` stated as the previous set plus one path | the reference resolves to the set the previous record wrote out; the added path exists. Stating it by reference is the shape a previous round reported, and it is reported again here rather than passed | mismatch |
| Non-finding sections | not declared | a Structural Cause paragraph naming the three findings as one cause | not claimed |

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| The Record integrity table of `v0.4.1..64667c6.md` | re-reported as `AC-1`, `AC-2`, `AC-3` and three refuted claims | inside | current finding → Dispositions |
| ROUNDS-UNSETTLED | `AC-4` reports the debt conversion holding | inside | Closed |
| The twelve carried findings | no gate opened; the input says so in its Gate Index | inside | closure candidate → Carried forward |
| F-11, F-12 | no commit object is enumerated | undetermined | Undetermined |

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | A disposition reconciles its input's header fields against a copy that reaches it by paste, and cannot tell a field the producer omitted from one that did not survive the trip — so it records the first | RECONCILED-AGAINST-A-PARTIAL-COPY (AC-1, AC-3, claims 1 and 3) | 1a persist the Review Record under `docs/reviews/<range>.md` and name it from the disposition's Source, so the reconciliation is checkable against its input | declare | `AGENTS.md:490`, `docs/reviews/v0.4.1..e693b19.md`, `docs/dispositions/v0.4.1..64667c6.md:11` | document — `AGENTS.md`, maintainer-owned |
| 1 | ″ | ″ | 1b have the triage state which fields it could not see instead of reconciling them as absent | restate | `docs/dispositions/v0.4.1..64667c6.md:11` | document — disposition record, maintainer-owned |
| 2 | A record's own Source line and its Record integrity table stated different finding counts, which no input can make true | SELF-CONTRADICTED-COUNT (AC-2, claim 2) | 2a reconcile the table against the Source line as well as against the input | restate | `docs/dispositions/v0.4.1..64667c6.md:3`, `docs/dispositions/v0.4.1..64667c6.md:15` | document — disposition record, maintainer-owned |

## Pattern

Cause 1 is the shape these records have been naming all along, arriving in the record rather than in
the code: **a claim was written from what was at hand rather than from the thing it names.** The
closure cells named symbols from the session instead of the file; this table named header fields
from a paste instead of the record. Both were checkable and neither was checked.

What is different is the repair. The symbol case was fixed by reading the file, which works only
because the file was there to read. Here the thing to read did not exist in the tree, so the repair
had to put it there. That is also, in retrospect, what the lost rounds were: no text to read.

Cause 2 needs no such repair. A record whose Source and whose reconciliation table disagree is
refutable from itself, and the check for it is reading the record whole before committing it.

## Coupling

- `1a` and `1b` are cumulative, not alternatives. `1a` gives the next round something to check
  against; `1b` is what an honest triage does when it is handed a partial copy anyway, which stays
  possible.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| RECONCILED-AGAINST-A-PARTIAL-COPY — three header fields recorded as absent that the producer had emitted | 1 | new | accept | `a171eaf`, `1a` and `1b` | The three cells now state what the producer's record carries and that this triage could not see it. `AGENTS.md:490` requires the Review Record persisted; `docs/reviews/v0.4.1..e693b19.md` is the first kept under it, and this record's Source names a file. What I cannot settle is whether the header was emitted and lost in transit or never emitted, and the cells say that rather than choosing |
| SELF-CONTRADICTED-COUNT — the Source line said one finding while the table said none was stated | 2 | new | accept | `a171eaf`, `2a` | The row now reads the input's count and says the contradiction stood whatever the input claimed. This one needed no new evidence: the record refuted itself |

## Carried forward

The twelve findings, unchanged in disposition and reason: **REF-FRAGMENT-CLOSES-QUOTE**,
**ORDINAL-IN-NEW-MODULE**, **CLOSURE-NAMES-WRONG-SYMBOL**, **VERDICT-SEAM**, **VERDICT-CONTROL**,
**FENCE-HANDREAD**, **BINARY-AS-TEXT**, **INPUT-PATTERN-SPLIT**, **REGISTRY-UNDERREPORT**,
**REGISTRY-NOT-DERIVED**, **LEDGER-TABLE-ROUNDS**, **RAWSCORES-PROVISIONAL**.

## Closed

| Finding | Prior disposition | Code evidence the cause no longer holds | Input evidence |
|---|---|---|---|
| ROUNDS-UNSETTLED | accept, `1a` of the prior round | `AGENTS.md:499` binds the requirement to each round as it happens and says why a later reconstruction is not a record; `development-knowns.yaml:100` carries the gap as accepted debt with a reconsider-when that fires if a round's record is lost again | `AC-4` checked both that the rule binds only the round at hand and that the recurrence trigger was kept, and reports it holding |

## Out of scope this round

None.

## Undetermined

**F-11 and F-12**, unchanged.

## Recurring

None. Both new findings are first instances in their units, though cause 1 is a fresh instance of
the pattern above.

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — the previous record's integrity table appears here as two new findings in Dispositions, ROUNDS-UNSETTLED in Closed, twelve in Carried forward, F-11 and F-12 in Undetermined |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — `1a`, `1b` and `2a` name `file:line`, each read back from the file. `1a` also names a whole file, the Review Record it requires, because that repair is the file |
| Every reconciliation row above was read from the file this record's Source names | pass — the row that fails this is the finding this record settles, and the Source now names a file rather than a paste |

## What this round leaves standing

- **Twelve carried findings**, unchanged. Still the whole of the standing ledger, and still waiting
  on one gated round over the units that carry them.
- **F-11 / F-12**, waiting on a governance decision nobody has asked for.
- **The coverage-by-reference shape**, reported again this round in the input's own Coverage line and
  recorded above as a mismatch rather than repaired: it is the producer's field, not this record's.
