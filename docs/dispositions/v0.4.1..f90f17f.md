## Disposition Record

**Source**: `docs/reviews/v0.4.1..f90f17f.md`
**Scope**: `v0.4.1..f90f17f`, derived with `git diff --name-only`; the input enumerates its coverage
as every path that derivation yields, which is fixed for a fixed range and decides membership
**Prior round**: `docs/dispositions/939e637..014887b.md`

### Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | PASS | every gate is `not inspected` and the gate-reviewed set is empty, so no opened gate and no gate-reviewed unit exists for a pass to be about; `static-review` records a Green unit as `triage-only` and says its gates are not to be claimed passed | mismatch |
| Calibration / Gate Index | Gates 1-7, for shared validation tooling | the index records none of those gates as opened, so the calibration was declared and not performed | mismatch |
| Finding count | 2 | two Against-Contract rows are VIOLATED, and the Structural Causes rows explain those same two rather than adding any | pass |
| Coverage | partial, with four enumerated sets | the sets are enumerated by a derivation from a fixed range, which decides membership; every path is `triage-only` and the other sets are empty | pass |
| Non-finding sections | the record states Gate 5 did not open, so it produces no Responsibility & Dependency Ledger | the Ledger is required where that gate opened, and it did not | pass |

One observation the table has no row for. The Triage line reports every path Green with no check
failing, and nothing in the record evidences what those checks ran over; the Verdict rests on it
entirely. Nothing in the record contradicts it, so it is not a mismatch — but it is what a later
round should ask the producer, and it is why the Verdict above is reconciled against the Gate Index
rather than against it.

### Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| the accepted ids of `docs/dispositions/939e637..014887b.md` | their units are in `scripts/round_chain.py`, `AGENTS.md` and `docs/dispositions/8d92ff7..1e37d86.md`, all inside the derived path set | inside | closure candidate → Carried forward |
| `RECORD-CITES-A-UNIT-THAT-DOES-NOT-EXIST-YET` | same | inside | declined, disposition and reason kept |
| `REVIEW-EXEMPTION-NARROWER-THAN-ITS-OBJECT` | `AGENTS.md` and `scripts/check_citations.py` are inside the derived path set | inside | closure candidate → Carried forward |
| every id the prior round placed Out of scope | measured against the derived path set: `scripts/tests/test_module_claims.py`, `docs/guards.md`, `docs/dispositions/8dc34ca..ba81ab1.md`, `docs/dispositions/d54813e..8c87363.md`, `scripts/tests/test_read_whole.py`, `scripts/tests/test_shell_script.py`, `scripts/tests/test_validate_skills.py`, `scripts/check_citations.py` and `scripts/shell_script.py` are each inside it | inside | closure candidate → Carried forward |
| `BUILDER-COMMAND-NOT-ON-PATH`, `PROFILE-CONSTRAINTS-UNGUARDED`, `GATE-PRESENCE-UNGUARDED` | inside, and nothing in the input contradicts their closure | inside | Closed remains Closed |

This is the shape of the round and it belongs in the record. A range this wide pulls every prior
finding back inside scope, and a review that opens no gate can close none of them. Ids that were
resting in `Out of scope this round`, which is a decision, are now resting in `Carried forward`,
which is the absence of one. The ledger grew and no id moved. That is a property of the input's
calibration, not of the tree.

### Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---:|---|---|---|---|---|---|
| 1 | `shell_script.commands` decides continuation from a line's trailing backslash before deciding whether that backslash sits inside a comment, and the comment test it does run recognises only a comment occupying the whole line | COMMANDS-JOIN-AN-INLINE-COMMENT | 1a refuse a multi-line script carrying an unquoted hash, the conservative route the input proposes | `forbid` | `scripts/shell_script.py` + `shell_script.commands` and the module docstring's measured claim, `scripts/tests/test_shell_script.py`, `docs/guards.md` | `plan-implementation` |
| 1 | " | " | 1b stop joining when the line being continued carries an unquoted hash ahead of its trailing backslash — nearer to what bash does, and it refuses fewer scripts that are legitimate | `guard` | same | `plan-implementation` |

The cause was verified against the code rather than taken from the input. Measured: bash runs `echo
ok` and then the following command, while `shell_script.commands` returns the two lines joined into
one.

Also measured, which bounds the repair's urgency without excusing it: the join always leaves the
hash as its own word, so `read_whole.shell_words` returns an unread text and the caller reports it.
Variants that would fail silently were tried and are not divergences, because a hash inside a word
is not a comment to bash either. The safety direction holds; what fails is the claim.

### Sent back for review, not triaged here

The `shell_script` module docstring states it was measured against bash over a corpus and that
nothing is read as fewer commands than bash runs. The falsifier behind the accepted finding refutes
that sentence. This is a defect the review did not report, and adding it is not this workflow's to
do — it is named so the next review reaches it, and cause 1's repairs touch it in Reach whether or
not a later round keys it as a finding of its own.

### Pattern

`none`. One cause is not a shape.

### Coupling

`1b` voids `1a`: they are two answers to one question and only one can land.

### Dispositions

| Finding | Cause | Carried | Disposition | Reason (REQUIRED for decline and defer) |
|---|---|---|---|---|
| COMMANDS-JOIN-AN-INLINE-COMMENT | 1 | new | accept | — |
| REVIEW-RULE-VIOLATED-BY-REGISTERED-DEBT | ungrouped | new | decline | `AGENTS.md` states that a round recorded once as accepted debt in `development-knowns.yaml` is not a standing violation of that rule, and that reporting such rounds every round costs a finding and repairs nothing. The input cites the registry entry itself and labels it already-registered debt, so the exclusion applies on the input's own evidence; the contract flag is carried by the other finding alone |
| REVISION-READ-AS-AN-OPTION | ungrouped | carried | decline, changed from accept | New evidence the prior round did not have, recorded in `docs/guards.md` under its dated 2026-09-15 section: `git rev-parse --verify --quiet` answers exit 1 with no output for a hyphen-leading revision, and answers identically with the end-of-options separator added. The repair changes nothing observable and no control can distinguish it |

### Carried forward

Every id below lies inside this round's derived scope and was not re-reported. None can be closed:
the input gate-reviewed nothing, and silence over a `triage-only` unit is never closure. Each keeps
the disposition and the reason its own round recorded.

| Group | Disposition | Closure condition not established |
|---|---|---|
| the accepted ids of `docs/dispositions/939e637..014887b.md` — DOCSTRING-REFUTED-BY-ITS-OWN-SIBLING, PROSE-DOES-NOT-DESCRIBE-THE-CHECK, CORRECTION-NOTE-OVERSTATES-THE-CHECK, FAILURES-DISCARDED-ON-AN-EARLIER-RETURN, PLACE-COLLAPSES-TWO-ABSENCES, BASE-RESOLVED-BUT-NEVER-READ, PRIOR-FIELD-COMPARES-ONLY-A-BASENAME, TOTAL-SPELLED-AS-A-WORD-IN-A-DOCSTRING | accept | The first only. Their repairs landed and `docs/guards.md` records several measured by revert, so the code condition is met — but this round opened no gate over the units, and closing on a ledger the input never read would be closing on this triage's own knowledge rather than on its input |
| `RECORD-CITES-A-UNIT-THAT-DOES-NOT-EXIST-YET` | decline | kept as declined; its reason is unchanged and needs no closure |
| `REVIEW-EXEMPTION-NARROWER-THAN-ITS-OBJECT` | defer | Neither. No gate opened over `AGENTS.md`, and `check_citations.RECORDS` still names only the dispositions directory, so the reopening condition has not fired |
| every id the prior round placed Out of scope — the accepted ids of `docs/dispositions/d12997d..7c88454.md`, `SCHEMA-STOPS-SHORT-OF-THE-MAP`, and the ids of `docs/dispositions/ebf16c7..48ce457.md` that `docs/dispositions/8dc34ca..ba81ab1.md` names one by one | as each round recorded | The first. Measured: every cause location those rounds named is inside this round's derived path set, so they are no longer decidably outside — but all of it is `triage-only`, so nothing about them was read |

### Closed

`BUILDER-COMMAND-NOT-ON-PATH`, `PROFILE-CONSTRAINTS-UNGUARDED` and `GATE-PRESENCE-UNGUARDED` remain
Closed. A scope change does not reopen a closure, and this round produces no evidence against one.

### Out of scope this round

`none`. The range covers every path any prior finding names.

### Undetermined

`none`. The input enumerates its coverage by a derivation from a fixed range.

### Recurring

`none`. No finding here returned after every location in a recorded repair's Reach had changed.

### Ungrouped

`REVIEW-RULE-VIOLATED-BY-REGISTERED-DEBT` and `REVISION-READ-AS-AN-OPTION`, both declined above and
grouped under no cause.

### Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — the builder findings remain Closed, `REVISION-READ-AS-AN-OPTION` is re-dispositioned in Dispositions on named new evidence, and every other prior id sits in Carried forward under exactly one group |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — cause 1 carries two, each written as a file and the unit inside it |
