# Disposition Record — `v0.4.1..5fd8c2f`

**Source**: a `static-review` Review Record for `v0.4.1..5fd8c2f`, 4 findings,
PASS + CONTRACT-VIOLATED + CLAIM-REFUTED
**Scope**: `v0.4.1..5fd8c2f`. The input enumerates its `triage-only` set in full and states
`gate-reviewed`, `partially-gate-reviewed` and `unread` as empty. The set is every path the range
changed, and it is the previous round's resolved set plus `docs/dispositions/v0.4.1..bd5127f.md`
**Prior round**: `docs/dispositions/v0.4.1..bd5127f.md`

The repairs settled here land above `5fd8c2f`, at `76a8cf9`.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | PASS + CONTRACT-VIOLATED + CLAIM-REFUTED | every gate `not inspected`, and the index says in its own words that this round cannot close a gated finding — the first round to state that rather than leave it to be reconciled | pass |
| Calibration / Gate Index | Gates 1-8 | no gate is opened. The declaration is still wider than the pass, but the index now says what the pass can and cannot settle, which is what the mismatch was reporting | mismatch, narrowed |
| Finding count | 4 | 3 VIOLATED contract rows + 1 REFUTED claim row, and that claim keys to the second contract row. So 4 rows and 3 stable findings | pass |
| Coverage | partial, four sets enumerated | the `triage-only` list is written out; the other three are stated empty. This is the enumeration the previous round referenced rather than wrote | pass |
| Non-finding sections | not declared | a Structural Causes table whose rows each name an existing finding | not claimed |

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| COUNT-IN-COMMITS | `AC-4` re-reads the cited messages under the amended rule and reports it holds | inside | Closed |
| ROUNDS-UNSETTLED | re-reported as `AC-3` | inside | current finding → Dispositions |
| Every other prior finding | in the enumerated coverage, no gate opened, not re-reported | inside | closure candidate → Carried forward |
| F-11, F-12 | the coverage enumerates no commit object | undetermined | Undetermined |

COUNT-IN-COMMITS is the one closure this round supports, and it is worth being exact about why,
because the previous two rounds closed nothing. Its unit is the rule text and the commit prose it
governs, both of which this round read; the input did not merely fall silent but re-read the cited
messages against the amended clause and reported the clause satisfied. That is the third closure
condition — the input explicitly verifies the repair — which the closure test accepts in place of a
gate-reviewed unit. No other carried finding has that.

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | The paragraph amending the count rule counted history, in permanent governance prose, while amending the rule that forbids it | RULE-COUNTS-ROUNDS (AC-1) | 1a say it without the number and name where such a number belongs — the dated records | restate | `AGENTS.md` | document — `AGENTS.md`, maintainer-owned |
| 2 | A resolvable reference to a prior round's coverage was read as this round's enumeration, and passed in Record integrity | COVERAGE-BY-REFERENCE (AC-2, claim 1) | 2a resolve the reference into the record, and report the input's coverage as a mismatch rather than a pass | restate | `docs/dispositions/v0.4.1..bd5127f.md` | document — disposition record, maintainer-owned |
| 2 | ″ | ″ | 2b send the record back to the producer for an enumeration | `return to producer (gap)` | not enumerable — the producing round is finished | declined — see Dispositions |
| 3 | Four review rounds have no persisted record and their texts are gone | ROUNDS-UNSETTLED (AC-3) | 3a persist a record per round from here on | restate | `docs/dispositions/v0.4.1..5fd8c2f.md` | document — maintainer-owned |

## Pattern

Both new findings are one shape, and it is the shape the previous record named for governance text:
**the amendment is written in the same act as the thing it amends, and inherits its defect.** The
paragraph narrowing the count rule counted; the record reporting a coverage contract accepted a
coverage that did not meet it.

What is new is that the previous round's pattern-level repair — run the admission rule over
governance text, not only over checks — worked, and this round says so: `AC-4` holds, and
COUNT-IN-COMMITS closes after recurring in round after round. The clause that could not be satisfied
stopped generating findings once it was cut where the rest of the rule cuts. That is one closure
against two new instances of the same shape, which is the honest ratio to report.

## Coupling

- `2a` voids `2b`.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| RULE-COUNTS-ROUNDS — the amending paragraph counts rounds | 1 | new | accept | `76a8cf9`, `1a` | The sentence now says "round after round" and names the dated records as where the number belongs. Read back from the file |
| COVERAGE-BY-REFERENCE — a reference read as an enumeration and passed | 2 | new | accept | `76a8cf9`, `2a` | The Coverage row reads mismatch and says the record made that mistake; the resolved set is written out and is every path the range changed. No disposition rested on it — nothing had closed |
| COVERAGE-BY-REFERENCE, alternative `2b` | 2 | — | decline | — | The producing round is finished; a record that waits for its input to be reissued settles nothing, and the resolution is available here |
| ROUNDS-UNSETTLED — four rounds have no record | 3 | carried | accept in part | `3a`, this file | The fourth consecutive round settled in the round it belongs to. The lost rounds stay lost |

## Carried forward

Every prior finding not re-reported and not closed, unchanged in disposition and reason:
**REF-FRAGMENT-CLOSES-QUOTE**, **ORDINAL-IN-NEW-MODULE**, **CLOSURE-NAMES-WRONG-SYMBOL**,
**VERDICT-SEAM**, **VERDICT-CONTROL**, **FENCE-HANDREAD**, **BINARY-AS-TEXT**,
**INPUT-PATTERN-SPLIT**, **REGISTRY-UNDERREPORT**, **REGISTRY-NOT-DERIVED**,
**LEDGER-TABLE-ROUNDS**, **RAWSCORES-PROVISIONAL**.

Each is `accept` and repaired, and each waits on the same thing: a round that opens the gate which
reported it, over the unit that carries it. Four rounds have now passed without one.

## Closed

| Finding | Prior disposition | Code evidence the cause no longer holds | Input evidence |
|---|---|---|---|
| COUNT-IN-COMMITS | accept, `1a` of the prior round | `AGENTS.md` cuts the clause at staleness: a number counting what a commit did is not the rule's subject, a measurement of the tree is and stays forbidden, as does transcribing a message's number into durable prose | `AC-4` re-read the previously cited messages under the amended clause and reports it holds — an explicit verification of the repair, not silence |

## Out of scope this round

None.

## Undetermined

**F-11 and F-12**, unchanged. No round has enumerated a commit object as a reviewed unit, and the
governance decision they wait on has not been asked for.

## Recurring

None new. COUNT-IN-COMMITS was recurring in the prior record and closes here.

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — COUNT-IN-COMMITS in Closed, ROUNDS-UNSETTLED in Dispositions, twelve findings in Carried forward, F-11 and F-12 in Undetermined |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — `1a` and `2a` name `file:line`, each read back from the file. `3a` names this file, a whole-file Reach because the repair is the file |

## What this round leaves standing

- **Twelve carried findings.** They are repaired and none can be closed by a round that opens no
  gate. This is now the largest standing item in the ledger, and it is a property of how the rounds
  are being run rather than of the code: a `triage-only` pass over an unchanged tree can find new
  prose defects and can never retire an old gated one.
- **F-11 / F-12**, still waiting on a governance decision nobody has asked for.
- **The four lost rounds**, permanently.
