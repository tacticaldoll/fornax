# Disposition Record — `v0.4.1..1609403`, second reading

**Source**: `docs/reviews/v0.4.1..1609403.second-reading.md`, 5 findings,
PASS + CONTRACT-VIOLATED + CLAIM-REFUTED
**Scope**: `v0.4.1..1609403`. A range this repository has already settled once, in
`docs/dispositions/v0.4.1..1609403.md`. This is a second reading of it, arriving after two later
rounds had landed, so some of what it reports was repaired by commits that postdate the tree it read
**Prior round**: `docs/dispositions/v0.4.1..de72982.md`, settled in the same turn as this one

The repairs settled here land at `b165753`.

**Why this file exists beside the other one.** A round is a review and its settlement, and two
reviews of one range are two rounds. Naming this record for the range alone would overwrite the first
reading's; naming it for a later HEAD would claim it read a tree it did not. So it carries the range
it read plus what it is.

**What a second reading of a settled range can and cannot establish.** It can falsify a claim the
first reading did not test — which is what it did. It cannot close a finding on the tree, because the
tree has moved: two of its rows report text that later commits changed, and this record says which
rather than crediting or blaming the reading for it.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | PASS + CONTRACT-VIOLATED + CLAIM-REFUTED | Gates 1–4 pass over the one partially-gate-reviewed module, 5–8 not inspected; two contract rows violated and two claims refuted | pass |
| Calibration / Gate Index | Gates 1–8 | Gates 1–4 carry statuses, 5–8 are `not inspected`. The declaration is wider than the pass and the index says so | pass |
| Finding count | 5, stated as 2 contract violations, 2 duplicate refuted claims and 1 partial claim | 2 violated rows + 2 refuted claims that key to them + 1 partial. So 2 stable findings, and the input says which of its rows duplicate which | pass |
| Coverage | partial, four sets, `triage-only` enumerated file by file | the enumeration is written out rather than counted, which is the shape earlier rounds were asked for | pass |
| Non-finding sections | not declared | the Structural Causes rows each name an existing finding | not claimed |
| Range already settled | not stated | the input does not say the range carries a record, and reads it as a first pass. Recorded here rather than as a defect: nothing in the record contract asks a producer to check for a prior settlement of its own range | mismatch |

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| GUARDS-INCOMPLETE | `AC-3` reports the clause demanding a guard per accepted finding while the ledger records findings with none | inside | current finding → Dispositions, and Recurring |
| GUARD-NOT-A-SYMBOL | `AC-4` reports the `VERDICT-CONTROL` row's cross-row reference | inside | current finding → Dispositions, shared with the round settled beside this one |
| NETWORK-PATH-AUTHORITY | `AC-1` and claim 1 report the repair holding | inside | Closed |
| COUNTS-IN-THE-NEW-PROSE | `AC-2` and claim 2 report both sites removed | inside | Closed |
| ROUND-ENDS-WHEN-ASKED | `AC-6` and claim 3 report both records saved in the repair turn | inside | Closed |
| The prose-unit closure arm | `AC-5` probes each closure condition and reports the arm holding | inside | Closed |

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | The guards clause states its demand as a guard per accepted finding and takes it back four sentences later, so the clause's scope and the ledger's contents disagree in reading order | GUARDS-CLAUSE-SELF-CONTRADICTS (AC-3, claim 4) | 1a state it once, in the sentence that states it: a row for every finding, naming the revert and the red test, or saying nothing can turn red and why | declare | `AGENTS.md` | document — maintainer-owned |
| 1 | ″ | ″ | 1b narrow the ledger to only the findings a test can guard | — | `docs/guards.md` | declined — see Dispositions |
| 2 | A ledger row named its revert by pointing at the row above it | ROW-NOT-SELF-CONTAINED (AC-4, claim 5) | 2a name the revert in the row | restate | `docs/guards.md` | document — maintainer-owned |

## Pattern

Both findings are the guards contract disagreeing with the guards file, which is the same family the
round settled beside this one reports three instances of. What this reading adds is the **reading
order**: the clause was amended to admit findings nothing can guard, and the amendment was placed
after the demand it qualifies. A reader meeting the demand first reads a contract the file violates,
and that is what this review did.

Claim 6 is the finding that is not a finding, and it is the most useful row in the input: the
repository holds result statements for its mutation runs and a static reading cannot verify a
historical execution. That is true, it is not repairable, and it is now stated in the ledger's own
preamble rather than left as an implied guarantee.

## Coupling

- `1a` voids `1b`.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| GUARDS-CLAUSE-SELF-CONTRADICTS — the demand and its exception four sentences apart | 1 | carried, second instance | accept | `b165753`, `1a` | The clause says it once now, and both kinds are named as rows in the same sentence. The earlier amendment is what this reading falsified: it added the exception without moving the demand |
| GUARDS-CLAUSE-SELF-CONTRADICTS, alternative `1b` | 1 | — | decline | — | Narrowing the ledger would drop the rows that say a repair has no guard, which is the information a later round most needs — a finding with no row reads as an omission and a finding with a "no guard, and why" row reads as a decision |
| ROW-NOT-SELF-CONTAINED — a row naming its revert by pointing at another | 2 | new here, shared | accept | `b165753`, `2a` | The row names the revert. Reported by both reviews this turn, against different HEADs; settled once |

## Carried forward

None.

## Closed

| Finding | Prior disposition | Code evidence the cause no longer holds | Input evidence |
|---|---|---|---|
| NETWORK-PATH-AUTHORITY | accept | `check_citations.URL_AUTHORITY` matches the scheme form and the network-path form, and exempts the authority alone | `AC-1` probes both the authority and a citation in the path and reports the rule holding; claim 1 verified |
| COUNTS-IN-THE-NEW-PROSE | accept | neither the completion-boundary paragraph nor the import-grouping entry carries a quantity | `AC-2` re-read both sites; claim 2 verified |
| ROUND-ENDS-WHEN-ASKED | accept | both records for the round in question exist | `AC-6` looked for both; claim 3 verified |
| The prose-unit closure arm | accept | `skills/triage-findings/SKILL.md` states the three conditions and the prose arm | `AC-5` removed each condition in turn and reports the arm holding |

## Out of scope this round

None. The input's enumerated coverage names every file in the range it read.

## Undetermined

**F-11 and F-12**, unchanged.

## Recurring

**GUARDS-CLAUSE-SELF-CONTRADICTS**, second instance of the guards clause disagreeing with the guards
file. The first repair added the exception; this one moves it into the sentence that states the
demand. The cause as it now stands: **an amendment placed after the clause it qualifies leaves the
clause reading as it did.**

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — four in Closed, two in Dispositions, F-11 and F-12 in Undetermined. `ROW-NOT-SELF-CONTAINED` appears in the record settled beside this one as well, and both say it was reported twice and settled once, which is the shared-finding case rather than a duplicate home |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — both name the file they touch; neither has a symbol to name, since both units are prose |
| This record was written in the turn that repaired | pass |

## What this round leaves standing

- **F-11 / F-12**, unchanged.
- **That a mutation result in the tree is not verifiable by reading it.** The input says so as a
  partial claim and it is right. The ledger's preamble now states it rather than implying otherwise,
  and names the two ways a run here has produced a wrong number.
- **A gap in the record contract**: nothing asks a producer to check whether the range it is
  reviewing already carries a Disposition Record. This reading did not know, and its report is
  sound anyway; recorded because a third reading of one range would be harder to place.
