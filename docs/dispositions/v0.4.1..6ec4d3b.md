# Disposition Record — `v0.4.1..6ec4d3b`

**Source**: `docs/reviews/v0.4.1..6ec4d3b.md`, 5 findings, FAIL at Gate 1 + CONTRACT-VIOLATED
**Scope**: `v0.4.1..6ec4d3b`, derived with `git diff --name-only`. The input states a focus rather
than an enumeration, which the Coverage row below reconciles
**Prior round**: `docs/dispositions/v0.4.1..6ff702e.md`

An input from another producer, and the first record in this range settling one. Finding 1 was
repaired at `0dc1076` before this record was written; the rest land in the commit carrying it.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | FAIL at Gate 1 + CONTRACT-VIOLATED | Gate 1 is recorded `fail` and the verdict names it. But the flag has nothing behind it: `CONTRACT-VIOLATED` is raised by the Against-Contract track, which composes one row per clause with a falsifier attempted, and this record carries no such table. Its four contract readings are filed as Gate 6 findings instead — and a gate finding is gated, which is what the next row is about | mismatch |
| Calibration / Gate Index | Gates 1-8 | eight gates carry a status, so the shape matches the calibration. The statuses do not cohere: Gate 6 is `fail` while Gates 2-5 are `blocked`. `blocked` at 2-5 says no reviewed file cleared Gate 1; `fail` at 6 says at least one did and was carried five gates higher. Both cannot hold. Measured: the four records carrying the Gate 6 findings have no trailing whitespace, so their Gate 1 passed and Gates 2-5 opened for them and owe a status | mismatch |
| Finding count | 5 | five finding rows. Findings 3 and 4 are one defect at two instances and the input does not declare them as keying to each other, which is a declaration this repository asks of its own producer and cannot ask of another | pass |
| Coverage | partial, stated as a focus on the `6ff702e..HEAD` repairs and the prior round's carry-forward units | not four enumerated sets. This is decidable and it costs something concrete: without an enumeration a prior disposition's membership is `undetermined` rather than inside or outside, and the previous record emptied Undetermined for the first time in this range. Rather than refill it, membership below is decided against the input's finding locations plus this round's own reading, and each row says which — an enumeration the consumer supplied, recorded as a mismatch of the input and not as a pass | mismatch |
| Non-finding sections | not declared | the Structural Cause section is a paragraph naming the four Gate 6 findings as one cause, and the Verification section carries claims rather than findings. Neither declares its status, which is the concession this state exists for: a record from another producer has nothing to reconcile here | not claimed |

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| ABSENT-CLAIM-AS-MISMATCH | finding 2 locates a Record integrity row convicting on an `Input claim` of "not stated" | re-reported | current finding → Dispositions, carried, recurring |
| PARAGRAPH-SPLIT-NOT-REFLOWED | the input ran `git diff --check` over `6ff702e..HEAD` and reports the whitespace class; paragraph reflow in `AGENTS.md` is not among its locations | outside | Out of scope this round |
| LIFECYCLE-OMITS-THE-NAMED-PRIOR, LEDGER-TABLE-ROUNDS, RAWSCORES-PROVISIONAL | none of their units appears in the input's locations, and the input enumerates no coverage set that would decide them | outside | Out of scope this round |
| FIXTURE-READ-AS-PRODUCT, COVERAGE-ENUMERATION-ABSENT, REVIEW-COVERAGE-AS-FINDING | declined; not re-reported, and no new evidence offered | outside | Out of scope this round |
| F-11, F-12 | their unit is commit prose, located by the prior round; the input's locations name no commit object | outside | Out of scope this round |

Membership is decided against the input's finding locations rather than against a coverage
enumeration, because it states none. Every row above says so. This is narrower than a coverage set
would allow — a location list cannot show that a unit was inspected and found clean — so no prior
disposition closes this round, and none is recorded as `undetermined` either, because the locations
do decide the ones above.

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | Nothing in this repository sees a trailing space, in any file. `check_text._hygiene` reads every tracked file and asks two questions of it — whether it holds a NUL and whether it ends with a newline — and Ruff holds a Python line's width and not its end | TRAILING-WHITESPACE-UNSEEN (input finding 1) | 1a remove it, and sweep the class rather than the site | `restate` | `development-knowns.yaml`, the `malformed-noqa-suppresses-silently` evidence clause about the 103-character probe; `skills/resolve-deadlock/SKILL.md`, the "Identify the Forcing Function" bullet | **taken at `0dc1076`** |
| 1 | " | " | 1b add a check for it | `guard` | a new check, its test, `check_workspace.STEPS`, `README.md`'s generated block | **not taken** — weighed below |
| 2 | A Record integrity row can convict on a claim the input never made, because nothing ties the row's Result to whether its `Input claim` cell names a claim at all | ABSENT-CLAIM-AS-MISMATCH (input finding 2) | 2a move the row out and keep its observation unscored | `restate` | `docs/dispositions/v0.4.1..1609403.second-reading.md`, the `Range already settled` row | **taken** |
| 3 | A row's key is chosen by the author, so a proposition that belongs to a declared check can be filed under an invented label instead | INTEGRITY-ROW-DUPLICATES-A-DECLARED-CHECK (input finding 5) | 3a merge it into the check that already claimed it | `converge` | `docs/dispositions/v0.4.1..e693b19.md`, the `Triage` row and the `Coverage` row's reconciled evidence | **taken** |
| 4 | A fact about the record being written can be filed in the table that audits the input, because the two tables' subjects are stated in prose and enforced by nothing | SELFCHECK-FOLDED-INTO-THE-INPUT-AUDIT (input findings 3 and 4) | 4a move both to prose beside the table, marked as facts about the record | `restate` | `docs/dispositions/v0.4.1..61789c2.md` and `docs/dispositions/v0.4.1..de72982.md`, each one's `Probe disclosure` row | **taken** |
| 5 | The condition for building the derived check keys on a row's Result, which is the weakest of the three axes a row varies along, so it both under- and over-fires | TRIGGER-KEYS-ON-THE-WEAKEST-AXIS | 5a restate it as a row whose `Input claim` cell does not name a claim the input made about itself | `restate` | `docs/dispositions/c60a6ba..af1185a.md`, the Weighing section's trigger sentence and the "Repair `1c` unbuilt" bullet; `docs/guards.md`, the `ABSENT-CLAIM-AS-MISMATCH` row's trigger clause | **taken** |
| 6 | Two tables' subjects, their key sets, and the rule that an absent claim forces `not claimed` are all stated in prose and enforced by nothing, so every defect above is a defect of authoring | causes 2, 3, 4 and 5 | 6a give the record's two tables declared disjoint subjects, closed key sets, and an invariant forcing `not_claimed` where the claim is absent — the schema `7b` was already routed to, now with the axes it must encode | `derive` | a new registry and generator, its test, `check_workspace.STEPS`, `README.md`'s generated block, `check_citations.SUBJECTS`, `docs/guards.md`, and `AGENTS.md`'s clause about keeping the repair guards there | **`design-boundaries` next** — carried from cause 7 of the prior round with the axes added |
| 7 | A review that has measured a violation can file it as a verified prior claim instead of as a live finding, because the prior round's disposition of it reads as settling the review's scope | REVIEW-FILED-A-VIOLATION-AS-A-VERIFIED-CLAIM | 7a record the distinction where the review's own claim sits | `restate` | `docs/reviews/v0.4.1..6ff702e.md`'s Claims Verified entry for the `1b` sweep, and this record | **taken** |

### Weighing repair 1b against the rule for adding a check

**What it would have caught**: this, and one other line — the class is two, swept by measuring every
tracked file. The other is in `skills/resolve-deadlock/SKILL.md` and predates `v0.4.1`, so it
shipped in a released skill; that is the furthest an instance of this class has travelled, and it
still reached no user in any way a user could observe.

**What it costs**: less than most candidates here. Both instances are a single trailing space, not
the two that make a CommonMark hard break, so no legitimate use exists in this tree and the check
needs **no exemption list** — the property that disqualified the count pre-filter and the
outcome-shape check. Against that: the failure never left the workspace, and the same turn that
repaired the defect would be buying the gate step. That is the reasoning `2b` was refused under one
round ago, and refusing this one differently would make the rule turn on who found the defect.

**Reconsider when** a third instance appears, or when one is found in a published release rather
than merely in a shipped file.

## Pattern

**Every cause above except the first is one shape: a table whose constraints are prose.** The
contract states five keys for Record integrity, states that `not claimed` is mandatory where no
claim exists, and states in two places that Record integrity audits the input while Self-check
audits the record. `AGENTS.md` states the subject separation a third time, with its cost. Nothing
enforces any of it, and the four rows this round repaired fail those constraints in three different
ways — which is why the input's own reading, that they share "沒有結構機制限制為契約定義的五列",
is right about the mechanism and short about the defect. Row count is one projection of one
constraint.

That is what makes cause 6 different from a repeat of the prior round's `7b`. The prior round routed
a registry for the guards ledger and justified it by uniqueness and derived tallies. This round
supplies what the schema has to encode: **the subject is a field, not a convention.** A row that
cannot say whose conduct it judges is how a self-check ends up convicting a producer, which is the
one defect here that reached `origin/main` in a record that read as settled.

## Coupling

- `1b` is independent of `1a` and not taken.
- `2a`, `3a`, `4a` and `5a` are independent of each other; `5a` is deliberately **first** in the
  commit order, because repairing the four rows under the old trigger would leave the next
  `pass`-scoring row unseen.
- `6a` is voided by nothing and blocked by nothing; it subsumes `1c` and `7b` of the prior rounds.

## Dispositions

| Finding | Cause | Carried | Disposition | Reason (REQUIRED for decline and defer) |
|---|---|---|---|---|
| TRAILING-WHITESPACE-UNSEEN — a trailing space passed the whole gate, in a class nothing here reads | 1 | new | accept | — |
| ABSENT-CLAIM-AS-MISMATCH — a Record integrity row scored `mismatch` on an `Input claim` of "not stated", while its own evidence cell said the contract asks no such thing | 2 | carried | accept | — |
| INTEGRITY-ROW-DUPLICATES-A-DECLARED-CHECK — a sound reconciliation was filed under an invented key whose declared home already claimed it | 3 | new | accept | — |
| SELFCHECK-FOLDED-INTO-THE-INPUT-AUDIT — a fact about the record being written sat in the table that audits the input, twice | 4 | new | accept | — |
| TRIGGER-KEYS-ON-THE-WEAKEST-AXIS — the condition for building the derived check tests a row's Result where the defect is in its subject | 5 | new | accept | — |
| REVIEW-FILED-A-VIOLATION-AS-A-VERIFIED-CLAIM — a review held the sweep result and recorded it as a verified prior claim rather than as a live contract violation | 7 | new | accept | — |

Six findings against the input's five. Findings 3 and 4 of the input are one identity, which the
input does not declare; and two identities are added that the input did not report — the trigger,
and the previous review's misfiling. Neither is a code defect the review missed, which would be sent
back for review: the first is a condition this repository wrote and this round found wrong while
acting on the input, and the second is a defect in a record, which is the input's own class.

**Where this round disagrees with its input, and it changes the repair.** The input gives findings 3
and 4 the violation "額外的 Probe disclosure 列違反固定表格形狀". Read that way, all four rows are
one defect and all four repairs are the same removal. They are not. `Range already settled` convicts
on an absent claim, `Triage` carries a true proposition under a wrong key, and the two
`Probe disclosure` rows put the wrong subject in the table — and the repairs are a removal, a merge,
and a move. Merging is not optional there: deleting the `Triage` row would have destroyed a correct
reconciliation, which the row-count reading gives no reason to preserve. The input's verdict on all
four holds; its stated reason for two of them does not, and the reason is what picks the repair.

## Carried forward

`none`. Every prior disposition not re-reported is Out of scope this round, because the input
enumerates no coverage set and its finding locations decide membership only for the ids named below.

## Closed

`none`. A location list is not a coverage enumeration: it can show a unit was inspected and found
defective, and cannot show one was inspected and found clean, so closure condition 1 is unavailable
for every carried id this round.

## Out of scope this round

**PARAGRAPH-SPLIT-NOT-REFLOWED, LIFECYCLE-OMITS-THE-NAMED-PRIOR, LEDGER-TABLE-ROUNDS,
RAWSCORES-PROVISIONAL, FIXTURE-READ-AS-PRODUCT, COVERAGE-ENUMERATION-ABSENT,
REVIEW-COVERAGE-AS-FINDING, F-11, F-12.** None appears among the input's finding locations. The
three declined ones keep their dispositions and reasons. F-11 and F-12 were located by the prior
round as commit prose and placed here for the same reason; this record's first draft dropped them
from every section, and its own Self-check is what caught that.

## Undetermined

`none`, and deliberately so despite the input's Coverage mismatch. This section exists as a
concession to a record that cannot support cross-round state, and an unenumerated coverage is
exactly that case — but the input's finding locations decide the seven ids above without it, so
recording them as undecidable would overstate what this round could not do. The previous record
emptied this section for the first time in this range and this one keeps it empty.

## Recurring

**ABSENT-CLAIM-AS-MISMATCH**, third instance. The prior repair's Reach was the rows of two records;
both changed, and the finding came back in a third record. Under this skill's own test that means
the stated cause was wrong rather than the Reach short — and this round has the cause: not a row to
remove, and not a table to count, but that nothing ties a row's Result to whether its `Input claim`
cell names a claim. Cause 2 states it that way, and cause 6 is what would enforce it.

## Ungrouped

`none`.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass, on the second answer. Counted from the previous record's own tables: it left ten ids open — eight in Carried forward and two, F-11 and F-12, in Out of scope this round. This record places one of them, `ABSENT-CLAIM-AS-MISMATCH`, in Dispositions as re-reported, and the remaining nine in Out of scope this round. One plus nine is ten. Carried forward, Closed and Undetermined are empty and each says why. The first answer counted only the eight and left F-11 and F-12 in no section at all, which is a hand-counted lifecycle tally going wrong for the third time in this range — the defect `CARRIED-TALLY-DISAGREES` reports, met again by the round settling the trigger for the schema that would remove it |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — causes 1, 2, 3, 4, 5 and 7 each carry a taken repair whose Reach names a file plus a symbol or a quoted phrase. Cause 6 is routed to `design-boundaries` and its Reach names an unbuilt registry and generator, which is such a phrase |
