## Disposition Record

**Source**: `docs/reviews/d54813e..8c87363.md`, 16 reported findings, FAIL at Gate 1 + CONTRACT-VIOLATED + CLAIM-REFUTED. Run in fresh context by a reviewer that did not author the range, and not purely statically: it cloned the range into an isolated copy and took six revert measurements there, which is the first input here to carry runtime evidence of its own
**Scope**: `d54813e..8c87363`, derived with `git diff --name-only` — eleven files, twelve commits
**Prior round**: `docs/dispositions/ba81ab1..1a84428.md`

### Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | FAIL at Gate 1 + CONTRACT-VIOLATED + CLAIM-REFUTED | the index records Gate 1 `fail` as the lowest failing gate; seven VIOLATED rows and the refuted claims answer for the two suffixes | pass |
| Calibration / Gate Index | Gates 1–7 | every gate's status recorded, the file that closed each ladder named, and Gate 8's absence reasoned | pass |
| Finding count | 16, itemised as Gate 1 one, Gate 2 four, Gate 5 two, Against-Contract seven, Structural Causes two | the itemisation holds. Against-Contract carries ten VIOLATED rows of which three duplicate gate findings and one is declared settled remainder, leaving seven; Structural Causes carries seven rows of which five name findings and two do not | pass |
| Coverage | partial, four unit sets enumerated | each partial unit names the gate that closed its ladder; `triage-only` and `unread` both empty and stated so | pass |
| Non-finding sections | the `Findings` field excludes duplicate-axis rows, the Ledger, and `holds`/`verified` rows | every excluded class is named and each duplicate row states the finding it duplicates. Three Gate 1 observations are excluded on grounds the field does not enumerate — a registered known, an existing minority style, and a pre-existing condition — and each row states its ground | pass |

The input carried runtime evidence of its own — an isolated clone of the range, six revert
measurements, both gates and `ruff` run there. There is no Record integrity row for that, because
the contract declares a closed set of checks and this is not one of them; it is recorded here
instead. The one finding this settlement re-measured before accepting agreed with the input.

### Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| the sixteen ids of `ba81ab1..1a84428` | four enumerated unit sets covering every file the repairs touched | inside | fifteen closure candidates → Closed; one deferred id stays deferred |

### Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach | Route |
|---|---|---|---|---|---|---|
| 1 | The boundary's only mechanism found its two sides by two different searches, so they were not complements and a module could belong to neither | BOUNDARY-SET-NOT-THE-OWNERS | 1a split the owner's whole-tree map by path | `converge` | `test_module_claims.PACKAGE_MODULES` | **landed** |
| 2 | A type moved and its old module kept re-exporting it, so it has two names and nothing says which is the one | LINE-RE-EXPORTED-UNDECLARED | 2a route the four readers to the type's own module | `converge` | `runtime_contract.run_commands`, `test_read_whole` at its `words` helper, `test_shell_script` at its `words` helper, `scripts/shell_script.py` at its import | `plan-implementation` |
| 2 | " | " | 2b record in `shell_script`'s prose which readers the alias is kept for, in the form `skill_model` already uses for its bindings | `restate` | `scripts/shell_script.py` module docstring | document is that module's own docstring |
| 3 | A measurement was written into a table, which ends a table, and under a heading that denies measurement | MEASUREMENT-BROKE-THE-TABLE, MEASUREMENT-UNDER-A-PROSPECTIVE-HEADING | 3a give it a dated section of its own naming the commit | `restate` | `docs/guards.md`, at "The undeclared-directory check, measured" | **landed** |
| 4 | A count of this tree was written into a commit message as the sole reason for a decision, and no reading reproduces it | TREE-COUNT-IN-A-MESSAGE | 4a record the reading in a dated section with the corpus it was taken over, and say the message's number is superseded | `restate` | `docs/guards.md` | document is `docs/guards.md`. The message itself is history and is not edited |
| 5 | An earlier round's measured reading was overwritten in place instead of superseded from the current section | SETTLED-READING-OVERWRITTEN | 5a restore the original reading and put the change in the current section, which is the form this file already demonstrates | `restate` | `docs/guards.md`, at the `FROZEN-CLAIM-OVER-A-MUTABLE-DICT` row | document is `docs/guards.md` |
| 6 | Two edits to a settled record disclosed themselves and two did not | SELFCHECK-EDIT-UNDISCLOSED | 6a extend the existing disclosure to cover the Self-check cell and the guard coordinates | `restate` | `docs/dispositions/8dc34ca..ba81ab1.md`, at its disclosure paragraph | document is that record |
| 7 | Guard rows are written prospective and nothing says what happens to them when the repair lands, so the same range treated them three ways | EDGE-GUARD-ROW-STALE, POLLUTION-ROW-CONTRADICTS-ITS-SIBLING | 7a update both rows to what landed | `restate` | `docs/guards.md`, at the `PACKAGE-EDGE-OUT-AND-BACK` and `GUARD-COUNT-IS-POLLUTION` rows | document is `docs/guards.md` |
| 7 | " | " | 7b state the rule once in `AGENTS.md` so the next round does not decide it again | `declare` | `AGENTS.md`, at "Write them when the record is written, not when the repair lands" | governance prose; needs the owner's assent |
| 8 | A recorded re-runnable reading uses a bracket expression whose grammar it did not ask its owner about | WHITESPACE-READING-UNRUNNABLE | 8a correct the reading to a POSIX class and record what the wrong one reports | `restate` | `docs/guards.md`, at the `TRAILING-WHITESPACE-IN-GUARDS` row | document is `docs/guards.md` |
| 9 | A sentence repaired for over-claiming was replaced by one claiming more, because it moved out of the paragraph that bounded its subject | ONLY-DEFAULT-CLAIM-WIDENED | 9a narrow the subject back to the filling | `restate` | `validate_skills.validate_skill` docstring | document is that function's docstring |
| 10 | The seam class's contract describes one polarity and holds a case with the other | SEAM-CONTRACT-POLARITY | 10a widen the contract to both polarities | `restate` | `test_validate_skills.SchemaSeamTests` docstring | document is that class's docstring |
| 10 | " | " | 10b move the collection-level case to a class of its own | `converge` | `test_validate_skills.SchemaSeamTests` | `plan-implementation` |
| 11 | One of four classes added in a naming commit does not take the suffix the other eleven in the file take | CLASS-NAME-OFF-PATTERN | 11a rename it to the file's form | `converge` | `test_validate_skills.FillingAskedNotDefaultedTests` | `plan-implementation` |
| 12 | A docstring moved whole and two of its sentences name the module it left | LINE-DOCSTRING-MOVED-WHOLE | 12a rewrite the two sentences to read true where they now are | `restate` | `read_whole.Line` docstring | document is that class's docstring |
| 13 | The collision assertion counts a package marker as a shared name, so it is loud about a normal structure and silent about the defect it was added for | INIT-COUNTED-AS-A-COLLISION | 13a exclude the marker where the collision is judged | `guard` | `test_module_claims.ModuleClaimTests` at its collision case | `plan-implementation` |
| 13 | " | " | 13b let the owner stop treating the marker as a citable stem | `converge` | `check_citations.modules` | `plan-implementation`. Wider: it changes what the citation gate sees |
| 14 | The suite's own helper keeps the default the repair removed from the check, which is where the next caller copies from | TEST-HELPER-KEEPS-THE-DEFAULT | 14a remove the helper's defaults so the suite states what it validates against | `forbid` | `test_validate_skills.check` | `plan-implementation` |

Repair 11a is the rename this record's Reach cell for cause 11 named, so that cell now names the
class under its new name. A Reach cell is a coordinate and the repair it describes is what moved it;
the citation gate refused the old one, which is the signal that row itself predicted a rename would
have. Disclosed here rather than left to a commit message.

### Pattern

Three of this round's causes are the previous round's Pattern, unrepaired by having been named: a
tool applied to a block of text rather than a reader applied to a sentence. Cause 12 moved a
docstring whole, cause 9 moved a sentence out of the paragraph that bounded it, and cause 3 wrote a
paragraph where a table was.

Cause 1 is worse than a pattern and is the round's real finding. The mechanism written to hold a
boundary had the defect it was written against, and a sweep three commits later — which repaired
exactly that shape in two other places, citing exactly that reason — did not look at it. The sweep
rule says to enumerate the mechanism rather than reread the change; this was *in* the change and
still missed, which means rereading was what happened.

What made the difference this round is not a sharper reading. It is that the input ran the tree: the
escape, the red counts and the whitespace matcher were all measured in an isolated clone, and three
of them contradict something a commit message here asserted from inspection.

### Coupling

`2b` voided by `2a`. `10b` voided by `10a` only in part — the contract still needs a sentence either
way. `13b` makes `13a` unnecessary and reaches further than this round's scope. `7b` voids neither
row of cause 7 and is what stops the question being decided a fourth way.

### Dispositions

| Finding | Cause | Carried | Disposition | Reason (REQUIRED for decline and defer) |
|---|---|---|---|---|
| BOUNDARY-SET-NOT-THE-OWNERS | 1 | new | accept | — |
| LINE-RE-EXPORTED-UNDECLARED | 2 | new | accept | — |
| MEASUREMENT-BROKE-THE-TABLE | 3 | new | accept | — |
| MEASUREMENT-UNDER-A-PROSPECTIVE-HEADING | 3 | new | accept | — |
| TREE-COUNT-IN-A-MESSAGE | 4 | new | accept | — |
| SETTLED-READING-OVERWRITTEN | 5 | new | accept | — |
| SELFCHECK-EDIT-UNDISCLOSED | 6 | new | accept | — |
| EDGE-GUARD-ROW-STALE | 7 | new | accept | — |
| POLLUTION-ROW-CONTRADICTS-ITS-SIBLING | 7 | new | accept | — |
| WHITESPACE-READING-UNRUNNABLE | 8 | new | accept | — |
| ONLY-DEFAULT-CLAIM-WIDENED | 9 | new | accept | — |
| SEAM-CONTRACT-POLARITY | 10 | new | accept | — |
| CLASS-NAME-OFF-PATTERN | 11 | new | accept | — |
| LINE-DOCSTRING-MOVED-WHOLE | 12 | new | accept | — |
| INIT-COUNTED-AS-A-COLLISION | 13 | new | accept | — |
| TEST-HELPER-KEEPS-THE-DEFAULT | 14 | new | accept | — |

### Carried forward

| Finding | Disposition | Original reason, unchanged | Closure condition not established |
|---|---|---|---|
| REVIEW-EXEMPTION-NARROWER-THAN-ITS-OBJECT | defer | the repair edits `AGENTS.md`'s governance prose and has no effect while `check_citations.RECORDS` names only `docs/dispositions`; now when a round proposes adding `docs/reviews` to that constant | deferred rather than repaired, so no closure was attempted; the input neither re-reports it nor names the constant as changed |

### Closed

All fifteen accepted findings of `ba81ab1..1a84428`. Each was gate-reviewed or partially
gate-reviewed with the relevant gate opened, the input confirms the repair at the recorded cause,
and no row contradicts the closure elsewhere. Three closed with a caveat the input states and this
record keeps rather than absorbs: `PACKAGE-EDGE-OUT-AND-BACK`'s cause no longer holds at the import
layer — measured, zero package-to-repository edges — while the mechanism guarding it was defective,
which is cause 1 above and a new finding rather than a survival; `SCHEMA-DOCSTRING-DESCRIBES-A-VALUE`
closed and its docstring's new over-claim is cause 9; `SEAMTESTS-CONTRACT-OVERREACHES` closed for the
members it named and left one polarity, which is cause 10.

### Out of scope this round

none.

### Undetermined

none.

### Recurring

none. Causes 9, 10 and 12 are adjacent to closed findings rather than returns of them: in each case
the prior cause no longer holds and the new finding names a different one.

### Ungrouped

none.

### Sent back for review

Nothing new. The divergence between the shipped `0.4.1` skill and this tree, sent back by the
previous settlement, is unaddressed and remains sent back; this round's input did not reach it.

### Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — fifteen in Closed, one in Carried forward, none in both |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — causes 1 through 14 each carry at least one, and every Reach cell names a file plus the unit inside it, or a quoted phrase where no unit owns the text |
