# Disposition Record — `v0.4.1..64667c6`

**Source**: a `static-review` Review Record for `v0.4.1..64667c6`. This triage read a copy carrying
no header fields, and nothing here says why: whether the producer emitted them is not determinable
from this end, and this record claims neither reading. See Record integrity, where reconciling
against that copy produced three statements about the input that the producer's own record refutes
**Scope**: `v0.4.1..64667c6`, the range as it stands after the three-pass prose consolidation
**Prior round**: `docs/dispositions/v0.4.1..5fd8c2f.md`

The repair settled here lands above `64667c6`, at `68ddbea`.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | `PASS + CONTRACT-VIOLATED` | This row originally read "no verdict line", because the copy this triage was handed began at the Gate Index. The producer's record carries the verdict, and it is consistent with an index in which no gate was opened | pass |
| Calibration / Gate Index | Gates 1-8 | none opened. The declaration is wider than the pass; the index says so in its own words, which is what earlier rounds had to be told | mismatch, stated by the input |
| Finding count | 1 | 1 VIOLATED contract row, 3 holding, 4 verified claims, 1 Structural Cause naming the same finding. This row originally read "not stated" while this record's own Source line said "1 finding" — a self-contradiction that stood whatever the input said | pass |
| Coverage | partial, four sets enumerated, `triage-only` expanded in full | the enumeration is the previous round's set plus this round's record. This row originally read "names no four sets", from the same partial copy | pass |
| Non-finding sections | not declared | a single Structural Cause paragraph, which names the one finding | not claimed |

Three of these five rows were wrong when this record was first written, and they were wrong in one
direction: each said the input had claimed less than it did. The copy this triage read began at the
Gate Index, so the header fields were reconciled as absent rather than as unread. What settles it is
that the producer's own record carries them, which this triage had no way to see and no way to know
it could not see. The corrections stay visible in the cells; the reason they were possible is under
Causes as its own finding, and the repair is that a Review Record is persisted like this record is.

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| ROUNDS-UNSETTLED | re-reported as `AC-1` | inside | current finding → Dispositions |
| RULE-COUNTS-ROUNDS, COVERAGE-BY-REFERENCE | `AC-2`, `AC-3` and two claims re-read the amended text and report each repair holding | inside | Closed |
| The twelve findings carried into the previous round | no gate opened; the input says in its own words that it neither reproves nor disproves them | inside | closure candidate → Carried forward |
| F-11, F-12 | no commit object is enumerated | undetermined | Undetermined |

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | The disposition requirement reads as covering rounds whose Review Record no longer exists, so it reports a violation every round and can never be satisfied | ROUNDS-UNSETTLED (AC-1) | 1a say that it binds each round as it happens, and record the lost rounds once as accepted debt | declare | `AGENTS.md`, `development-knowns.yaml` | document — `AGENTS.md` and the registry, maintainer-owned |
| 1 | ″ | ″ | 1b reconstruct the lost records from the commits that repaired those rounds | `reconstruct (gap)` | not enumerable — the texts do not exist | declined — see Dispositions |
| 1 | ″ | ″ | 1c leave the requirement as it reads | — | — | declined — see Dispositions |

## Pattern

None new. This round applies the pattern the last two recorded, to the last clause carrying it: the
weighing rule that governs adding a check governs a rule written in prose as well, and a clause
nobody can satisfy is worse than its absence. It has now retired two standing findings — the
commit-prose clause in the previous round, this one here — and in both cases what was cut was the
half of the clause that named no harm, with the half that names one left in force.

The prose consolidation this round reviewed produced no finding of its own, which is the result
worth recording: the largest untestable change in the range passed static reverification, and the
three-pass discipline it was written under is what the next such pass should use — a sentence stays
if a reader acts differently because of it, and the account of which round found what belongs in
these records.

## Coupling

- `1a` voids `1b` and `1c`.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| ROUNDS-UNSETTLED — rounds settled with no persisted record, unrecoverable | 1 | carried, fifth round | accept | `68ddbea`, `1a` | `AGENTS.md` states the requirement binds each round as it happens and why a later reconstruction would not be a record; `development-knowns.yaml` carries the gap as accepted debt, with the cost it imposed recorded once — a finding from a lost round had to be re-found because no record held its Reach — and with a reconsider-when that fires if a round's record is lost again |
| ROUNDS-UNSETTLED, alternative `1b` | 1 | — | decline | — | A record assembled from repairing commits states what the repairs were, not what the round decided, and would read as a record. That makes the gap harder to see rather than smaller |
| ROUNDS-UNSETTLED, alternative `1c` | 1 | — | decline | — | A clause satisfiable only by texts that no longer exist reports a violation every round and repairs nothing |

## Carried forward

The twelve findings this round's predecessor carried, unchanged in disposition and reason:
**REF-FRAGMENT-CLOSES-QUOTE**, **ORDINAL-IN-NEW-MODULE**, **CLOSURE-NAMES-WRONG-SYMBOL**,
**VERDICT-SEAM**, **VERDICT-CONTROL**, **FENCE-HANDREAD**, **BINARY-AS-TEXT**,
**INPUT-PATTERN-SPLIT**, **REGISTRY-UNDERREPORT**, **REGISTRY-NOT-DERIVED**,
**LEDGER-TABLE-ROUNDS**, **RAWSCORES-PROVISIONAL**.

The input states plainly that it neither reproves nor disproves them, which is the clearest a
`triage-only` round has put it.

## Closed

| Finding | Prior disposition | Code evidence the cause no longer holds | Input evidence |
|---|---|---|---|
| RULE-COUNTS-ROUNDS | accept, `1a` of the prior round | `AGENTS.md` states the amended clause without counting the rounds behind it, and names the dated records as where such a number belongs | `AC-2` re-read the amended file and the ordinary source comments and reports the clause holding |
| COVERAGE-BY-REFERENCE | accept, `2a` of the prior round | `docs/dispositions/v0.4.1..bd5127f.md` reports its input's coverage as a mismatch and writes out the set the reference resolves to | `AC-3` verifies both the mismatch marking and the expansion |

Both close on the third closure condition — the input explicitly verifies the repair — not on
silence.

## Out of scope this round

None.

## Undetermined

**F-11 and F-12**, unchanged.

## Recurring

None. ROUNDS-UNSETTLED had recurred in each record since the gap was found; it is dispositioned here
rather than carried, because the repair changes what the requirement claims rather than attempting
the impossible thing it was read as claiming.

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — ROUNDS-UNSETTLED in Dispositions, RULE-COUNTS-ROUNDS and COVERAGE-BY-REFERENCE in Closed, twelve in Carried forward, F-11 and F-12 in Undetermined |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — `1a` names `AGENTS.md` and `development-knowns.yaml`, both read back from the file |

## What this round leaves standing

- **Twelve carried findings**, unchanged and unclosable by a round that opens no gate. This is the
  whole of the standing ledger now, and one gated round over the units that carry them is what
  retires it.
- **F-11 / F-12**, waiting on a governance decision nobody has asked for.
- **The lost rounds**, now recorded as debt rather than reported as a violation. The gap is the same
  size; what changed is that it is stated once instead of found again each round.
