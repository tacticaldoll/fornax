# Disposition Record — `7b98106..d75d484 (second reading)`

**Source**: `docs/reviews/7b98106..d75d484.second-reading.md`, 8 findings, FAIL at Gate 6 + CONTRACT-VIOLATED + CLAIM-REFUTED
**Scope**: `7b98106..d75d484`, derived with `git diff --name-only` — the record shape check, its suite, the shipped `triage-findings`, the repair ledger, and one archived Disposition Record
**Prior round**: `docs/dispositions/v0.4.1..d75d484.md`

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | names Gate 6 | the index records Gate 6 `fail` | pass |
| Calibration / Gate Index | declares Gates 1-7 | the index records eight rows, Gates 1-5 `pass`, 6 `fail`, 7 `blocked`, 8 `not inspected` — which is the calibration stated rather than a gate skipped | pass |
| Finding count | 8 | one gate row, five Against-Contract rows reading `VIOLATED`, and two claim rows reading `REFUTED` and `partial`. The two `holds` rows, the eight verified claims and the Ledger rows are not findings and are not counted | pass |
| Coverage | `partial`, four sets enumerated, with the gates named for each partial unit | the enumeration is decidable and was used for prior membership below | pass |
| Non-finding sections | the Ledger is declared as carrying no findings | it is, and every Structural Causes row names a finding already counted, so none adds to the total | pass |

This input reconciles clean on all five, which the round before it did not. It also carries a Gate 1
note explaining why the ladder was left open over an already-settled finding rather than closing on
it — a producer stating its own reasoning where a reader would otherwise read leniency.

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| UNDECLARED-LABEL-TALLY-WRONG | four enumerated unit sets — `record_shape.py` `gate-reviewed` at Gates 1-7 | inside | current finding → Dispositions |
| SECTION-ABSENCE-SKIPS-ITS-RULES | same | inside | closure candidate → Carried forward |
| DUPLICATE-SECTION-READ-AS-ONE | same | inside | closure candidate → Carried forward |
| KEY-READ-BY-PREFIX | same | inside | closure candidate → Carried forward |
| DISPOSITION-DOMAIN-UNCHECKED | same | inside | closure candidate → Carried forward |
| MISSING-RESULT-COLUMN-YIELDS-A-DOMAIN | same | inside | closure candidate → Carried forward |
| COMMENT-OUTLIVED-ITS-CODE | same | inside | closure candidate → Carried forward |
| RULE-ANNOTATED-SHAPE-GETS-DECLARED | same | inside | closure candidate → Carried forward |
| READ-FAILURE-SPELLED-THREE-WAYS | same — one of its locations is inside | inside | closure candidate → Carried forward |
| TREE-COUNT-IN-NEW-PROSE | same, and `docs/guards.md` is `partially-gate-reviewed` at Gates 1-2 | inside | closure candidate → Carried forward |
| ABSENT-CLAIM-AS-MISMATCH | same | inside | closure candidate → Carried forward |
| SELFCHECK-FOLDED-INTO-THE-INPUT-AUDIT | same | inside | closure candidate → Carried forward |
| UNIQUENESS-CONTROL-MISSING | `scripts/tests/test_record_shape.py` is `partially-gate-reviewed` at Gates 1-6 | inside | closure candidate → Carried forward |
| GUARDS-LEDGER-INCOMPLETE | `docs/guards.md` `partially-gate-reviewed` at Gates 1-2 | inside | closure candidate → Carried forward |
| LEDGER-TABLE-ROUNDS | same | inside | closure candidate → Carried forward |
| SHIPPED-SKILL-NAMES-A-PRIVATE-SYMBOL | `skills/triage-findings/SKILL.md` `gate-reviewed` | inside | closure candidate → Carried forward |
| SUBJECT-ABSENCE-READS-AS-CLEAN | the enumeration names five files; `scripts/check_citations.py` is not among them | outside | out of scope → Out of scope this round |
| SHIPPED-SKILL-CITATION-UNVERIFIED | same | outside | out of scope → Out of scope this round |
| AGENTS-POINTS-AT-THE-WRONG-SYMBOL | same | outside | out of scope → Out of scope this round |
| SHELL-COMMENT-RULE-NARROWER-THAN-CLAIMED | same | outside | out of scope → Out of scope this round |
| NOQA-CLAIM-REFUTED | same | outside | out of scope → Out of scope this round |
| TRAILING-WHITESPACE-UNSEEN | same | outside | out of scope → Out of scope this round |
| TRIGGER-KEYS-ON-THE-WEAKEST-AXIS | same | outside | out of scope → Out of scope this round |
| LIFECYCLE-OMITS-THE-NAMED-PRIOR | same | outside | out of scope → Out of scope this round |
| REVIEW-COVERAGE-AS-FINDING | same | outside | out of scope → Out of scope this round |
| REVIEW-FILED-A-VIOLATION-AS-A-VERIFIED-CLAIM | same | outside | out of scope → Out of scope this round |
| PARAGRAPH-SPLIT-NOT-REFLOWED | same | outside | out of scope → Out of scope this round |
| FIXTURE-READ-AS-PRODUCT | same | outside | out of scope → Out of scope this round |
| COVERAGE-ENUMERATION-ABSENT | same | outside | out of scope → Out of scope this round |
| RAWSCORES-PROVISIONAL | same | outside | out of scope → Out of scope this round |
| F-11 | same | outside | out of scope → Out of scope this round |
| F-12 | same | outside | out of scope → Out of scope this round |

A narrow range is what puts sixteen prior ids out of scope in one round. That is the enumeration
working rather than a regression: this input looked at five files and says nothing about the rest,
and saying so is what keeps the next round from reading its silence as closure.

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | The seat rules are written as claims about a record and applied to every record in the corpus, with no notion of which revision of the contract each was settled under, so an edit to the contract convicts the archive rather than the change | ARCHIVE-BOUND-TO-CURRENT-CONTRACT | 1a key each record to the contract revision it was settled under and judge it against that | `distinguish` | `record_shape.RequiredKeys`, `record_shape.ClosedKeys`, `record_shape.declared`, `record_shape.record_defects`, `test_record_shape.Seats` | `plan-implementation` |
| 1 | " | " | 1b scope the seat rules to the record the round is producing and leave the archive to the round that wrote it | `forbid` | `record_shape.check`, `record_shape.record_defects`, `test_record_shape.Seats` | " |
| 1 | " | " | 1c keep the coupling and state it as the check's standing cost, freezing the declared labels deliberately rather than by accident | `declare` | `scripts/record_shape.py` "a predicate covering that costs nothing", `development-knowns.yaml` | none — document repair |
| 2 | `AGENTS.md` governs editing a record's numbers, an archived Review Record's language and a raw score, and decides nothing about a row label — so a round had to invent the distinction and recorded it in the record it was editing, leaving the next round to decide it again | LABEL-EDIT-RULE-UNWRITTEN | 2a write the label-versus-reading distinction where the other record-editing rules live | `restate` | `AGENTS.md` "recorded here so the next reader is not deciding again", `AGENTS.md` "a later rule makes it a worse record of the reading it reports" | none — document repair |
| 3 | A revert measurement stated as a code shape rather than as a named unit cannot be reproduced by the round that would use it, which is the whole purpose of recording one | REVERT-CLAIM-NAMES-A-SHAPE | 3a record the measurement in the repair ledger as a named unit whose removal produces it | `restate` | `docs/guards.md`, `record_shape.rules_for` | " |
| 3 | " | " | 3b none available for the commit prose itself, which is history and not editable — the ledger row is the only re-runnable form | `restate` | `docs/guards.md` | " |

Cause 6 of the prior round — the round's own new prose carrying tallies of the tree — is re-reported
here and keeps its repairs unchanged; see Dispositions.

## Pattern

`none`. No shape recurs across two independent causes in this round. Causes 1 and 2 are one situation
seen from two sides rather than two instances of a shape — the check reaches backwards into the
archive, and the rule that would say whether reaching backwards is allowed does not exist — so they
belong in Coupling below, and calling them a pattern would invent a shape to fill the section.

The prior round's pattern is unaffected and still open: a reader whose not-read state is spelled as
its clean state, held by that record's causes 1, 2 and 14, with its repair routed to
`design-boundaries`.

## Coupling

`1c voided by 1a` and `1c voided by 1b` — once a record is judged against the contract it was settled
under, or the archive is left alone, there is no standing coupling left to declare.

`2a` is answered by `1b` in one direction only: leaving the archive alone removes the occasion for
editing a record's label, but not the question of whether such an edit is permitted when a round
chooses to make one. So `2a` survives either repair to cause 1 and is not voided.

`3b` is `3a` restated for the half that has no repair, not a second task.

## Dispositions

| Finding | Cause | Carried | Disposition | Reason (REQUIRED for decline and defer) |
|---|---|---|---|---|
| ARCHIVE-BOUND-TO-CURRENT-CONTRACT — a contract edit convicts every archived record rather than the change | 1 | new | accept | — |
| LABEL-EDIT-RULE-UNWRITTEN — the rule that would authorise the edit lives in the edited record | 2 | new | accept | — |
| REVERT-CLAIM-NAMES-A-SHAPE — a measurement stated as a code shape cannot be reproduced | 3 | new | accept | — |
| UNDECLARED-LABEL-TALLY-WRONG — the docstring's tally is one more than the corpus holds | prior 6 | carried | accept | unchanged from `docs/dispositions/v0.4.1..d75d484.md`: the round's own new prose carries a tally of the tree its own rule forbids. No recorded Reach changed, so the repair never landed and the disposition is frozen rather than re-litigated. This input adds the cause: the tally was true when measured and false by the end of the same commit, because that commit's own label edit removed one |

Three new findings and one carried, from eight producer rows. The input's Against-Contract row about
the declined record-level rule is **not** a finding of its own — it is evidence that the prior
round's cause 1 repair `1a` is the right one, since the ground for declining it was that such a rule
would hold only itself and three now want it. Recorded here rather than counted, so the next round
does not triage the same defect twice.

## Carried forward

| Finding | Prior disposition | Original reason, unchanged | Closure condition not established |
|---|---|---|---|
| SECTION-ABSENCE-SKIPS-ITS-RULES | accept | deleting a governed section skips every rule for that seat | 2 — no repair landed; reproduced again by this input |
| DUPLICATE-SECTION-READ-AS-ONE | accept | a second governed section is read by nothing | 2 — no repair landed |
| KEY-READ-BY-PREFIX | accept | a row's key is the text before an invented separator | 2 — no repair landed |
| UNIQUENESS-CONTROL-MISSING | accept | no alternate-spelling control for the separator | 2 — no repair landed |
| DISPOSITION-DOMAIN-UNCHECKED | accept | the findings seat's declared verdict column has no domain rule | 2 — no repair landed |
| MISSING-RESULT-COLUMN-YIELDS-A-DOMAIN | accept | a misread contract builds a usable-looking shape | 2 — no repair landed |
| TREE-COUNT-IN-NEW-PROSE | accept | the round's new prose tallies the tree its own rule forbids tallying | 2 — no repair landed |
| GUARDS-LEDGER-INCOMPLETE | accept | the ledger's rows cover a subset of the findings it claims | 2 — no repair landed; this round adds prospective rows for its own findings and does not backfill |
| SHIPPED-SKILL-NAMES-A-PRIVATE-SYMBOL | accept | the Reach example is a private function here | 2 — no repair landed |
| COMMENT-OUTLIVED-ITS-CODE | accept | a comment stranded by the deletion it documented | 2 — no repair landed, and this input confirms the comment is still there as diff context |
| RULE-ANNOTATED-SHAPE-GETS-DECLARED | defer | the annotation is satisfied only by proxy | — deferred; no new evidence, and no type checker has entered the gate |
| READ-FAILURE-SPELLED-THREE-WAYS | defer | one question, three spellings | — deferred; no new evidence |
| ABSENT-CLAIM-AS-MISMATCH | accept | a Record integrity row can convict on a claim the input never made, because nothing ties the row's Result to whether its `Input claim` cell names a claim at all | 2 — the cause holds; registered as `record-shape-cannot-read-a-row-subject` |
| SELFCHECK-FOLDED-INTO-THE-INPUT-AUDIT | accept | a fact about the record being written can be filed in the table that audits the input, because the two tables' subjects are stated in prose and enforced by nothing | 2 — the cause holds |
| LEDGER-TABLE-ROUNDS | out of scope in earlier rounds | unchanged | 1 — `docs/guards.md` reached Gates 1-2 only |

## Closed

`none`. No repair has landed since the prior record, so no closure test can pass on code evidence.

## Out of scope this round

**SUBJECT-ABSENCE-READS-AS-CLEAN, SHIPPED-SKILL-CITATION-UNVERIFIED, AGENTS-POINTS-AT-THE-WRONG-SYMBOL,
SHELL-COMMENT-RULE-NARROWER-THAN-CLAIMED, NOQA-CLAIM-REFUTED, TRAILING-WHITESPACE-UNSEEN,
TRIGGER-KEYS-ON-THE-WEAKEST-AXIS, LIFECYCLE-OMITS-THE-NAMED-PRIOR, REVIEW-COVERAGE-AS-FINDING,
REVIEW-FILED-A-VIOLATION-AS-A-VERIFIED-CLAIM, PARAGRAPH-SPLIT-NOT-REFLOWED, FIXTURE-READ-AS-PRODUCT,
COVERAGE-ENUMERATION-ABSENT, RAWSCORES-PROVISIONAL, F-11, F-12.** Sixteen ids, none of whose units is
among the five files this input's coverage enumerates. Neither carried nor closed; the declined ones
keep their dispositions. They return to the ordinary flow at the next round whose range reaches them.

## Undetermined

`none`. The input enumerates four coverage sets, which is what empties this section.

## Recurring

`none`. `UNDECLARED-LABEL-TALLY-WRONG` is re-reported and is not recurring: no location in its
recorded Reach changed, so its accepted repair was never attempted, and reappearance without an
attempted repair is evidence about nothing.

## Ungrouped

`none`.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — the prior record left thirty-two ids open: eighteen in its Dispositions table and fourteen in its Carried forward table. One is re-reported and sits in Dispositions, fifteen are Carried forward, sixteen are Out of scope this round. One plus fifteen plus sixteen is thirty-two, none in two homes and none dropped |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — causes 1, 2 and 3 each carry at least one repair whose Reach names a symbol or a quoted phrase, and the carried finding's repairs are the prior record's `6a` and `6b`, unchanged. Every symbol named here was resolved through `check_citations.module_symbols` before this record was written |
| Every commit this record names is reachable from HEAD | pass — `7b98106`, `d75d484` and `3cbab14` all resolve and all are ancestors of HEAD |
| Every finding this record accepts has a row in `docs/guards.md` | pass — all three are in the second-reading section of that file, written in this turn. Each is prospective and says so: no repair has landed, so no row carries a measured number, and `REVERT-CLAIM-NAMES-A-SHAPE` records that nothing can go red for it at all |
| This record was written in the turn that repaired | mismatch — no repair landed in this turn either, which is why Closed is empty and every carried finding fails closure condition 2 on the same ground |
