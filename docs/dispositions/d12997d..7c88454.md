## Disposition Record

**Source**: `docs/reviews/d12997d..7c88454.md`, 12 reported findings, FAIL at Gate 2 + CONTRACT-VIOLATED + CLAIM-REFUTED. Run in fresh context, with an isolated clone: the input parsed this file's tables at every commit of the range with the CommonMark owner, and took seven revert or injection measurements
**Scope**: `d12997d..7c88454`, derived with `git diff --name-only` — eleven files, eleven commits
**Prior round**: `docs/dispositions/d54813e..8c87363.md`

### Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | FAIL at Gate 2 + CONTRACT-VIOLATED + CLAIM-REFUTED | the index records Gate 2 `fail` as the lowest failing gate; six VIOLATED rows and one refuted claim answer for the suffixes | pass |
| Calibration / Gate Index | Gates 1–7 | every gate's status recorded with the file that closed each ladder; Gate 1 passes for the first time in four rounds, measured with the CommonMark owner on every commit | pass |
| Finding count | 12, itemised as Gate 2 five, Gate 6 one, Against-Contract six | the itemisation holds: Against-Contract carries nine VIOLATED rows, two duplicating gate findings and one declared settled remainder | pass |
| Coverage | partial, four unit sets enumerated | each partial unit names the gate that closed its ladder; both empty sets stated | pass |
| Non-finding sections | the `Findings` field excludes duplicate-axis rows, the Ledger, `holds`/`verified` rows, and observations the prior round settled as non-findings | every excluded class is named; the two carried-over observations each state the prior judgment they rest on | pass |

The input carried runtime evidence of its own and this settlement re-measured the heaviest finding
before accepting it, agreeing with the input at all three entrances.

### Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| the sixteen ids of `d54813e..8c87363` | four enumerated unit sets covering every file the repairs touched | inside | fifteen → Closed; one deferral carried |

### Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach | Route |
|---|---|---|---|---|---|---|
| 1 | One map answers two questions and its filter serves only one: a package marker is not a name a citation can use, and is a file that imports like any other | BOUNDARY-BLIND-TO-MARKERS, EVERY-MODULE-CLAIM-NOW-FALSE | 1a build the boundary's two sides from one walk of the files, keyed by path | `converge` | `test_module_claims.SOURCES`, `test_module_claims.PACKAGE_SOURCES`, `test_module_claims.OUTSIDE_NAMES`, `test_module_claims.imports_at` | **landed** |
| 1 | " | " | 1b correct the two `every module` sentences to say what the filter excludes | `restate` | `check_citations.modules` docstring, `test_module_claims` at its `MODULES` comment | document is those two modules' own prose |
| 2 | A repair landed and its guard row still describes the alternative that did not | COLLISION-ROW-NAMES-THE-WRONG-REPAIR | 2a rewrite the row to 13b and record what was measured | `restate` | `docs/guards.md` at the `INIT-COUNTED-AS-A-COLLISION` row | document is `docs/guards.md` |
| 3 | Section headings and leads are contradicted by the rows they govern, in two sections | HEADINGS-DENY-THEIR-OWN-ROWS | 3a correct both headings and leads, leaving the rows | `restate` | `docs/guards.md` at "the 1a84428 round, nothing measured" and at "the 8c87363 round" | document is `docs/guards.md` |
| 4 | A dated reading does not reproduce on the tree its own heading names | CITATION-COST-OFF-BY-ITS-OWN-SENTENCE | 4a re-measure on the commit that carries the section and record both numbers with what moved between them | `restate` | `docs/guards.md` at "the citation cost of moving a symbol" | document is `docs/guards.md` |
| 5 | A disclosure names "this round" in a section belonging to another, and a later coordinate change was not folded into it | DISCLOSURE-NAMES-THE-WRONG-ROUND | 5a name the round and fold the later change in | `restate` | `docs/guards.md` at "Three rows below had their class names updated" | document is `docs/guards.md` |
| 6 | A paragraph inserted into a settled record took the previous paragraph's closing sentences with it | DISCLOSURE-TOOK-THE-CLOSING-SENTENCES | 6a give each paragraph its own ending and separate the two kinds of edit | `restate` | `docs/dispositions/8dc34ca..ba81ab1.md` at "Same treatment and same reason" | document is that record |
| 7 | The type's old name survives in two docstrings, in units this range edited | OLD-TYPE-NAME-IN-TWO-DOCSTRINGS | 7a rename both, as one sweep of every prose site | `restate` | `test_read_whole.words`, `test_shell_script.LineHoldsNoNewline` | document is those two units' docstrings |
| 8 | A repair landed in a different form from the one the record and the ledger describe, and only the message says so | KEYWORD-ONLY-NOT-WHAT-WAS-RECORDED | 8a record the form that landed in the settled record's Reach and the ledger row | `restate` | `docs/dispositions/d54813e..8c87363.md` at its cause 14 Reach, `docs/guards.md` at the `TEST-HELPER-KEEPS-THE-DEFAULT` row | document is those two files |
| 8 | " | " | 8b make the helper call the check by keyword so the seam shows the filling is asked for on both sides | `converge` | `test_validate_skills.check` | `plan-implementation` |
| 9 | A count of this tree is a commit message's stated reason and lives nowhere else | CLASS-COUNT-IN-A-MESSAGE | 9a record the reading in a dated section | `restate` | `docs/guards.md` | document is `docs/guards.md`; the message is history and is not edited |
| 10 | A contract sentence counts the cases in its own class | CASE-COUNT-IN-A-CONTRACT | 10a describe the polarities without counting | `restate` | `test_validate_skills.SchemaSeamTests` docstring | document is that class's docstring |
| 11 | Two causes rode in one commit | TWO-CAUSES-ONE-COMMIT | 11a none — the commit is history and names both | `restate` | — | nothing to change; recorded so the next round does not re-report it |

### Pattern

The input names it and it is the same one, sharpened: **a repair's reach is taken to be the unit
rather than every place the question is answered.** Cause 7 here is the type rename reaching one
docstring and missing two in files the same range edited; cause 1 is a filter correct for one
consumer and wrong for the other reading the same map; causes 4 and 9 are measurements written
without being re-run on the tree they name.

Three rounds have now named a version of this, and the third one repaired two instances of it while
making a third. What has not been tried is the step the sweep rule actually asks for and none of the
three performed: after a repair, enumerate every site that answers the same question and check each
against the property the repair established. Naming the pattern again is not that step.

### Coupling

`1b` does not void `1a` — one is the mechanism and the other the sentence describing it. `8b` makes
`8a`'s ledger row describe something stronger but does not remove the need to correct the record.
`11a` is a disposition without a repair, which is what a finding about a commit that cannot be
edited leaves.

### Dispositions

| Finding | Cause | Carried | Disposition | Reason (REQUIRED for decline and defer) |
|---|---|---|---|---|
| BOUNDARY-BLIND-TO-MARKERS | 1 | new | accept | — |
| EVERY-MODULE-CLAIM-NOW-FALSE | 1 | new | accept | — |
| COLLISION-ROW-NAMES-THE-WRONG-REPAIR | 2 | new | accept | — |
| HEADINGS-DENY-THEIR-OWN-ROWS | 3 | new | accept | — |
| CITATION-COST-OFF-BY-ITS-OWN-SENTENCE | 4 | new | accept | — |
| DISCLOSURE-NAMES-THE-WRONG-ROUND | 5 | new | accept | — |
| DISCLOSURE-TOOK-THE-CLOSING-SENTENCES | 6 | new | accept | — |
| OLD-TYPE-NAME-IN-TWO-DOCSTRINGS | 7 | new | accept | — |
| KEYWORD-ONLY-NOT-WHAT-WAS-RECORDED | 8 | new | accept | — |
| CLASS-COUNT-IN-A-MESSAGE | 9 | new | accept | — |
| CASE-COUNT-IN-A-CONTRACT | 10 | new | accept | — |
| TWO-CAUSES-ONE-COMMIT | 11 | new | accept | — |

### Carried forward

| Finding | Disposition | Original reason, unchanged | Closure condition not established |
|---|---|---|---|
| REVIEW-EXEMPTION-NARROWER-THAN-ITS-OBJECT | defer | the repair edits `AGENTS.md`'s governance prose and has no effect while `check_citations.RECORDS` names only `docs/dispositions`; now when a round proposes adding `docs/reviews` to that constant | the constant is unchanged; the input confirms a new section mentions `docs/reviews` only as a corpus contrast and proposes nothing |

### Closed

All fifteen accepted findings of `d54813e..8c87363`. Each was gate-reviewed or partially
gate-reviewed with the relevant gate opened, and the input verifies the repair at the recorded cause
— by measurement for the ones that admit one. Two close with a caveat this record keeps rather than
absorbs. `BOUNDARY-SET-NOT-THE-OWNERS` closed at the stem layer, measured, while the same boundary
was blind at the marker layer, which is cause 1 above: a second entrance to one hole rather than the
survival of the first. `TEST-HELPER-KEEPS-THE-DEFAULT` closed against a repair the record describes
differently from the one that landed, which is cause 8.

### Out of scope this round

none.

### Undetermined

none.

### Recurring

none. Cause 1 is a new finding against a closed one's mechanism, not a re-report: the prior cause —
two sides found by two searches — no longer holds, and the new one is a filter correct for a
different question.

### Sent back for review

Still nothing new, and the previous two remain: the divergence between the shipped `0.4.1` skill and
this tree, and the `family` flat-field remainder that three inputs have now re-derived and each
recorded as settled.

### Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — fifteen in Closed, one in Carried forward, none in both |
| Every accepted cause carries at least one repair with an enumerated Reach | pass for ten of eleven; cause 11 carries a disposition and no repair, because its unit is a commit message, and the row says so rather than enumerating a Reach that does not exist |
