# Disposition Record — `v0.4.1..3901827`

**Source**: `docs/reviews/v0.4.1..3901827.md`, 2 Gate 3 rows, 6 violated contract rows and 3 refuted
claims. The copy this triage read carried no header fields
**Scope**: `v0.4.1..3901827`. The input's Gate Index reports Gates 1 and 2 pass, Gate 3 fail, Gate 4
blocked, 5–8 not inspected, over `scripts/check_citations.py`. It states no coverage sets
**Prior round**: `docs/dispositions/v0.4.1..804961c.md`

The repairs settled here landed at `ef8b53c`, before this record existed.

**Written late, and this is the second round in a row that happened.** The mechanism is not the one
the previous examination found. There the round's findings were about the previous record, so the
repairs went into it. Here the repairs landed correctly and the record was offered as a follow-up
question — "write it now, or push?" — and the next round's input arrived before the answer. A round
does not end when its repairs land; it ends when its record is written, and an offer is not a record.
`AGENTS.md` now says that as the completion boundary, and `development-knowns.yaml` carries the
second examination.

**The first gated round of this range.** Gates 1–3 were opened over one module, which is what makes
this round different from the six before it: a gate that opens can fail, and this one did.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | not in the received copy | Gate 3 is fail and Gate 4 blocked, which is a `FAIL at Gate 3` shape | not claimed |
| Calibration / Gate Index | not in the received copy | Gates 1–4 carry statuses, 5–8 are `not inspected` | not claimed |
| Finding count | not in the received copy | 2 Gate 3 rows + 6 violated contract rows + 3 refuted claims, with each refuted claim keying to a row, so 8 stable findings | not claimed |
| Coverage | not in the received copy | the Gate Index names the module the gates were opened over; no set is enumerated | not claimed |
| Non-finding sections | not declared | a Structural Causes list whose entries each name an existing finding | not claimed |

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| The citation checker's error states | reported as two Gate 3 rows | inside | current finding → Dispositions |
| CITATION-CHECK-MISSES-A-FORM | reported again as `AC-1` and `AC-2` | inside | current finding → Dispositions, and Recurring |
| CITATION-CHECK-SKIPS-A-TYPO | reported again as `AC-3` | inside | current finding → Dispositions, and Recurring |
| COUNTS-IN-THE-NEW-PROSE | reported again as `AC-4` and `AC-5` | inside | current finding → Dispositions, and Recurring |
| RECONCILED-AGAINST-A-PARTIAL-COPY | reported again as `AC-6` | inside | current finding → Dispositions |
| ROUND-SETTLED-BY-ITS-REPAIRS | `AC-7` reports the two missing records now exist | inside | Closed |
| The twelve carried findings | no coverage enumerated | undetermined | Undetermined |

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | One `None` carried three states — no such module, unreadable, unparseable — so a broken module read as a defective citation | CHECKER-ERROR-STATES (Gate 3) | 1a return a mapping or a reason, never both, and report the reason as its own diagnostic | distinguish | `check_citations.Symbols`, `check_citations.module_symbols`, `check_citations.citations`, `test_check_citations.ModuleIdentity.test_a_module_that_cannot_be_parsed_is_reported_as_that` | `plan-implementation` |
| 2 | A dict keyed by file stem lost one of two files sharing a stem, and a citation into the shadowed one was checked against the other | CHECKER-STEM-COLLISION (Gate 3) | 2a make the collision a value and report it against the repository, since which file a bare stem means is not a question the citation form can answer | distinguish | `check_citations.Modules`, `check_citations.modules`, `check_citations.check`, `test_check_citations.ModuleIdentity.test_a_stem_naming_more_than_one_module_is_reported_once` | `plan-implementation` |
| 3 | The line-citation pattern capped the extension's length and admitted a URL's authority | CITATION-FORM-STILL-PARTIAL (AC-1, AC-2) | 3a drop the length cap and leave a scheme-bearing word alone | guard | `check_citations.LINE_CITATION`, `check_citations.URL_AUTHORITY` | `plan-implementation` |
| 4 | A similarity threshold decided whether an unknown module was external, so every misspelling unlike enough to a real name passed | CITATION-TYPO-BELOW-THRESHOLD (AC-3) | 4a ask importability instead of similarity | guard | `check_citations.citations` | `plan-implementation` |
| 5 | The rule's prose, its checker's docstring, and a test's docstring each carried a measurement of the tree | COUNTS-IN-THE-NEW-PROSE (AC-4, AC-5) | 5a say the thing without the number | restate | `AGENTS.md`, `check_citations.py`, `test_check_citations.py` | document — maintainer-owned |
| 6 | A record's Source line read as a claim about the producer's document | RECONCILED-AGAINST-A-PARTIAL-COPY (AC-6) | 6a say only what this triage read, and drop the phrase that described the producer's document | restate | `docs/dispositions/v0.4.1..64667c6.md` | document — disposition record, maintainer-owned |
| 7 | A round's record is written after its repairs and offered rather than produced, so the next round's input can arrive first | ROUND-ENDS-WHEN-ASKED (AC-7 of the next round) | 7a make the record part of the turn that repairs, before reporting | declare | `AGENTS.md`, `development-knowns.yaml` | document — maintainer-owned |

## Pattern

Causes 3 and 4 are the shape the previous record named — a check containing the class it refuses —
arriving a second time in the same module, and that is worth saying plainly: the first repair to that
module closed the instances the round reported and left the boundary undefended. A pattern named in a
record is not a pattern repaired.

Causes 1 and 2 are different and are what a gate opening buys. Neither is a wrong answer; both are
error states that cannot be told apart from a correct one. Six rounds of contract probes had not
found them, because a probe asks what the code answers and a gate asks how it fails.

## Coupling

- `4a` was later replaced rather than kept: the round after this one found that importability asks
  the environment, and the answer is derived from the declared interpreter and this repository's own
  imports. Recorded here because a repair this record accepted did not survive one round.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| CHECKER-ERROR-STATES — three failures shared one return value | 1 | new | accept | `ef8b53c`, `1a` | A module that will not parse is reported as that, and the assertion checks the diagnostic does not also say the citation names nothing. Collapsing the type back turns the case red |
| CHECKER-STEM-COLLISION — two files sharing a stem left one unreachable | 2 | new | accept | `ef8b53c`, `2a` | Measured: no stem collides in the tree today, so the repair is against the silence rather than a live defect. A fixture with two `sample_module.py` files reports once, against the repository; dropping the collision list turns it red |
| CITATION-FORM-STILL-PARTIAL — a capped extension and an exempt authority | 3 | carried, second instance | accept | `ef8b53c`, `3a` | Measured before and after on a long extension and a URL. The next round found the exemption was still too wide and the extension rule too narrow, both repaired there |
| CITATION-TYPO-BELOW-THRESHOLD — a misspelling unlike enough to a real name passed | 4 | carried, second instance | accept | `ef8b53c`, `4a` | Measured: names at three distances from a real module are all reported. The repair itself was superseded one round later, for the reason in Coupling |
| COUNTS-IN-THE-NEW-PROSE — a measurement in the rule and a stale list length in a test | 5 | carried, third instance | accept | `ef8b53c`, `5a` | Both reworded. The governance sentence took one more round to lose its quantity entirely |
| RECONCILED-AGAINST-A-PARTIAL-COPY — the Source line read as a claim about the producer | 6 | carried, fourth instance | accept | `bf76e7f`, `6a` | The line now says only that this triage read a copy carrying no header fields, and that why is not determinable from this end. The phrase describing the producer's document is gone, which is what three earlier repairs had kept |
| ROUND-ENDS-WHEN-ASKED — the record was offered rather than written | 7 | new | accept | `bf76e7f`, `7a` | `AGENTS.md` states the completion boundary and the registry carries the second examination. What makes this different from the first repair is that the first restated the rule and this names when a round ends |

## Carried forward

The twelve findings, unchanged in disposition and reason: **REF-FRAGMENT-CLOSES-QUOTE**,
**ORDINAL-IN-NEW-MODULE**, **CLOSURE-NAMES-WRONG-SYMBOL**, **VERDICT-SEAM**, **VERDICT-CONTROL**,
**FENCE-HANDREAD**, **BINARY-AS-TEXT**, **INPUT-PATTERN-SPLIT**, **REGISTRY-UNDERREPORT**,
**REGISTRY-NOT-DERIVED**, **LEDGER-TABLE-ROUNDS**, **RAWSCORES-PROVISIONAL**.

This round opened gates over one module, and none of the twelve is carried by that module. A gated
round retires them only when it is calibrated over the units that carry them.

## Closed

| Finding | Prior disposition | Code evidence the cause no longer holds | Input evidence |
|---|---|---|---|
| ROUND-SETTLED-BY-ITS-REPAIRS | accept, `5a` of the previous round | `docs/reviews/` and `docs/dispositions/` each hold a file for `v0.4.1..87c7bb4` and `v0.4.1..804961c` | `AC-7` looked for both and reports them present |

## Out of scope this round

None decidable — the input enumerates no coverage.

## Undetermined

**F-11 and F-12**, and the twelve carried findings' membership.

## Recurring

- **CITATION-FORM-STILL-PARTIAL** and **CITATION-TYPO-BELOW-THRESHOLD**, second instances, in the
  module whose first repair the previous round accepted. Every location in that repair's Reach
  changed and the findings survived, which says the cause was stated too narrowly: it was recorded as
  two gaps in one check, and what the code shows is that **a rule stated in prose and implemented by
  one pattern will be defended only at the examples the round happened to name.**
- **COUNTS-IN-THE-NEW-PROSE**, third instance, and **RECONCILED-AGAINST-A-PARTIAL-COPY**, fourth.
  Both are prose whose repairs kept the wording that produced the next report.

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — one in Closed, seven in Dispositions, twelve in Carried forward, F-11 and F-12 in Undetermined |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — every Reach names files and symbols, and the symbols are checked by `scripts/check_citations.py` rather than by a reading |
| This record is dated by the round it settles | stated in the header, with why it was written late and how that differs from the previous time |

## What this round leaves standing

- **Twelve carried findings.** A gate opened for the first time in this range, over one module, and
  none of the twelve is carried by that module.
- **F-11 / F-12**, unchanged.
- **A repair this record accepted that did not survive a round** — `4a`, replaced when the next round
  found it asked the environment. Recorded rather than quietly overwritten.
