# Disposition Record — `v0.4.1..61789c2`, second reading

**Source**: `docs/reviews/v0.4.1..61789c2.second-reading.md`, 6 findings,
PASS + CONTRACT-VIOLATED + CLAIM-REFUTED
**Scope**: `v0.4.1..61789c2`, the same range the record beside this one settles. Two readings of one
range are two rounds, so this carries the range plus what it is — the naming the previous
second reading established
**Prior round**: `docs/dispositions/v0.4.1..61789c2.md`, settled in the same turn

The repairs settled here land at `74cd7e3`, read after that commit was final.

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | PASS + CONTRACT-VIOLATED + CLAIM-REFUTED | Gates 1–4 pass over the one partially-gate-reviewed module, 5–8 not inspected; three contract rows violated and three claims refuted | pass |
| Calibration / Gate Index | Gates 1–8 | Gates 1–4 carry statuses and 5–8 are `not inspected` | pass |
| Finding count | 6, stated as 3 contract violations plus 3 independently counted refuted claims | 3 + 3, and each refuted claim keys to one of the violated rows, which the input says by counting them separately on purpose | pass |
| Coverage | partial, four sets, `triage-only` derived as the range's changed files minus the one partially-gate-reviewed module | derived from the range rather than transcribed, which is the shape `AGENTS.md` asks a record's scope to take | pass |
| Non-finding sections | not declared | the Structural Causes rows each name an existing finding | not claimed |

A sixth row, "Range already settled", stood here and was removed by the `74cd7e3..85d0569` round. The
record contract declares five checks; that row was a sixth of this record's own invention, and it
scored an input claim of "not stated" as `mismatch` — the value meaning the input contradicts itself,
where the contract's third value, `not claimed`, is the one for a claim the input never makes. The
row's own text said "recorded, not a defect", which is the reasoning `not claimed` exists to carry.
Nothing is lost by its removal: the observation it made — that no producer is asked whether the range
it reads already carries a record — is not a reconciliation of this input and belongs in a
development known if it is worth keeping.

## Prior scope resolution

| Prior finding | Scope evidence stated by this round's input | Membership | This round's slot |
|---|---|---|---|
| SETTLEMENT-NAMES-AN-ORPHAN | `AC-3` and one refuted claim report the same object | inside | current finding → Dispositions, shared with the round settled beside this one |
| LEDGER-ROW-IN-THE-WRONG-TABLE | `AC-2` reports the schema sentence against the rows that cannot satisfy it | inside | current finding → Dispositions, shared |
| GUARDS-LEDGER-MISSES-THE-SETTLING-ROUND | `AC-1` and two refuted claims report six accepted findings with no row | inside | current finding → Dispositions |
| IMPORTS-OUTSIDE-THE-INVARIANT | a claim reports the repair holding | inside | Closed |
| ROW-NOT-SELF-CONTAINED | a claim reports the row readable alone | inside | Closed |
| LEDGER-QUOTES-WRONG-FIGURES | a claim reports the figures corrected | inside | Closed |
| SNAPSHOT-NEEDS-A-WORKTREE, FINGERPRINT-COLLAPSED-NONE | `AC-5` and `AC-6` report both repairs holding | inside | Closed |
| DECLINE-NOT-RE-WEIGHED | `docs/guards.md` is among the `triage-only` files, which open no gate | inside | Carried forward |
| REACH-STOPS-AT-THE-FILE | the Reach cells it names sit in records among the `triage-only` files | inside | Carried forward |
| PARAGRAPH-SPLIT-NOT-REFLOWED | `AGENTS.md` and `docs/dispositions/v0.4.1..de72982.md` are among the `triage-only` files | inside | Carried forward |

The last three rows were added by the `74cd7e3..85d0569` round. They are the accepted findings of the
record this one names as prior, and they had no lifecycle home here while the self-check below
answered `pass` — the omission that round reports. They resolve to Carried forward rather than Closed
because this round's input lists their units as `triage-only`, and a unit that opened no gate cannot
close a finding by not mentioning it. Their repairs may well have landed; what is missing is this
input's authority to say so.

## Causes and candidate repairs

| # | Cause (the thing to change) | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | The settlement turn wrote the records and treated the round as finished, without the ledger update the same turn's own clause requires | GUARDS-LEDGER-MISSES-THE-SETTLING-ROUND (AC-1, two claims) | 1a add a row for every finding the two records accepted, measuring the one that has a guard | restate | `docs/guards.md`, the "Measured 2026-08-28" table and the "Repairs with no guard, and why" table | document — maintainer-owned |
| 2 | The ledger's schema sentence describes a guarded row and is stated over every row | LEDGER-ROW-IN-THE-WRONG-TABLE (AC-2) | 2a say "each guarded entry", and describe the other kind as its own | restate | `docs/guards.md`, the sentence opening "Each guarded entry names" | document — maintainer-owned |
| 3 | A record named a commit that no longer exists in any reachable history | SETTLEMENT-NAMES-AN-ORPHAN (AC-3, one claim) | 3a name the reachable commit and verify every hash the way a later round would | restate | `docs/dispositions/v0.4.1..de72982.md`, the Scope line and each `Settled by` cell | document — maintainer-owned |

## Pattern

Cause 1 is this turn's sharpest instance of the shape these records keep naming, and it is mine at
one remove: **I wrote the clause saying the ledger is written with the record, and then wrote the
records without the ledger.** The clause was one commit old. The previous round had even noted that
it "takes effect from the next round" — and the next round is the one that skipped it.

That is not an argument for restating the clause again. It is the case for what the previous record
already concluded about this class: the repair is to move when the work happens, and here the work
is one step of a settlement that was treated as finished when the record was written. The record and
the ledger are one act.

## Coupling

- `1a` and `2a` land together: adding rows of the second kind is what made the schema sentence's
  overreach visible.
- `3a` is independent and shared with the round settled beside this one.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| GUARDS-LEDGER-MISSES-THE-SETTLING-ROUND — six accepted findings with no row, one commit after the clause requiring one | 1 | new | accept | `74cd7e3`, `1a` | Corrected by the `74cd7e3..85d0569` round, which refuted this cell's original claim. It read "All six have rows", and the six that received rows were the accepted findings of the **`v0.4.1..de72982`** round — `IMPORTS-OUTSIDE-THE-INVARIANT` among them, which is the guard measured here: reverting the raising accessor on `check_citations.Symbols` turns its case red, at 1. The six findings **these two records** accepted received none, so this repair supplied its predecessor's rows and not its own. That is the finding's own cause, reproduced by its repair, and it is re-derived in `docs/dispositions/74cd7e3..85d0569.md`, where the rows for both rounds are added in one act |
| LEDGER-ROW-IN-THE-WRONG-TABLE — a schema sentence stated over rows that cannot satisfy it | 2 | new, shared | accept | `74cd7e3`, `2a` | The sentence says "each guarded entry" and the no-guard row is described as its own kind. The four rows that carried dashes under a dated heading moved to the table built for them |
| SETTLEMENT-NAMES-AN-ORPHAN — a record settled against an unreachable object | 3 | new, shared | accept | `74cd7e3`, `3a` | Verified independently of both inputs, with each result bound to its hash: the pre-amend object `9354d75` exists in this clone and is **not** an ancestor of HEAD, which is the defect; the settlement commit named in the cell to the left **is** reachable. Every record in `docs/dispositions/` was then swept for the class — none other names an unreachable commit |

## Carried forward

Added by the `74cd7e3..85d0569` round, with the three rows it added to Prior scope resolution above.

| Finding | Prior disposition | Original reason, unchanged | Closure condition not established |
|---|---|---|---|
| DECLINE-NOT-RE-WEIGHED | accept | a decline was left standing on evidence the file has since superseded, with its own reconsider-when arguably fired | condition 1 — `docs/guards.md` is `triage-only` in this round's input, so no gate opened over the unit and its silence is not closure |
| REACH-STOPS-AT-THE-FILE | accept | a Reach cell named a file and stopped, treating "the unit is prose" as licence to skip the unit the contract's own alternative provides | condition 1 — the records holding those cells are `triage-only` in this round's input |
| PARAGRAPH-SPLIT-NOT-REFLOWED | accept | a repair split an overlong line instead of reflowing the paragraph, so the paragraph stayed ragged and the record called it rewrapped | condition 1 — `AGENTS.md` is `triage-only` in this round's input |

## Closed

| Finding | Prior disposition | Code evidence the cause no longer holds | Input evidence |
|---|---|---|---|
| IMPORTS-OUTSIDE-THE-INVARIANT | accept | `check_citations.Symbols` exposes `imports` through an accessor that raises the reason, as `names` does | a claim reports it inside the value-or-reason invariant, verified |
| ROW-NOT-SELF-CONTAINED | accept | the `VERDICT-CONTROL` row names its own revert | a claim reports the row readable alone, verified |
| LEDGER-QUOTES-WRONG-FIGURES | accept | each row carries its own figures and names the record holding them | a claim reports the figures corrected, verified |
| SNAPSHOT-NEEDS-A-WORKTREE | accept | `distribution_manifest.install_documents` falls back to a filesystem walk | `AC-5` traces the control flow with `.git` removed and reports it holding |
| FINGERPRINT-COLLAPSED-NONE | accept | `evidence_currency.fingerprint` returns a digest or a named reason | `AC-6` traces the reason-bearing variants and reports neither collapsed into a success value |

## Out of scope this round

**LEDGER-TABLE-ROUNDS** and **RAWSCORES-PROVISIONAL**.

## Undetermined

**F-11 and F-12**.

## Recurring

**GUARDS-LEDGER-MISSES-THE-SETTLING-ROUND** is the third instance of the guards ledger falling short
of its own clause, and the first where the clause was already correct. The two before it were the
clause and the file disagreeing; this one is the clause being right and the turn not doing it. That
distinction is why no further wording repair is recorded here.

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass — answered from the prior record's own tables rather than from what this record placed, which is the correction the `74cd7e3..85d0569` round made: five in Closed, three in Dispositions of which two are shared with the record beside this one and say so, three in Carried forward, two prior ids in Out of scope this round, F-11 and F-12 in Undetermined. The answer this row first gave omitted the three accepted findings of the record named as prior, because it counted what this record had placed instead of enumerating what that record left open |
| Every accepted cause carries at least one repair with an enumerated Reach | pass — each names the file and the unit inside it, as a table name or a quoted phrase |
| Every commit this record names is reachable from HEAD | pass — checked before committing |

## What this round leaves standing

- **F-11 / F-12**, unchanged.
- **That two readings of one range keep finding different things.** This one found the missing ledger
  rows; the one beside it found the orphan commit and the ragged paragraph. Neither found the
  other's. Recorded because it is the strongest evidence in this range for what a second independent
  reading buys, and the clearest cost of a single reading.
