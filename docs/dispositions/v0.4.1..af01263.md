# Disposition Record — `v0.4.1..af01263`

**Source**: `static-review` Review Record for `v0.4.1..HEAD`
**Scope**: 37 changed files in `v0.4.1..af01263`; coverage was partial
**Prior round**: `docs/dispositions/9162b15..d8ed52f.md`

## Record integrity

| Check | Input claim | Reconciled evidence | Result |
|---|---|---|---|
| Verdict / Gate Index | FAIL at Gate 3 | Gate 3 is fail | pass |
| Calibration / Gate Index | Gates 1-7 | Gates 1-7 have pass/fail/blocked statuses; Gate 8 is not inspected | pass |
| Finding count | 8 | 1 gate row + 4 violated contract rows + 3 adverse claim rows | pass |
| Coverage | Partial with four coverage sets | "all 13 changed static-review scenario files" is not an enumeration and the range contains 11, not 13 | mismatch |
| Non-finding sections | Ledger and Structural Causes excluded | Ledger rows are producer-declared non-findings; every Structural Causes row maps to an existing finding | pass |

The eight reported rows normalize to four stable findings.

## Prior scope resolution

The prior record omits stable units and repair Reach. Combined with this round's incomplete coverage
enumeration, unmatched prior findings cannot be classified inside or outside.

| Prior finding | Scope evidence | Membership | This round's slot |
|---|---|---|---|
| F-01 – F-08, F-10 – F-15 | Unit absent from prior record; current coverage enumeration mismatched | undetermined | Undetermined |
| F-09 | Explicitly re-reported as EC-BOUNDARY | inside | Dispositions |

## Causes and candidate repairs

| # | Cause | Findings | Repair | Kind | Reach (every location it touches) | Route |
|---|---|---|---|---|---|---|
| 1 | Evidence-entry paths have no complete boundary contract; `record.parent` is implicitly trusted as an ownership root | EC-BOUNDARY / prior F-09 | 1a require relative, traversal-free `tests` paths inside the repository and `record` paths strictly below `scripts/tests/scenarios/<scenario>/` | forbid | `scripts/evidence_currency.py:127`, `scripts/tests/test_evidence_currency.py:14`, `:127`, `:284` | `plan-implementation` |
| 1 | ″ | ″ | 1b resolve both fields through the existing repository boundary abstraction, rejecting outside, unresolvable, and covering-root results | converge | `scripts/evidence_currency.py:38`, `:127`, `:183`, `:293`, `scripts/tests/test_evidence_currency.py:54`, `:284` | `plan-implementation` |
| 1 | ″ | ″ | 1c separate evidence-artifact location from an explicit scenario ownership root in the registry model, while independently bounding `tests` | distinguish | `scripts/evidence_currency.py:51`, `:72`, `:127`, `:195`, `:224`, `scripts/tests/scenarios/evidence.yaml:2`, `scripts/tests/test_evidence_currency.py:14` | `plan-implementation` |
| 2 | `section_text` treats any hash-prefixed line as a heading instead of the repository's actual CommonMark grammar | EC-HEADING | 2a implement the supported ATX-heading grammar explicitly, including required separation and valid depth | guard | `scripts/evidence_currency.py:159`, `scripts/tests/test_evidence_currency.py:63` | `plan-implementation` |
| 2 | ″ | ″ | 2b expose heading-section extraction through the existing CommonMark parser and converge evidence currency on it | converge | `scripts/markdown_links.py:18`, `:55`, `scripts/evidence_currency.py:38`, `:159`, `scripts/tests/test_evidence_currency.py:63` | `plan-implementation` |
| 3 | The release-pin matcher recognizes a semantic-version prefix without proving where the tag ends | INSTALL-TAG-BOUNDARY | 3a require an exact tag boundary after `x.y.z` and add suffix near-misses such as `.999` and `rc1` | guard | `scripts/distribution_manifest.py:130`, `scripts/tests/test_validate_skills.py:985` | `plan-implementation` |
| 4 | Workflow dependency checking models one shell spelling rather than the declared invariant over inline exact pins | WORKFLOW-PIN-SPELLING | 4a derive every `name==version` token from the workflow independently of the command prefix and compare each with requirements | derive | `scripts/runtime_contract.py:14`, `:43`, `:125`, `scripts/tests/test_runtime_contract.py:140` | `plan-implementation` |
| 4 | ″ | ″ | 4b forbid all inline exact package pins in the workflow, making `requirements-maintenance.txt` the only permitted owner | forbid | `scripts/runtime_contract.py:14`, `:43`, `:125`, `scripts/tests/test_runtime_contract.py:140`, `AGENTS.md:547` | `plan-implementation` |

## Pattern

Causes 2-4 share one shape: **a hand-written matcher claims a grammar-wide invariant while accepting
only one prefix or spelling.** The pattern-level repair is to require two negative controls for such
a matcher — a near-miss sharing the accepted prefix, and a valid alternate syntax carrying the same
meaning. That policy belongs in `AGENTS.md` beside the existing rule that a validation fixture must
fail when its rule is removed. Instance tests remain necessary.

## Coupling

- `1a`, `1b`, `1c` are alternatives; landing one complete boundary model voids the other two.
- `2a` and `2b` are alternatives. `4a` and `4b` are alternatives.
- The pattern-level testing rule complements rather than replaces the instance repairs.
- Cause 1's previous repair landed incompletely, but the prior record did not enumerate Reach, so it
  cannot be classified as recurring.

## Dispositions

| Finding | Cause | Carried | Disposition | Settled by | Verified how |
|---|---|---|---|---|---|
| EC-BOUNDARY — evidence-entry paths can escape or silence scenario ownership | 1 | carried from F-09 | accept | this round, `1a` | Eight record shapes classified before and after; three fail when the shape rule is relaxed |
| EC-HEADING — pseudo-headings truncate the fingerprinted section | 2 | new | accept | `327f7fa`, `2b` | A shebang in a fenced block no longer ends the section; two tests fail with the hand-written rule restored |
| INSTALL-TAG-BOUNDARY — a longer tag passes by semantic-version prefix | 3 | new | accept | `4a12777`, `3a` | `.999`, `rc1`, `-rc1` yielded `0.4.1` before and `[]` after; three subTests fail with the boundary removed |
| WORKFLOW-PIN-SPELLING — alternate pip invocation bypasses pin comparison | 4 | new | accept | `383fbf1`, `4a` | Four spellings compared; three fail with the old anchor restored, and the fourth is recorded as passing either way |

## Carried forward

None. F-09 was re-reported and appears in Dispositions.

## Closed

None established.

## Out of scope this round

None decidable from the supplied records.

## Undetermined

Fourteen prior findings: F-01 – F-08 and F-10 – F-15. The prior record does not preserve their stable
units or repair Reach, and this round's coverage contains an incorrect, non-enumerated scenario-file
group. Exact four-set coverage plus prior stable units would settle membership.

F-12 retains its prior `defer` decision and reason: its 76-character subject is already published and
was recorded as a consequence of the unresolved commit-partition cause. Its lifecycle membership
remains undetermined here.

## Recurring

None. F-09 survived a repair, but the prior record contains no enumerated Reach with which to prove
the complete repair landed.

## Ungrouped

None.

## Self-check

| Check | This record's answer |
|---|---|
| Every prior id sits in exactly one exclusive lifecycle home | pass |
| Every accepted cause carries at least one repair with enumerated Reach | pass |

The Reach column was dropped when this record was first persisted while that answer was left
reading `pass`. Restored from the produced record rather than re-derived, because a self-check that
survives its own evidence being removed is the failure this file exists to prevent.

## What this round leaves standing

- The **pattern-level rule** (two negative controls for a grammar-claiming matcher) is not yet in
  `AGENTS.md`.
- Fourteen prior findings stay `undetermined` until a prior record preserves stable units and Reach.
  The prior record at `docs/dispositions/9162b15..d8ed52f.md` is a digest, not a record, and its
  filename names a range twelve commits wider than what it settled.
